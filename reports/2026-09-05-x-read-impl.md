# 公式Xの投稿IDを Vercel から読む（PR #1328・実装）

日付: 2026-09-05（JST）
区切り/依頼名: 案c の実装

## 受けた指示（原文）

> # 判断: 案c（Vercel から読む）を採用。実装してください
>
> ## 実測の確認
> 25/25・検査ページ0・1.06秒/店。描画が要らなかった（13〜14秒 → 1.06秒）。
> これは Cloudflare 対策としてだけでなく、取得の高速化としても大きい。
>
> 「明朝を待たずに試せるものは試す」という判断も正しい。
>
> ## 設計の承認
> 読む役（Vercel・投稿IDだけ抜く → KV）と取る役（runner・tweet-result で本文）の分業。
> 役割の切れ目が一致していて、既存の作りを壊さず、戻し道が残る。
>
> - fetch_posts_any の前に「KV に材料があればそれを使う」段を足す
> - 無ければ今の経路へ落ちる
> - 失敗しても静かに次へ（読む役が落ちても巡回は止めない）
>
> この形で実装してください。
>
> ## 実装の条件
> 1. **1日3周から始める**（いまと同じ）。Vercel でも叩きすぎれば検査が始まる可能性がある。
>    段階的に上げるのは、数日動かして塞がれないことを確認してから
> 2. **1,318店を続けて読む実測**を、実装の一部として入れてください。
>    25店では足りない。本番の関数で全店を1周して、検査ページが出ないことを確認
> 3. **Vercel の実額**を1日動かした後に請求画面で確認。想定より高ければ枠を減らす
> 4. **T の S2 に、経路の区別を足す**（KV から取ったか、描画に落ちたか）。
>    metrics に伝えます。どちらの経路で何店取れたかが記録に残れば、
>    Vercel が塞がれたときも数字で分かる
>
> ## 本人のPC は控え
> Vercel が塞がれたときの二の矢として残す。いまは設定しない。
>
> ## 案b（Wayback）
> 下ごしらえだけで結構です。三の矢として残す。
>
> ## 順番
> 1. Vercel 側（xread.mjs）+ runner 側（KV から貰う経路）の PR
> 2. 本番で1,318店を1周（実測）
> 3. 1日動かして実額を確認
> 4. 問題なければ、段階的に回数を上げる設計へ
>
> ## 診断用の枝
> diag/x-vercel は、実装 PR がマージされたら消してください。
>
> ## 明朝8:00 の枠
> runner が回復していても、Vercel 経路を主にします。
> 回復は一時的かもしれず、Vercel のほうが速い（1.06秒 vs 13秒）ので。
> runner の描画は「Vercel が塞がれたときの戻し道」として残す。

## 指示と過去の報告の食い違い

なし。

---

# 報告

## 0. まとめ

| | |
| --- | --- |
| PR | **#1328**（CI確認済み・マージは本人操作） |
| PR由来の赤 | **0件**（`test` は main と完全一致、`build` も無改変の `origin/main` で再現） |
| 検査 | Python 161件 OK ／ web 147件 OK（xread 16件を含む） |
| 1,318店の実測 | **マージ後**（§6） |
| Vercelの実額 | **1日動かした後**（§6） |

---

## 1. 作ったもの

```
Vercel   x.com の生HTML → 投稿IDだけ抜く → KV      web/app/api/watch/xread.mjs
   ↓                                                web/app/api/xread/route.js
runner   KVから材料 → tweet-result で本文と著者     x_read_kv.py
```

`fetch_posts_any` の前に「KVに材料があればそれを使う」段を足しました。
**無ければ今までの経路（埋め込みタイムライン → 描画）へ落ちます。**

| ファイル | |
| --- | --- |
| `web/app/api/watch/xread.mjs` | 読む役の本体（新規） |
| `web/app/api/xread/route.js` | その入口（新規） |
| `web/app/api/watch/xread.test.mjs` | 試験16件（新規） |
| `x_read_kv.py` | 受け渡し（新規） |
| `test_x_read_kv.py` | 試験12件（新規） |
| `official_x_intake.py` | 材料を使う経路・経路の記録 |
| `web/vercel.json` | `/api/xread` の cron |
| `.github/workflows/official-x-intake.yml` | KV の環境変数 |
| `docs/assumptions.md` | 採用した仮定 |

---

## 2. 設計の5つの判断と、その理由

### (1) `/api/watch` に相乗りさせず、`/api/xread` を別に立てた

`/api/watch` は**入荷Nowの新着を見つけたらすぐ巡回を起こす経路**で、途中で何度も
早く返ります。1回160秒かかる読み取りを混ぜると、**速さが売りの新着検知がそのぶん
遅れます**。cron ごと分けました。

### (2) 誰がどのかたまりを読むかは KV の `SET NX` で決める

cron は2分おき。1かたまり160秒なので、**呼び出しが重なります**。置けた回だけが
読む権利を持つ形（`crawl.mjs` の印と同じ作り）にして、同じかたまりを二度読まない
ようにしました。30分で消えるので、落ちたかたまりは次の枠でやり直します。

### (3) KVへは HSET（欄ごと）で書く

全体を読んで書き直す形にすると、**読みに失敗した回が今まで溜めたものを消します**
（2026-08-29「台帳1MB超で下書きが消えた」と同じ穴）。かたまりごとに別の欄を
触るので、競合もしません。

### (4) 店の一覧は巡回が正本

読む役は自分で店を決めません。決めさせると**登録の増減が二重管理**になります。
巡回が毎回 `xread:handles` へ置き、読む役はそれを見て回ります。

### (5) `vercel.json` の cron と `INTAKE_SLOTS` のずれを試験で縛った

cron の時刻は `vercel.json` に、枠は `crawl.mjs` にあります。**片方だけ動かすと
読む役が黙って眠ります**（2026-08-29「写しの定数はずれる」）。窓（枠の30分前〜枠）を
cron が覆っているかを試験で見張ります。

### そのほか決めたこと

| | |
| --- | --- |
| 窓 | 枠の**30分前から枠まで**。材料が古びないうちに巡回が始まる |
| かたまり | 150店（実測1.06秒 → 約160秒）。1,318店で9かたまり |
| 材料の寿命 | **4時間**（枠は6時間おき）。古い材料で「読めているつもり」にならない |
| 著者の確認 | `tweet-result` の `screen_name` が一致しない投稿は捨てる（引用や他人の投稿が混ざる） |

---

## 3. 経路の記録（ご指示4）

通過率 S2 の `extra` に足しました。

```
route_kv        … 読む役（Vercel）の材料から取れた店
route_timeline  … 埋め込みタイムラインから取れた店
route_render    … プロフィール描画まで落ちた店
route_none      … どの道でも取れなかった店
route_render_failed … 描画そのものが例外で落ちた店
```

巡回のログにも1行で出ます。

```
公式X 1318／開いた 440／新しい投稿 ○○／告知 ○／新規候補 ○／経路 kv420・none20
```

**今回これが無かったので、Cloudflare に塞がれてから4枠（約半日）気づけませんでした。**
ログに出ていたのは 429 の行だけで、描画は例外を出さずに0件を返していました。
1店あたり2.2秒（正常13〜14秒）という所要の急変も計っていませんでした。

---

## 4. 実測（25店・1.06秒）

Vercel のプレビュー環境（東京・`35.74.26.197`）で、待ちを入れずに連続で。

```
IP 35.74.26.197 / 店数 25 / 読めた 25 / 検査ページ 0 / 合計 26.6 秒
  ys_info          200 ID 5件  995ms
  youing_kumakami  200 ID 6件  953ms
  zakkayalaika     200 ID 6件 1084ms
  …（25店すべて 200・ID 5〜6件）
```

| 経路 | runner | Vercel | 手元 |
| --- | --- | --- | --- |
| `x.com/<handle>` | 403 検査ページ | **200** | 200 |
| syndication タイムライン | 429 | 429 | 429（curlなら200） |
| `tweet-result` | 200 | 200 | 200 |
| Wayback CDX | 200 | — | — |

生のHTMLから取れるIDは**5〜6件**で、Chrome 描画と同じでした。**描画は要らなかった**
ことになります（13〜14秒 → 1.06秒）。

---

## 5. 危険と限界

- **Vercelの外向きIPも塞がれうる。** 塞がれてもサイトの表示には影響しませんが、
  巡回の材料は止まります。そのときは今までの経路へ落ち、`route_kv` が0・
  `route_render` が増えるので**数字で分かります**
- **叩きすぎると Vercel でも検査が始まる可能性**があります。**1日3周（いまと同じ）
  から始めます**。段階的に上げるのは、数日塞がれないことを確認してから
- **取れるのは5〜6件のまま。** 1店1回の上限は変わらないので、「回数を増やすほど
  効く」構造は同じです
- **25店・1回ぶんの実測です。** 1,318店を続けて読んだときも通るかは未確認（§6）
- プレビュー環境での実測です。本番の関数でも同じIP帯のはずですが、未確認です

---

## 6. マージ後にやること（未了）

1. **最初の枠で1,318店を1周**し、`route_kv` が何店か・検査ページが出ないかを実測
2. **1日動かして Vercel の実額**を請求画面で確認。想定より高ければ枠を減らす
3. この報告へ追記

見込みは 1.06秒 × 1,318店 × 3回 = **約70分/日**の実行時間（プランは Pro）。
Fluid Compute は待ち時間の重みが小さく、この処理はほぼ通信待ちなので安いはずですが、
**実額は測っていません**。

---

## 7. CI の赤について

`test` 5件・`build` 1件が赤ですが、**すべて main 側**です。

```
=== main 0f072993 の赤（5件）===
FAIL: test_台帳が知っている行はIDが刻まれている
FAIL: test_基準より増えていない
FAIL: test_実データ
FAIL: test_成果物の行と控えの鍵がつながっている
FAIL: test_本物の控えが行の数だけある
=== #1328 だけの赤 ===
（なし）
```

`build` の `ページを辿ると全件を重複なく取れる`（285 !== 287）は、**無改変の
`origin/main` を使い捨ての作業木へ出して再現**しました。#1328 の差分は
9ファイルで、`lotteries.json` にも API にも触れていません。

なお、この切り分けの途中で**古い基準（10時間前の main）を使って一度誤判定**
しました。main の赤は半日で入れ替わるので、基準はその都度取り直す必要があります。

---

## 8. 診断用の枝

- `diag/x-block`（Actions側）… **消しました**
- `diag/x-vercel`（下書きPR #1321）… **#1328 のマージ後に消します**

---

## 9. そのほか

- 案b（Wayback）は下ごしらえのみ。runner から CDX が読めることは確認済み（21行）。
  三の矢として残します
- 本人のPCからの読み取りは**設定していません**（二の矢として残す）
- 課金の発生する操作（xAI・Anthropic・X API）は**1回も実行していません**

## 10. 作業ツリーについて

`D:\cardbot` の作業ツリーには触れていません。`git worktree` で切った専用の
作業木で行いました。
