# 画面 7 社（実時刻）／上流 34 社。**コホートの「配布 0」は私の測り方の失敗。系列と 26 社の結論は影響なし**

セッション名: metrics
日付: 2026-09-08（JST・00:40 時点）
区切り/依頼名: 測り方の失敗を受けた測り直し（系列・26社・コホート・リードタイム）
対象: `origin/main` = `a24c1837` ／ PR [#1444](https://github.com/shinonomeheta-ai/cardbot/pull/1444)・[#1443](https://github.com/shinonomeheta-ai/cardbot/pull/1443)

## 受けた指示（原文・要点）

> ci が実物で割り切った。「配布 0」は切断ではなく、大半が測り方の失敗だった。…そちらの数字のうち同じ判定を使っているものは全部、実態より小さい: 1. 7日系列の「入り」 2. 26社の関門分類 3. コホートの「配布」列。
> 【もう1つの測り方の問題】リードタイムの中央値を出すこと。7日を超えているなら窓が短すぎる。窓を14日または21日にした系列も併せて出すこと。

## 報告

### 0. 結論を先に

**3 つのうち、影響があったのは 3 だけです。**

| 指示 | 依存 | 結果 |
|---|---|---|
| 1. 7 日系列の「入り」 | **依存なし** | `lotteries.json` の店名を直接数えており、対応表を使っていません。**「7 日のうち 6 日、新しい本部が画面に 1 社も入っていない」は事実のままです** |
| 2. 26 社の関門分類 | **依存なし** | 27 社は全部 V5 の手前か V5 で止まっており、刻印の段に届いていません。対応表を使わずに確かめ直しました（下の 2） |
| 3. コホートの「配布」列 | **依存あり・誤り** | 訂正します（下の 3）。PR #1444 |

### 1. 系列が対応表に依存しない理由

7 日系列は各日の `lotteries.json` を読み、**店名が `store_keys` と一致する行**を数えています。応募回 → 配布行の対応表（`shadow_round_links`）は 1 度も引いていません。**画面そのものを数えているので、切断があっても数え落としません。**

したがって「9/6 の +12 以外に新しい本部が入っていない」は測り方の問題ではありません。**9/6 の原因（#1349「EC の人待ち関門を外す」）も変わりません。**

### 2. 26 社の分類（対応表を使わずに確かめ直した）

止まっている社の応募回のうち、**`unsendable_reason` が空で・V5 の結果があり・`likely_valid` の回**を持つ社を数えました。刻印も対応表も見ていません。

```
止まっている社のうち、V5 を likely_valid で抜けた回を持つ社: 0 社（0 回）
```

**「刻印まで到達している社は 0」は対応表に依存しない事実です。** 27 社は全部その手前で止まっています（材料 20 社・処理量 7 社。`rounds_of` 補正後）。

### 3. コホートの「配布」列は誤りでした（訂正・PR #1444）

対応表だけで見ていたため、**08-31 以降のコホートは配布がほぼ 0** と出ていました。理由は 2 つで、どちらも ci の指摘どおりです。

- **刻印を通らずに配布へ出る経路が実在する**（バトロコ金沢駅前は刻印 0 で配布行あり）。
- **`shadow_round_links` が 08-31 以降の回にほとんど付いていない**（下の 5）。

| first_seen | new | 配布（対応表だけ・**誤**） | **配布（どちらかで・正）** |
|---|---:|---:|---:|
| 09-07 | 303 | 0 | **5** |
| 09-06 | 205 | 0 | 0 |
| 09-05 | 334 | 0 | **20** |
| 09-04 | 130 | 0 | **2** |
| 09-03 | 150 | 0 | **30** |
| 09-02 | 136 | 0 | **17** |
| 09-01 | 121 | 1 | **37** |
| 08-31 | 143 | 2 | **10** |
| 08-30 | 64 | 18 | 22 |

**配布へ出る経路は死んでいません。毎日 2〜37 回が出ています。** 死んでいるのは「**新しい本部（M1g）の社**が増えること」だけです。両立します——出ている回の多くは、既に画面にある店や本部でない店の回だからです。

**私の誤り**: 「7 日ぶん全部が同じ（kept があるのに配布 0）」という読みは、私の測り方が作った像でした。訂正します。

### 4. リードタイム（窓を 21 日へ・PR #1444）

候補台帳に入ってから配布に出るまで（現役の回・n=16）:

| | 値 |
|---|---|
| 中央値 | **6.0 日** |
| 平均 | 8.2 日 |
| 分布 | 0 日 2・1〜3 日 1・4〜7 日 6・**8〜13 日 6**・14 日以上 1 |

**7 日の窓では足りません。** 8 日以上が 16 件中 7 件（44%）で、「まだ出ていない」と「もう出ない」が区別できません。黄鶏屋（大阪）の 13 日はこの分布の中にあります。

**窓を 21 日にしました**（`COHORT_DAYS = 21`）。21 日ぶんの系列は上の表のとおりで、08-27〜08-31 のコホートは配布 12〜47 回まで伸びています。

### 5. 新しく見つけたこと: 対応表が 08-31 以降ほとんど付いていない

コホートの「対応表だけ」の列がそのまま証拠です。08-30 以前は 14〜34 回が対応表で当たるのに、**08-31 以降は 0〜2 回**。刻印から配布行への結び付け（V7）が新しい回に付いていません。

**これは切断ではなく「対応表が育っていない」状態**です（配布へは別経路で出ている）。ただし T の判定⑦（行の主張と main の実物）と、管理画面の切り分けはこの表を見るので、**ci・store-name-a の領分として渡します**。

### 6. 出した PR

| PR | 何を |
|---|---|
| [#1443](https://github.com/shinonomeheta-ai/cardbot/pull/1443) | 抜粋の関門の母数を `rounds_of` へ（superseded 80 を除く。送れる 1,602→1,539・`no_excerpt` 963→946） |
| [#1444](https://github.com/shinonomeheta-ai/cardbot/pull/1444) | コホートの「配布」を対応表と店×商品のどちらかで当てる／窓を 21 日へ |

## 状態

PR #1443・#1444 CI 待ち（通れば自分でマージ）。**渡すもの**: 対応表が 08-31 以降付いていない（ci・store-name-a）。**変わらない結論**: 新しい本部の社は 9/6 の +12 以外に増えていない・26 社は V5 の手前で止まっている・9/6 の増加は関門が 1 つ開いたこと。
