# レビューへの回答（社名の確定・公式X探索の実測・触るファイル・重複除去・CI）

**本部（M1g）の受付中: 8社（前回 2026-09-07 夕の測定と同じ 8社・増減なし）**
フルコンプ2・ポケモンカードストア2・晴れる屋2 4・ドラゴンスター2・コジマ1・ファミマオンライン1・WonderGOO1・GIRAFULL2

セッション名: api-customer
日付: 2026-09-07（JST）
区切り/依頼名: レビューの最優先3点と回答が要るもの a〜d
対象: `origin/main` = `5a426b12`

## 受けた指示（原文・要点）

> 【最優先・今すぐ】1. 社名の確認（イオン九州と iAEON は同じ社か）／2. 公式X探索でも同じ打ち切りが起きていないか実測／3. `search_any` を呼ぶ場所を全部列挙
> 【回答が要るもの】a. 「12社に効く」の根拠／b. 全体 CI の結果／c. 複数エンジンの結果を足すときの重複除去／d. 429
> 先に「どの PR が何のファイルを触るか」を答えること。台帳に触る PR があれば移行完了まで待つ。

## 0. どの PR が何を触るか（移行との関係）

| PR | 触るファイル | 台帳（データ）に触るか |
| --- | --- | --- |
| #1437 文字コード | `html_charset.py` `linked_page_cache.py` `url_quality.py` `ec_lottery_watch.py` `candidate_evidence_fetch.py` `test_html_charset.py` | **触らない**（.py のみ） |
| #1440 検索の道 | `discover_official_x_web.py` `ec_lottery_watch.py` `test_discover_official_x_web.py` | **触らない**（.py のみ） |
| #1432 site_blocked | `vocab_master.py` `test_golden_chains_data.py` **`web/public/data/golden_chains.json`** `vocab_history.jsonl` `web/app/lib/vocab.gen.mjs` | **触る**（M1g 台帳） |
| #1433 ToysRus | **`store_x_accounts.json`** | **触る**（X 登録の台帳） |
| #1438 ヤマダ link | **`ec_lottery_sources.json`** | **触る**（EC巡回の台帳） |

**#1437 と #1440 は .py だけなので、移行と competing しません。** #1432・#1433・#1438 は JSON の台帳を触るので、鍵1,901本の追記が終わるまで待ちます。

（ci の移行が触るのがどのファイルかは私からは見えていません。`store_aliases.json` などであれば #1433・#1438 も競合しない可能性がありますが、**安全側に倒して3本とも待ちます**。移行対象のファイル名を教えていただければ、競合しないものだけ先に出せます。）

## 1. 社名の確定（最優先1）— **同一社です**

台帳を横断して引きました。

```
M1g（golden_chains.json）  canonical_name: "iAEON"
                           store_keys: ["イオン九州 (iAEON)"]
EC巡回（ec_lottery_sources） name: "イオン九州（iAEONアプリ）"  list: aeon-kyushu.info/customer/information/
公式X（store_x_accounts）   "イオン九州 (iAEON)"      → aeon_kyushu
                           "イオン九州（iAEONアプリ）" → aeon_kyushu   ← 同じハンドル
サイト（store_sites）       "イオン九州 (iAEON)"      → aeon-kyushu.com
                           "イオン九州（iAEONアプリ）" → aeon-kyushu.info
                           "イオン九州"              → iaeon.aeon-kyushu.info
```

**同じ社です。** M1g のチェーン名が `iAEON`（アプリ名）で、`store_keys` と EC巡回・X・サイトの登録名が `イオン九州（iAEONアプリ）` 系です。台帳が両者を同じものとして結んでいます。

私の報告で表1に「イオン九州」、効き目に「iAEON」と書き分けたのは、**表1が EC巡回の登録名、効き目が M1g のチェーン名だった**ためです。測定と効き目は同じ社です。

**表記は `iAEON`（M1g のチェーン名）に統一します。** 以後の報告はそうします。台帳の登録名を変えるかは別の判断です（`store_keys` を触ると鍵の照合に効くので、移行中は触りません）。

## 2. 公式X探索の実測（最優先2）— **取りこぼしは 0本でした**

`{店名} X` のクエリで、4エンジンそれぞれのハンドル取得数を4社で測りました。

| 店 | Yahoo | DDG | DDG lite | Brave | Yahoo で打ち切ると失う |
| --- | --- | --- | --- | --- | --- |
| イトーヨーカドーネットスーパー | 471KB・1本 | 14KB・0本 | 14KB・0本 | 429 | **0本** |
| ヤマシロヤ | 583KB・2本 | 14KB・0本 | 14KB・0本 | 429 | **0本** |
| 竜星のPAO | 395KB・4本 | 14KB・0本 | 14KB・0本 | 429 | **0本** |
| GAME ARC宝島 | 367KB・3本 | 14KB・0本 | 14KB・0本 | 429 | **0本** |

**公式X探索では、いまのところ Yahoo で足りています。**

理由が分かります。**クエリの形が違います。**

```
公式X探索   "{店名} X"                    site: 制限なし → Yahoo が普通に返し、ハンドルも取れる
EC巡回      "site:<host> 抽選 カードゲーム"  site: 制限あり → その host の該当ページが
                                          Yahoo のインデックスに無ければ 0本になる
```

したがって「official_route が47件中1件しか当たらない」の原因は、**この打ち切りではありません**（少なくともこの4社では）。原因は別のところにあります。

**ただし2点、限界があります。**
- **Brave が4社とも 429** で試せていません。Brave が返せば増える可能性は残ります。時間を置いて測り直します。
- 4社しか測っていません。store-name-a の47件の対象社そのものは測れていません（社名が分からないため）。**対象の47件のリストをいただければ測ります。**

「片側配線」のご指摘は受け取ります。`candidates_for` に `十分か` を渡す配線は**入れていません**（実測で取りこぼしが 0 だったため）。入れるべきかの判断をお願いします。入れる場合、`{店名} X` の `十分か` は「ハンドルが1本でも取れたか」になります。

## 3. `search_any` を呼ぶ場所（最優先3）— **2か所です**

| # | 呼ぶ場所 | クエリ | `十分か` |
| --- | --- | --- | --- |
| 1 | `discover_official_x_web.candidates_for(store, search=search_any, …)` の既定引数 | `{店名} X` と `site:bookoff.co.jp/shop {店名}` | **渡していない** |
| 2 | `ec_lottery_watch.検索で一覧` | `site:{host} {query}` | **渡している**（#1440） |

`candidates_for` を呼ぶのは `discover_official_x.py:276` と `x_ai_pick.py:76` の2か所です（どちらも `search` を差し替えていないので、既定の `search_any` が使われます）。

試験からの呼び出しが3か所（`test_discover_official_x_web.py`）。

**渡した場所 1／渡していない場所 1** です。

## 4. 回答が要るもの

### a. 「12社に効く」の根拠 — **実測は3社のみ。訂正します**

「12社に一度に効く」はレビュー担当の判断文の表現をそのまま使ったもので、**私の実測ではありません**。私が測ったのは iAEON・HMV・イエローサブマリンの3社です。

```
iAEON              0件 → 4件
HMV                5件 → 5件（Yahoo で足りる）
イエローサブマリン   0件 → 0件（Brave 429・サイトに抽選記事が無い可能性）
```

**#1440 マージ後の巡回で12社すべてを測り、社ごとに前後の本数を出します。**

対象の12社（seen 0 かつ search 無し 13社から Joshin を除く）:
iAEON・イオンスタイルオンライン・エディオン・トイザらス・ドラゴンスター・ジャスティス・ヤマシロヤ・平和堂・サンエーアプリ・イエローサブマリン・フルコンプ・ホビーゾーン

**ただし注意が1つ。** この12社は**そもそも `search` を持っていません**（だから検索の道に入らない）。#1440 は「search を持つ社の検索が Yahoo で打ち切られる」のを直すもので、**12社に効かせるには `search` を足す必要があります**。#1440 単体では、この12社の数字は変わりません。

効くのは**いま `search` を持つ7社**（あみあみ・HMV・駿河屋 通販・HobbyLink Japan・カーナベル・TCG shop193・DMMマイカ）と、今後 `search` を足す社です。iAEON は私が実測のために一時的に `search` を渡して測りました（台帳には入れていません）。

**この食い違いを先に報告すべきでした。** 12社に効かせるなら「#1440 ＋ 12社への `search` 追加」の2つが要ります。

### b. 全体 CI — **#1432 に PR 固有の赤がありました。直しました**

| PR | test（Python） | build（web） | vocab | 判定 |
| --- | --- | --- | --- | --- |
| #1437 | FAILURE | **run なし** | run なし | main と同じ赤か要確認 |
| #1440 | FAILURE | run なし | run なし | 同上 |
| #1432 | FAILURE | FAILURE | **FAILURE** | **PR 固有の赤があった** |
| #1433 | FAILURE | run なし | run なし | main と同じ赤か要確認 |
| #1438 | FAILURE | run なし | run なし | 同上 |

**#1432 の `vocab` が PR 固有の赤でした。** 「intake_route: 履歴の再生とマスターが食い違う」。`vocab_master.py` に値を足しただけで、`vocab_history.jsonl` と `web/app/lib/vocab.gen.mjs` を更新していませんでした（**語彙は3点セット**という既存の決まりの漏れ）。`433147cf` で直し、`test_vocab_master` 5件・`gen_vocab_js --check` とも緑になりました。

**ご指摘のとおり「run が作られない」現象が出ています。** .py だけの PR では `web-build-check` が起動しません（paths の絞り込み）。赤/緑を見る前に run の有無を見る、を守ります。

**main の基準を起こしました**（`gh workflow run python-test.yml --ref main`・run 34119669609）。結果が出たら PR の test と差分を取って「新規の赤が無い」を判定し、追報で出します。**それまでマージしません。**

### c. 重複除去 — **HTML は重複しますが、取り出す側で消えます**

`search_any` は複数エンジンの HTML を `"\n".join` で足すので、**HTML 自体は重複します**。ただし取り出す側で消えます。

```
handles_in()   docstring に「順番どおり・重複なし・小文字」。out に無いものだけ足す
記事リンク()    if u not in out: out.append(u)
```

したがって**結果に重複は出ません**。増えるのは扱う文字列の大きさだけです（イオン九州で 398KB＋204KB＝約600KB）。

### d. 429 — **明日の実測で見ます**

今日の測定でも Brave は 429 が頻発しました（公式X探索4社・EC巡回2社とも）。#1440 は「足りなければ次へ進む」ので、**足りている社では追加の検索を叩きません**（試験で固定）。増えるのは「Yahoo で足りない社」の分だけです。

明日 8:00 からの x-intake 4回運用（告知 51→143）と合わせて、429 の数を測って報告します。

## 5. ヤマダの画像レーン

ヤマダ1社では判断しない、を了解しました。ci の no_excerpt 801件の分類と突き合わせて「本文が画像で抜粋が作れない社が全部で何社・何件か」を出します。**ci の分類が出た時点で声をかけてください。**私からは ci セッションを特定できません。

## 6. イエローサブマリン

store-name-b の「href 末尾の空白の不具合で seen 0 だった4社」（#1431 マージ済み）と突き合わせます。Brave の 429 が明けたら測り直し、結果を追報で出します。

## 7. 実行したこと・していないこと

- 実行: 社名の横断確認、公式X探索の4エンジン実測（4社）、`search_any` の呼び出し元の全列挙、PR 5本の触るファイルの一覧、CI の状態確認、#1432 の語彙の3点セットの修正、main の基準 run の起動。
- していない: PR のマージ（main の基準待ち）、12社の実測（#1440 マージ後）、`candidates_for` への `十分か` の配線（判断待ち）、Brave の再測定（429 待ち）。費用 0 円。
