# ①#1463 マージ ②上流の食い違いは「締切の絞り」で確定・数え方を1か所に ③まとめX 4口は読めた（合図は 0）

日付: 2026-09-08（JST・12:35 時点）
区切り/依頼名: いま回すこと3件
セッション名: roundup

## 冒頭（指定の形）

| | |
|---|---|
| **画面** | **7 社 11 行**（2026-09-08 12:30 JST・実時刻・`store_keys` の完全一致・支店を数えない） |
| **上流** | **19 社 35 回**（同じ規則・母数は `rounds_of`） |

## 受けた指示（原文）

> ① #1463 をマージすること。CI は基準と完全一致で PR 固有の赤は無い。
> ② 上流の定義を metrics と直接突き合わせること。
>    metrics が「store_keys の完全一致だけでも29社116回なので寄せ方の差ではない」と返しており、
>    そちらの「metrics の30社は定義B」という説明と食い違う。
>    同じ時点・同じ母数で回して、どちらが誤っているかを確定させること。
>    これが揃わないと冒頭の数が今後もばらつく。
> ③ まとめX の4口（HTTP 403）を読む手を試すこと。
>    次の official-x-intake を待たない。読めれば合図の差分が出せる。

## 報告

根拠データ: [2026-09-08-three-tasks.json](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/data/2026-09-08-three-tasks.json)

### ① #1463 マージ済み（`94aca4c9`）

判定の段で書いた「記録だけ」の行が、押す前の `reset --hard` で消えていた件です。ci も独立に数えて PR 固有の赤 0 を確認しています。

### ② 食い違いは「締切の絞り」で確定。**どちらも誤っていません**

同じ時点（12:24）・同じ母数（`rounds_of`）で、条件を 1 つずつ外して測りました。

| 寄せ方 | 生死 | 締切 | 社 | 回 |
|---|---|---|---:|---:|
| **本部だけ**（`store_keys` 完全一致） | 回が active | **実時刻で今以降** | **19** | **35** |
| 本部だけ | 回が active | 絞らない | 30 | **110** |
| 本部だけ | 候補が active | 絞らない | 31 | 141 |
| 本部だけ | 絞らない | 絞らない | 47 | 340 |
| 支店を親に寄せる | 回が active | 実時刻で今以降 | 27 | 129 |
| 支店を親に寄せる | 回が active | 絞らない | 35 | 429 |

**寄せ方を本部に固定したまま締切の絞りだけ外すと、35 回 → 110 回**になります。metrics の **116 回はここに乗ります**。支店を寄せる軸では 129 回・429 回にしかならず、**116 回はどう組み合わせても作れません**。

したがって:

- metrics の「完全一致だけでも 29 社 116 回なので**寄せ方**の差ではない」は**正しい**
- 私の「差は**締切の絞り**」も**正しい**
- **両者は別の軸を指しており、矛盾していません。** 私の前の報告が「metrics は定義 B」と書いたのは、まさにこの「締切で絞らない」行のことです。metrics は寄せ方の話として読み、私は締切の話として書いていました

**どちらが誤っているか、という問いへの答えは「どちらも誤っていない」**です。誤っていたのは、3 つの別の量に「上流」という 1 つの名前を付けていたことです。

**数え方を 1 か所に置きました（PR #1465・`upstream_count.py`）。**

    py upstream_count.py          正本の数え方だけ（冒頭に書く数）
    py upstream_count.py --grid   条件を1つずつ外した総当たり

今後、上流や画面の数を出すセッションはこれを回してください。食い違ったら `--grid` を同じ時点で突き合わせれば、どの軸かがすぐ出ます。試験 9 本（実データは触らず合成の表と台帳）。

### ③ まとめX 4 口は**読めました**。ただし**合図は 0**

**KV の経路は今は起こせません。** 読む役の cron は UTC の 2・6・10・22 時台だけで、いまは窓の外です。`/api/xread` は `CRON_SECRET` が要り、手元にありません。

そこで**この PC のブラウザで 4 口を直接読みました**（runner は Cloudflare に弾かれますが、ブラウザは通ります）。**4 口とも本文が取れました。**

取れた本文を**合図の判定器にそのまま通した**結果:

| 口 | 当たった店 | この面では新しい | うち公式が無い社 |
|---|---:|---:|---:|
| PokeGetInfoMain | 4 | 3 | 2 |
| ONE_PIECETCG | 1 | 0 | 0 |
| dbfw_cardgameJP | 0 | 0 | 0 |
| Meisy_TCG_News | 0 | 0 | 0 |

**17 面すべてで数え直しても、合図は 0 社です。** 理由は明確で、**新しく店を載せた面が 1 つしかない**からです（合図の条件は 2 面以上）。07:58 の回で 13 面が新しく見た店は 0 でした。

つまり **「読めていないから 0」ではなく、「読んでも 0」** でした。合図 0 の理由が確定しました。

**新しく見つけた穴が 1 つあります。** PokeGetInfoMain の本文には **ヨドバシドットコム**と **GEO(ゲオ)** が受付開始として載っていますが、**判定器がどちらも当てられません**。

| 本文の書き方 | 当たるか |
|---|---|
| `ヨドバシドットコム` | ✗（`ヨドバシカメラ` の変種に無い） |
| `ヨドバシ.com` | ✗ |
| `GEO(ゲオ)` / `ゲオ` | ✗（`GEO` の変種は `geo` だけ） |
| `ヨドバシカメラ` | ○ |

**この 2 社は、#1457 で対象に入れ直したばかりの 4 社のうちの 2 社**です。まとめが実際に使う書き方を当てられないので、対象に入れても拾えません。**取得・照合を広げる作業は優先しない**という判断を受けているので直していません。指示があれば別名 4 本を足すだけの小さい PR で出します。

## 実行したこと・していないこと

- #1463 をマージ（`94aca4c9`）
- 総当たりの実測と `upstream_count.py`（PR #1465・CI 待ち）
- ブラウザで 4 口を読み、判定器に通して測定（**台帳は書いていません**・課金 0）
- 別名の追加はしていません（優先しない判断に従う）
- `roundup-daily` の schedule はそのまま。収集元 3 つは止めたまま

## 判断が要る点

1. **ヨドバシドットコム・GEO(ゲオ) の別名を足すか**（4 本足すだけ。足さないと #1457 で入れ直した 4 社のうち 2 社は永久に拾えません）
2. まとめX の 4 口を runner から読めるようにするか。KV の経路は次の窓（UTC 6 時台＝JST 15 時台）で自然に通る見込みです。通らなければ `roundup-daily` の中で KV へ口を配る一行を足すのが最短です
