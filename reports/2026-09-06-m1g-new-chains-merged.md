# #1351 のマージと、カードの枠の確認

セッション名: store-name-a
日付: 2026-09-06（JST）
区切り/依頼名: 連結形・各店形の鍵と新設2チェーンのマージ
対象: `origin/main` = `5a5bc62c`（マージ後）

## 受けた指示（原文）

> # #1351 をマージしてください（自分で）
> 商品ごとの突き合わせを私がやりました。
> BOX 4/20店、デッキ 3/17店。残りの大半が ai_not_run で、
> これは roundup が特定した「根拠キャッシュが 09-04 から止まっていた」が原因。
> キャッシュが直って AI審査が回れば、6〜7店が通る見込み。
> #1351 で6店が M1g に紐づく。マージして、巡回後にカードの枠が出ることを確認。

## 指示と過去の報告の食い違い

**1点あります。カードの枠は巡回を待たずに出ます。**

枠は `golden_chains.json` から直接出るので、**マージした時点で出ています**
（巡回が要るのは「受付中◯件」になる配布行のほう）。下の §2 は**マージ後の main の
実データ**で測った結果で、巡回は挟んでいません。

## 1. マージ

```
merge commit  5d2b44c6
```

2度、巡回との競合でマージできませんでした（`store_x_accounts.json` と
`shadow_url_owner.json` を巡回が書き換えるため）。main を取り込み直し、
`shadow_url_owner.json` は `--raw` で作り直しています。

CI は **main の失敗5件と完全一致（PR固有の赤 0）**。

## 2. カードの枠 — **6店とも出ています**

マージ後の main の実データを、画面と同じ関数（`goldenRows` / `stateLabel`）で
そのまま通しました。

```
カードの枚数  54（52 ＋ WonderGOO・GIRAFULL）   今日(JST) 2026-09-06

Bee本舗      枠=出る  現在受付中の抽選はありません   鍵 2本
プレイズ       枠=出る  現在受付中の抽選はありません   鍵 1本   ← pending が解消
フルコンプ      枠=出る  現在受付中の抽選はありません   鍵 6本
晴れる屋2      枠=出る  **受付中 2件**              鍵 3本
WonderGOO   枠=出る  現在受付中の抽選はありません   鍵 1本
GIRAFULL    枠=出る  現在受付中の抽選はありません   鍵 1本
```

### 晴れる屋2 の「受付中 2件」について（正確に）

**この2件は、私が足した鍵で当たったものではありません。**

```
晴れる屋2 通販  ポケモンカードゲーム MEGA 30th CELEBRATION プレミアムデッキセット  締切 09-07T18:00
晴れる屋2 通販  ポケモンカードゲーム MEGA 拡張パック「30th CELEBRATION」        締切 09-07T18:00
```

当たっているのは**元からあった鍵 `晴れる屋2 通販`** で、配布に出るようになったのは
関門の側の変化（`not_reviewed_ec` が外れた分）です。私が足した
`晴れる屋2各店（店舗）` の回は、まだ配布に出ていません。

### 残り5店が「受付中0」なのは、配布行がまだ無いからです

鍵は当たるようになりました（前便の測定どおり6件）。**配布に出るかは #1349 の関門と
AI審査次第**で、そちらは ci が確認する範囲です。

## 3. 状態

- #1351 は**マージ済み**（`5d2b44c6`）。
- カードの枠は**6店とも出ています**（54枚）。
- **追加の変更はしていません。** PR も出していません。
- 課金はありません。

## 4. 持ち越し（変わらず）

```
2026-09-16 以降   りらい(福島) の別名を足す
別の区切り        案3・案4（ロスターの別名23本・②の部分一致）— 設計書 §11-1-3 が入口
見張り            配布を作り直す回のあと、エディオン2行の lot_key が動いたかを1度だけ確認
保留             イオンスタイルオンライン（公式サイトが読めず、公式Xの有無を確かめられない）
M1g の残り        判断1・2 は公式X の Vercel 経路が安定してから
```
