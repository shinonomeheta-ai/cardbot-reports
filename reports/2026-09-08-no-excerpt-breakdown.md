# no_excerpt 946 の内訳 — 181 は通信0・課金0で消える

セッション名: store-name-b
日付: 2026-09-08（JST・未明）
区切り/依頼名: metrics の回答を受けての確認
対象: `origin/main` = `f318784f` ＋ PR #1446（`06b4b217`）

**画面 7社（受付中101行のうち M1g 本部12行・23:20 JST 時点から変化なし）／上流 24社。**

## 1. metrics の回答で確定したこと

`no_excerpt` 946 は **既に `rounds_of` 2,830 の母数**で出した数。**80 を引く必要はない。**
私が心配した二重の引き算は起きない。

- **#1443**（マージ済み `4f2bb831`）が**数える側**の母数を `rounds_of` へ
- **#1446**（私・`06b4b217`）が**作る側**の跡を落とす

同じ 80 件を、数えるときと作るときにそれぞれ除くので重複しない。実測で確認した。

```
両方入った状態: dropped_out_of_scope_rounds 80 / doc["rounds"] 2,830 / rounds_of 2,830
```

`#1443` と `#1446` は同じファイル（`build_evidence_cache.py`）の**別の場所**
（`抜粋の関門` と `_応募回ごとの抜粋`）で、**競合しなかった**。main を取り込んで push 済み。

## 2. 【新しい発見】946 のうち 181 は「材料が無い」ではない

metrics の内訳を確かめるつもりで測ったところ、**保存済みキャッシュと、通信0で
作り直した結果で数が大きく違った。**

| | 保存済みキャッシュ | 通信0で作り直した後 |
|---|---|---|
| `doc["rounds"]` の本数 | **2,661** | 2,830 |
| `V4/excerpt` ok | 1,884 | **2,064** |
| `V4/sendable` ok | 1,538 | **1,721** |
| `no_excerpt` | **946** | **766** |
| `fetch_calls` | — | **0** |

内訳を追うとこうなる。

```
母数（rounds_of）                                2,830
  保存済みキャッシュに行そのものが無い回            249
  excerpt ok が増えた                             +181
    その 181 は**全部**「保存済みに行が無かった回」
    「行はあったが ok でなかった回」からの回復は 0
  作り直しても ok にならない回                     766
```

**つまり `no_excerpt` 946 は「未生成 181 ＋ 本当に材料側 766」に分かれる。**
181 は `build_evidence_cache` を1回回すだけで `excerpt_status=ok` になり、
**通信0回・課金0**。

### 明日の a/b/c の切り分けへの影響

「なぜ `ai_not_run` が1,000件あるのか」の a（材料が無い）に、**未生成が 181 混ざっている**。
a でも b（送っていない）でもなく、**その手前の「まだ作っていない」**。
946 という数字も母数 2,830 も正しく、**分類だけがずれている。**

### 原因の見立て（未確認・裏を取っていない）

`candidate-ai-review` が `build_evidence_cache.py` を上限つき（`CACHE_ARGS`）で
回しているため、増えた回にキャッシュが追いついていないのだと思う。
**workflow の引数は見ていない。** metrics か ci の方が近い。

## 3. PR #1446 の状態

main を取り込み、`06b4b217` で push 済み。差分は2ファイル・64行の追加のみ。

実データ試験は **FAIL 2**（`test_実データで別支店が同じ抜粋を共有しない` /
`test_実データの抜粋が店ごとに分かれている`）。**ERROR ではなく FAIL** で、
守りが動いて違反5件を指している状態。

### ci の「条件B＝守りが緑になること」への申し送り

筋は通ると思う。ただし**5件のうち1件は人の確認が要る**。

| 違反 | 誰が直せるか |
|---|---|
| `store:エディオン` と `store:トレカキャピタル` が同じ抜粋を共有 | **人の確認**（`確認済みマスター根拠` への登録）。#1412 の主体分離の帰結 |
| 抜粋の混入4件（ミント仙台店・お宝創庫・TCバトロコ小山駅前・ポケモンカードストア） | 私が明日見る |

**緑を条件にするなら、その1件の決着が案E の前提に入る。**

## 根拠データ

- [2026-09-08-no-excerpt-breakdown.json](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/data/2026-09-08-no-excerpt-breakdown.json)
  — `origin/main` f318784f ＋ #1446（06b4b217）。保存済みと作り直しの比較、
  181 の内訳、#1443 と #1446 が重ならないことの実測、違反5件の担当。

## 未着手

- S2b の rows がまだ main へ着地していない（`shadow_candidates.json` の内容衝突。
  押し直しの提案は前の報告に書いた）
- 抜粋の混入4件
- 4社の分類やり直し（429 の記録を見て）
- 17社の一覧特定
- `linked_page_cache` の化けた保存本文17本の無効化
