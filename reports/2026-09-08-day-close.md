# 一区切り: #1456・#1453 マージ／closed を API へ（#1459）／イオン九州の実測（#1461）／#1440・#1432 の土台を入れ替え

**画面 6社（実時刻・10行）／上流 34社**

セッション名: api-customer
日付: 2026-09-08（JST）
区切り/依頼名: 今日の5件
対象: `origin/main` = `97822a12`（#1453・#1456 マージ後）

## 受けた指示（原文・要点）

> ① #1456 と #1453 をマージする。承認済み。
> ② apply_state: "closed" を API の active も見るようにする。いま数が動かないうちに揃えること。
> ③ イオン九州のロゴを拾う。ゴールデンチェーンの本部で画面に出る。拾ったら x-intake に知らせること。
> ④ #1450 が入ったら HMV を本番の経路で測り直す。
> ⑤ #1440 と #1432 のマージ状況を報告すること。

## 結論

| | 状態 |
| --- | --- |
| ① #1456・#1453 | **マージ済み**（`cd4519f2`・`e885b718`） |
| ② closed を API へ | **#1459**（数は動かない・確認済み） |
| ③ イオン九州 | **#1461**。取りに行ったが取れない。**前提の訂正あり** |
| ④ HMV の再測定 | **#1450 待ち**（まだ open） |
| ⑤ #1440・#1432 | **どちらも open。土台が古かったので main を入れ替えて測り直し中** |

## ① マージ2本

新しい基準 run 34173418711（main `acee7b40`・赤6件）と突き合わせ、**両方とも PR 固有の赤なし**を確認してマージしました。

```
基準(新) の赤6件
  ERROR: test_実データで別支店が同じ抜粋を共有しない
  ERROR: test_実データの抜粋が店ごとに分かれている
  FAIL: test_サイト既知の店にロゴが揃っている     ← ③ で直す
  FAIL: test_実データ
  FAIL: test_成果物の行と控えの鍵がつながっている
  FAIL: test_本物の控えが行の数だけある

#1456 固有: なし     #1453 固有: なし
```

#1456 の「build が fail」は `npx next build` ではなく、そのあとの `node --test app` の既存の赤でした。**ビルドは通っています。**

## ② closed を API の active も見る（#1459）

`isActive` に1行足しました。#1456 で判定を1本へ寄せたので、店の口とゴールデンチェーンにも同時に効きます。

```
全316行
closed を見ない  91行
closed を見る    91行   ← 差 0
受付中の apply_state: undefined 85 / open 5 / unknown 1
```

**`active=true` の側に closed の行は1本もありません。** ご指示どおり、動かないうちに揃えました。試験 3,657件 pass・赤1件は main 由来。

## ③ イオン九州（#1461）— 前提を1つ訂正します

**実際に取りに行きました。取れません。**

```
https://iaeon.aeon-kyushu.info/            200・3,538バイト
   題名「イオン九州用アプリ連携サイト」
   HTML に icon の宣言なし・og:image もなし
https://iaeon.aeon-kyushu.info/favicon.ico  404
```

`build_store_logos.py` を実際に走らせても取れませんでした。

### 会社としてのロゴは既にあります

```
イオン九州 (iAEON)          → /logo/logo_55f2b85037.ico
イオン九州（iAEONアプリ）    → /logo/logo_f913b18b28.png
イオン九州                  → 無し   ← これだけ
```

同社の本体 `www.aeon-kyushu.info` には favicon も apple-touch-icon もあり、別の鍵はそこから取れています。欠けているのは、探索が足した**素の「イオン九州」**という鍵だけです。

### 訂正: イオン九州はゴールデンチェーンではありません

ご指示に「ゴールデンチェーンの本部で画面に出る」とありましたが、`golden_chains.json` に イオン九州 はありません。「イオン」を含む本部は **イオンスタイルオンライン** と **イオン北海道eショップ** の2社です。

配布データにも イオン九州 の行は1本もありません（「イオン」を含む行は ＣＢ田中書店イオンモール都城駅前店 などの支店名だけ）。**いま画面に出ていません。**

### なぜサイトを差し替えなかったか

この鍵のサイトを本体へ差し替えれば取れます。**やりませんでした。** `store_sites.json` は `url_quality` と `verify_platform_owner` が**URLの持ち主の判定**に使います。ロゴ1枚のために持ち主の判定を動かすのは割に合いません。

既存の3件と同じ扱いで、理由を書いて `NO_USABLE_ICON` へ入れました。差し替えたい場合は、サイトの付け替えではなく `store_logo_manual.json` へ入れる形をお勧めします。

**x-intake へは知らせられていません。** エージェントの一覧に `x-intake` という名前のセッションが見当たりませんでした。仲介をお願いします。伝える中身は上の3点（取れない・会社のロゴは別の鍵にある・ゴールデンチェーンではない）です。

## ④ HMV は #1450 待ち

`#1450` はまだ open です（head `f447a99d`・最終更新 09-07 14:42Z）。入り次第、本番と同じ呼び方で測り直します。**無効化前の値は測ってあります**（0/5・5件とも置換4,300前後）。

## ⑤ #1440・#1432 のマージ状況

**どちらも open でした。** 新しい基準と突き合わせると、両方に同じ1件だけ PR 固有の赤が出ます。

```
#1440  FAIL: test_公開データの応募URLが減らない
#1432  FAIL: test_公開データの応募URLが減らない
```

**これは PR のせいではありません。** 両方の run が **09-07 12:28 / 12:30 の古いもの**で、当時の main のデータを見ています。いまの main で同じ試験を回すと**緑**です。

```
main 97822a12 で test_公開データの応募URLが減らない  → OK
```

そこで**両ブランチに main を取り込んで押し直しました**。

| PR | 取り込み | 試験（手元） | 新しい run |
| --- | --- | --- | --- |
| #1440 | 競合なし | `test_url_candidate` + `test_discover_official_x_web` 65件 OK | 34174771130 実行中 |
| #1432 | **競合1件を解いた** | `test_url_candidate` + `test_vocab_master` 38件 OK | 34174825565 実行中 |

#1432 の競合は `vocab_history.jsonl` でした。**追記ログなので両方残し、時刻順に並べました**（20:00 の `passrate_v4_gate_reason` の init と、22:30 の `site_blocked` の add）。片方を捨てると語彙の履歴が欠けます。

完走したら判定して報告します。**429 の再分類の前提として、#1440 はこれで通る見込みです。**

## 残り

- #1450 待ち → HMV の再測定。
- #1459・#1461 の判断。
- #1440・#1432 の新しい run の判定。
- #1447 は保留のまま。link の型4社は着手しない。

## 実行したこと・していないこと

- 実行: #1456・#1453 のマージ、#1459（closed）、#1461（イオン九州）、#1440・#1432 への main 取り込みと競合の解決と押し直し、基準との突き合わせ。
- していない: #1440・#1432・#1447・#1459・#1461 のマージ、HMV の再測定（#1450 待ち）、x-intake への連絡（宛先が見つからず）。**費用 0 円**。
