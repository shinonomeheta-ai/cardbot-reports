# #1471 も CI 緑（同じ土台・同じデータで新規の赤 0）／残る問いは1つ

セッション名: store-name-b
日付: 2026-09-08（JST）
区切り/依頼名: #1471 の CI
対象: PR `#1471` head `bcfb1a3f`（土台 `ba9e5a47`）

## 1. `#1471` の CI — **新規の赤 0**

```
#1471（run 34190390511・7,898件）  赤 5件
  test_candidate_ai_runner「実データの抜粋が店ごとに分かれている」
  test_candidate_ai_runner「実データで別支店が同じ抜粋を共有しない」
  test_lottery_overrides ×2
  test_url_candidate.実データ ×1
```

**土台を揃えて突き合わせた。** `main` の基準 run は土台が違う（`1c73f1c6`）ので、
CI を1本増やす代わりに**同じ worktree で `candidate_ai_contract.py` だけを
土台（`ba9e5a47`）へ戻して**同じ試験を回した——データも同じなので、差は
この PR の1ファイルだけになる。

```
土台のまま（candidate_ai_contract.py を HEAD~1 へ戻す）  赤 5件
#1471                                                    赤 5件
→ **名前の集合まで完全一致。新規の赤 0。**
```

`#1471` が触るのは `candidate_ai_contract.py` 1ファイルなので、この測り方で
CI と同じことが言える。

## 2. 判断待ちは2段のまま

| | CI | 新しく増える聞き直し | うち抜粋が1字も同じ |
|---|---|---:|---:|
| **`#1471`**（先に入れる） | 緑・新規の赤0 | **0** | 0 |
| **`#1468`** だけ | 緑・新規の赤0 | 82 | 48 |
| **`#1468` ＋ `#1471`** | — | **34** | **0** |

**`#1471` は単体では聞き直しを1回も増やさない**（指紋を1バイトも変えないため）。

## 3. 費用の並び（大きい順）

```
prompt_version を1つ上げる     345〜391回（答えを持つ回が全部）
#1468 だけ                       82回（うち抜粋が同じ 48）
#1468 ＋ #1471                   34回（うち抜粋が同じ  0）
#1471 だけ                        0回
```

**いちばん高い買い物は `prompt_version` を上げる判断**で、`#1468` の増分より
一桁大きい。`#1471` はここには効かない——**問いが変われば聞き直すのが正しい**ので、
効かせてはいけない。

## 4. 残る問い（metrics と共同・明日の宿題）

「`main` のまま既に聞き直しになる 266回」の 98%（261回）は**古い
`prompt_version` の積み残し**で、一度聞き直せば消える。ここは metrics と
独立に測って一致した（あちら 263回）。

**ただし1つだけ確かめきれていない。** いまの版（12）に限った聞き直しが、

```
私の測定（14:0x 頃）   5 / 84   （6%）
metrics（14:28）      35 / 128  （27%）
```

と**数時間で動いている**。母数が 84 → 128 に増えているので、そのあいだに
v12 の答えが44回増え、うち30回ぶん材料が動いたことになる。

**今日は特殊**（候補AI確認の手起動が3回・`ai-read` が1回、キャッシュを
書き換えている）ので、平常日に同じことが起きるかは分からない。

**言い切れるのは「263回は一度きりの積み残し」までで、「毎日は積み上がらない」
とはまだ言えない。** metrics が明日の同時刻に測り直す。

### metrics の測定にあった食い違いは解消した

あちらは指紋を**その答え自身が持っている `prompt_version`** で計算していた。
本番（`check_candidates_ai.対象`）は**いまの版**で計算する。直したら v11 が
4/13 → 13/13 になり、私の測定と一致した。

## 根拠データ

- [2026-09-08-resend-baseline-by-prompt-version.json](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/data/2026-09-08-resend-baseline-by-prompt-version.json)
  — `prompt_version` 別・答えた日別の聞き直し率、`attempts` の分布。

## 判断がほしいこと（変わらず2段）

1. **`#1471` を先に入れてよいか**（単体では聞き直しは1回も増えない）
2. そのうえで **`#1468` をマージしてよいか**（増分34回・全部が抜粋の変わった回）
