# 引き継ぎ書（更新4）— 本業の詰まりを特定した。**送れる回 1,334件 / 1日20件**（2026-09-09）

宛先: 次のセッション
前任: Claude（セッション ci・2026-09-08 20:40 〜）

**前版**: [更新3](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/2026-09-09-handoff-3.md)
（決まり9により追記せず新ファイル）

---

## 0. いちばん大事な発見

**AIへ送れる回が 1,334件あるのに、1日20件しか聞いていない。**

```
台帳の応募回        3,094件
いま送れる回        1,334件   ← 材料が揃っていて、聞けば答えが出る
1日に聞く数            20件
全部聞き終わるのに   約67日
```

抽選の締切は数日〜2週間先。**順番が回ってくる前に、ほぼ全部が締切切れになる。**

memory の「送れる回 662」が、いま **1,334件**まで増えている（**倍**）。

**これが「持っているのに画面に出ない」の主因である可能性が高い。**
ただし**まだ断定していない**（§3 に確かめる手順）。

---

## 1. 本業の段（2026-09-09 11:10 JST 時点）

```
① ゴールデン台帳       55社
② 候補台帳に居る       50社   ← 落ちるのは5社だけ（**発見は詰まっていない**）
③ 配布行に出る         27社・66行   ← ★ここで23社落ちる
④ 受付中で出る          5社・6行
目標                   20社
```

**①→② の落ちは5社だけ**（KIDDY LAND / ToysRus / GAME ARC宝島 /
イオン北海道eショップ / Amazon）。元の引き継ぎ書の「発見は詰まっていない。
詰まっているのは、持っているものを画面に出すところ」が数字で裏付けられた。

### ★ 最優先の8社（候補に「受付中の回」があるのに配布に出ていない）

```
YAMADA 3 / BIC CAMERA 2 / イエローサブマリン 1 / ドン・キホーテ 1
セブンネット 1 / 竜星のPAO 1 / HMV&BOOKS online 1 / お宝創庫 1
```

**この8社が出れば 27社 → 35社**で、目標20社を大きく超える。

---

## 2. ★8社の落ち方（`AI昇格できるか()` を実際に通した結果）

```
11件の受付中の回 / うち商品名が空 9件（82%）

  ai_not_run        6件
     ├ 送る並びに入っている   2件  YAMADA 53番目 / セブンネット 736番目 ← **順番待ち**
     ├ 圏外                2件  YAMADA / ドン・キホーテ           ← 理由未確認
     └ 材料が無い(no_excerpt) 2件  HMV&BOOKS online / 竜星のPAO
  missing_identity  4件   ← **商品名が空で素性が作れない**（素性は 店｜商品｜締切）
  型の問題          1件   ← `AI昇格できるか` が `end < 今日` で str と date を比べて落ちる
```

**商品名が空が 11件中9件（82%）。** `missing_identity` の4件は直接これが原因。

---

## 3. 次に確かめること（**推測で直さない**）

1. **「圏外2件」がなぜ `select_targets` から外れるか**
2. **1日20件の上限がどこで決まっているか。** 上げられるなら、**8社どころではない
   規模で効く**（送れる回 1,334件）。ただし **1件あたりの課金**が乗るので、
   上げる判断は費用の見積もりとセット。**本人の承認が要る**
3. **商品名が空の9件は、根拠本文に商品名が書かれているのか。**
   書かれているのに取れていないなら、抽出を直せば一気に出る
4. **`ai_not_run` を「順番待ち」と「材料不足」で分けて数える。**
   memory に「`ai_not_run` の多くは順番待ち」とあるが、**★8社では
   2件が順番待ち・2件が材料不足・2件が圏外**で、割れている

### 数えるときの決まり

* **時点・母数・定義**の3点を添える
* **他人の関数を呼ぶ前に引数の形を確かめる。**
  今日また踏んだ——`AL.rounds_of()` と `SD.select_targets()` は
  **台帳全体（`{"candidates": …}`）**を受け取るのに `sc["candidates"]` を渡し、
  **「送れる回 0件」という誤った数を出した**

---

## 4. CI の型分け（前フェーズ・目的は達成済み）

```
赤 8 → 5 → 3 → 2の見込み ／ 飛ばし 19 → 62 ／ 移行中 6件 ／ データ契約 86件緑
A 本当のバグ 0件 ／ B 実データ 6件 ／ C 外の状態 3件（**全部片付いた**）
```

**PR の赤がコードのせいだけになる、という当初の目的は達成済み。**
残りの移行中6件は掃除で、**画面には効かない**。

### ★ `test_lottery_overrides.管理用の控え` はデータの実害（直していない）

```
同じ round_id なのに、配布行と控えで event_id が違う行が5件
  行   evt_e1964be6…#rnd_a27e543a232acc97
  控え evt_a32d5c31…#rnd_a27e543a232acc97
配布行 405 / 刻んだ 404 / 控え 404 / 欠け 5
```

TierOne 渋谷店の2件は**互いに eid が入れ替わっている**。管理画面はこの鍵で控えを
引くので、**この5行は「ID準備中」になり更新が409**。直すのは `event_id` 台帳の側。
**推測で触ると #1469 と同じ形**になるので記録だけ残した。

---

## 5. CI ログから試験名を数えないこと

今日**3回**、別々の理由で間違えた（化けた行の結合／別の試験が印字した記録済み
ログの中の `FAIL:`／存在しない試験名）。**信用できるのは `failures + errors` の
数だけ。** 名前は手元で走らせて確かめる。

---

## 6. 第2段の残り（DB）

**巡回はいまも JSON に書いている。DB は写しのまま＝4か所目。**

| | |
| --- | --- |
| A | 突き合わせの判定を**取得時刻**で見る（いまは前方一致だけで粗い） |
| B | **3モジュールの差し替え**。移行し直す → 突き合わせ0 → **その足で切替**（間に巡回を挟まない） |
| C | `x_post` のデータ移行 |

2026-09-08 16:24 の実測で「切り替えると消える・減る」が **311件**（移行の2時間後）。

---

## 7. 触ると危ないところ

* **`lottery_io.save()`** — `round_id` は `守る()` で発番され `_古い応募回を外す()`
  で外れる。**書き出す直前が唯一の確定点。推測で行を消さない。計測器を先に**
* **データ契約** — 落ちたら push を止める。**赤いものを入れると巡回が止まる**
* **`test_data_contract.load_tests`** — 配線を忘れると**緑のまま何も見ていない**
* **印スクリプト** — 「ファイル全体に印があるか」で判定すると、理由文が同じ2件目が
  黙って飛ばされる。クラス宣言の**直前の行**で判定し、付けたあと実際に読む
* **`event_id` 台帳** — §4 の★の直しはここ
* **AI へ送る件数** — **1件あたり課金が乗る。上限を上げる判断は本人の承認が要る**

---

## 8. 環境

```
Neon   Free / us-east-1 / PostgreSQL 18.6 ／ 0.5GB ／ 100 CU-hours/月 ／ ブランチ10
       5分でゼロまで落ちる（無効化できない）／**超過は課金ではなく停止**
DATABASE_URL   GitHub Secrets ＋ Vercel（本人が 09-08 に登録）
```

---

## 9. 本人にしかできないこと

管理画面 https://torecanavi.app/admin の**手動補正**タブ（KV `site:lotfix` 1本・
Python に書く道具は無い）。

| 店 | いま | 直す | 締切 |
| --- | --- | --- | --- |
| **ブックオフプラス新宿駅西口** | `BRIGHTNESS` | `BRIGHTNESS OF HOPE` | **2026-09-09（当日）** |
| MINT GAMES池袋店 | `30th` | `30th CELEBRATION` | 2026-09-13 |

---

## 10. 前任が今日踏んだ誤り

| 誤り | どう分かったか |
| --- | --- |
| 直す場所を推測で2回外した | 計測器で巡回1回で決着 |
| 古い基準で CI を4回比べた | 基準は20分で腐る |
| 「`round_id` 非空を関門に」 | 空は正当 |
| 「本文の直読み4本」 | 実際は2本 |
| 「`url` を主キーに」 | 92組が正規化すると同じページ |
| 「本当のバグ3件」 | 全部違った |
| CI の名前の数え方を3回間違えた | 化け／出力の中の FAIL／存在しない試験名 |
| 印スクリプトの判定が甘く1件飛ばした | 付けたあとに走らせて気づいた |
| 空の台帳の鍵の名前を間違えた | 実ファイルを見る前に書いた |
| `GI.チェーン()` の呼び方を間違えて「0社」 | 定義を見て直した |
| **`rounds_of` / `select_targets` に `sc["candidates"]` を渡して「送れる回0件」** | **引数の形を確かめる前に数を出した** |
| heredoc の罠2回 | Write でファイルに書いてから実行 |
| 根拠なく巡回の切れ目を2回待った | 触るファイルが重ならなければまたいでよい |

**共通する型: 実物を確かめる前に、名前や見た目で判断して数を出した。**
**効いた判断はどれも「先に測る道具を作った」もの。**
