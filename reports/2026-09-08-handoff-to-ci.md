# 引き継ぎを1本にまとめる（ci へ集約）

2026-09-08 ／ ci

## 受けた指示（原文・抜粋）

> セッションを ci 1本にまとめます。他のセッションから引き継ぎが届きます。
> 受け取ったら1本の表にして「引き継ぎ完了」を本人へ報告してください。

---

## 1. 引き継ぎの表

| 元 | 引き継ぐもの | いまの状態 | 次の一歩 |
| --- | --- | --- | --- |
| **api-customer** | **同じ id の7組**（本番で応募できない） | いま**5組**（下の §2） | 明日① A で畳む |
| | 403 の切り分け（UA・リダイレクト・Accept） | 17回・2店 | 1つずつ当てる。急がない |
| | 実時刻判定 #1456 | マージ済み | — |
| | #1447 | 保留（移行の後） | — |
| **store-name-b** | 案B の測り方（同じ worktree で `candidate_evidence_excerpt.py` だけ入れ替え・通信0） | 確立 | そのまま使う |
| | 条件B の測定（v16→v17 で 混入1→0・共有1→0） | 済 | #1468 後に再測 |
| | **#1471 → #1468 の順**（増分 82→34） | **本人の承認待ち** | 本人が直接出す |
| **store-name-a** | V5 の送信手順（1便ずつ main に乗るのを見る） | 残り376回・35便 | 継続 |
| | 口A（`extract_set`）#1482 | マージ済み | — |
| | `sorted(key=len)` が同順を決めていない不具合 | product が発見 | 弾名は素性なので効く |
| **roundup** | 口B #1483 | マージ済み | — |
| | **名簿の `BRIGHTNESS` が残る** | **FB1x（9/12 発売）で口B が再び開く** | 日付つきの既知の穴 |
| | 合図レーンの3点 | 判断待ち | 急がない |
| **product** | M2 の名寄せ（⑱⑳・#1480・#1481） | 進行中 | ⑤ |
| | 辞書とAIの使い分け／「呼ぶ側が正準化する」 | 確定 | 踏襲 |
| | (3) の9行（表記ゆれ4・切れ2・候補なし3） | 表を作成済み | ⑤ |
| **x-intake** | `announce_type` の台帳 #1474 | マージ済み | — |
| | `unknown` は口の登録漏れも含む | 実例あり | — |
| | 2枠続けて遅い（0.16→0.32s） | 16:00/20:00 で再測 | — |
| | 竜のしっぽ・竜星のPAO の登録 | **足さない**（本人判断） | — |
| **metrics** | 命中率の測り方（正解表はコードに落とさない） | 確立 | — |
| | 45社の内訳・定義A/B/G | — | — |
| | PR #1475（代理で起票） | オープン | metrics 不在なら ci が面倒を見る |

**未着（届いていない）**: 各セッションからの直接の引き継ぎ。上の表はレビュー担当の
集約文から起こしたもの。**届いたら差分だけ足す。**

---

## 2. 明日① — 同じ id の重複は、`round_id` の重複と**同じ集合**だった

時点 2026-09-08 18:5x JST ／ 配信 421行

    id が重なる組       5
    round_id が重なる組  5
    → **同じ5組**（api-customer の7組は当時の断面）

| id | 店 | 中身 |
| --- | --- | --- |
| `b7b1b02d0f44dc12` | トレカキャピタル | **1文字も違わない**（round_id も同じ） |
| `cbfcc7ee25862130` | ドラゴンスター各店 | 同上 |
| `54df7a1174973f90` | トレカプラザ55通販店 | 同上 |
| `081ec9d77d90c68b` | トレカキャピタル | 同上 |
| `f1bc4bfc96c5096b` | トレカキャピタル広島本店 | **`round_id` が違う**（片方 `None`）＋ `source_url` が違う |

**5組とも「17:45 の行」と「18:44 の行」**。今日ずっと追っている「新しい巡回が既存行を
更新せず、もう1行足す」形そのもの。

**api-customer が見た「apply_method が store と web で矛盾」は、いまの断面には無い。**
5組とも `method=web` で揃っている。**畳む前に、もう一度その時点の断面で確かめる**
（当時の行が期限切れで落ちた可能性がある）。

### 直しの見通し

* **作られるのを止める**のは #1484（刻印 39%→95% ＋ 寄せた身元でも引く）。**まだマージ前**
* **既にある5組**は #1484 では消えない。**A（データ側で畳む）が要る**
* 畳む規則は「**空でない値を残す／応募URLを持つ側を勝たせる**」（本人判断）

---

## 3. 明日の優先順位（受領）

    ① 同じ id の5組（A で畳む）      ← 本番で利用者が応募できない
    ② first_source を埋める（421/424 が空）  ← ①の調査にも効く
    ③ 締切が過ぎた回を候補にしない関門（159回・90店）
    ④ 案E の解凍（#1471 → #1468）
    ⑤ 名寄せの続き（表記ゆれ4行）

---

## 4. 私の側の未処理

| PR | 中身 | 状態 |
| --- | --- | --- |
| #1484 | 刻印 39%→95% ＋ 寄せた身元でも引く | **main の基準を取り直して判定中** |
| #1479 | `source_gone`（7本） | 同上。#1468 の測り直しの土台を動かすので**待たせる** |
| #1454 | 今日の運用（docs） | **マージする** |

**`web build check` が main への push で走らない**件は、①と同じ場所（重複）を
見張れる唯一の検査なので、走る形にできるかを見る。
