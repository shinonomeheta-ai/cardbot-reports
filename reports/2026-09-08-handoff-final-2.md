# 引き継ぎ（最終版・その2）— ①〜④ を実行した後の実測

2026-09-08 ／ ci

前の版（[2026-09-08-handoff-final.md](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/2026-09-08-handoff-final.md)・19:32 JST）は
**#1454 と #1484 がマージされる前に書いた**。その後のマージと、マージ後に取り直した
基準を入れて差し替える。**前の版は消していない**（決まり: 追記しない・新しい名前で出す）。

---

## 1. 今日の締めで実行したこと

### ① #1484 をマージした（刻印 39% → 94%）

    マージ      803b36b1（2026-09-08 10:37Z ＝ 19:37 JST）
    PR の枝     fix/stamp-round-links

**基準はその場で取り直した。**（決まり: main の基準はマージのたびに取り直す）

| | run | 対象 | 試験 | 赤 |
| --- | --- | --- | --- | --- |
| マージ前の main | 34215676725 | 93f22633 | 7,998 | **8** |
| PR（最終 push） | 34213597324 | 95da6770 | — | 8（同じ名前） |
| **マージ後の main** | **34217318431** | **803b36b1** | **8,017** | **7** |

**新しく赤くなった試験は 0。** マージ前の8件のうち1件
（`test_公開データの応募URLが減らない`）が緑になり、残り7件は同じ名前のまま。

    緑になった  test_公開データの応募URLが減らない          (test_url_candidate)

    残る7件（マージ前と同一）
      test_実データの抜粋が店ごとに分かれている        (test_candidate_ai_runner)
      test_実データで別支店が同じ抜粋を共有しない        (test_candidate_ai_runner)
      test_成果物の行と控えの鍵がつながっている         (test_lottery_overrides)
      test_本物の控えが行の数だけある                (test_lottery_overrides)
      test_実データでJS側と一致する                 (test_shadow_url_owner)
      test_共有基盤のokは登録済みの店だけ             (test_shared_platform_gate)
      test_実データで完全に一致する                  (test_url_fields)

7件とも**実データを見る試験**で、main が前から赤い分（#1484 由来ではない）。
**「CI が緑になり次第」を額面どおりには満たしていない**——main はこの実データ試験で
前から赤く、判定はマージ前後の**赤い試験の名前の集合**で行った（決まり: PRの赤は
main と差分を取る）。

**刻印はマージ後の main で数え直した**（前の報告の 401/424 は 18:12 の版の数）。

    時点     2026-09-08 19:50 JST ／ main 803b36b1
    母数     配布行 420（`web/public/data/lotteries.json`）
    刻印     395 ＝ **94.0%**（`round_id` を持つ行は 417）

数が 424→420 に動いたのは、**マージ後に巡回（nyuka-watch 10:43Z）が回って配布行が
入れ替わったから**で、刻印が減ったのではない。**数は時点と母数を添えて読むこと。**

**受付中2行の刻印が main にある**（本人の操作の前提・実物で確認）:

    ブックオフプラス新宿駅西口  rnd_a3d25394c8d520b7 → crnd_fbb010e48f73
    MINT GAMES池袋店         rnd_1e73cecab56bc33f → crnd_5cd79841623a

### ② 直した棚卸しを出した

誤った表は**残したまま先頭に「この表は使わないでください」を立てた**（消すと、既に
読んだ人が同じ表を持ったまま気づけない）。

* 誤り（使わない） — <https://raw.githubusercontent.com/shinonomeheta-ai/cardbot-reports/main/reports/2026-09-08-m1g-inventory.md>
* **直した表** — <https://raw.githubusercontent.com/shinonomeheta-ai/cardbot-reports/main/reports/2026-09-08-m1g-inventory-fix.md>

### ③ #1454 をマージした（今日の運用・14例 ＋ 新しい規則2つ）

    マージ      3ed2c967（2026-09-08 10:30Z ＝ 19:30 JST）
    追加の commit 20b1bd95（docs/target-architecture.md ＋12行）

足した2つ（§10-3 の末尾・#1469 の教訓のすぐ下）:

* **表を渡すときは、どの鍵で引いたかを添える**
* **他人の表で採点するときは、その表がどの鍵で引かれているかを先に確かめる**

### ④ この表（引き継ぎの最終化）

---

## 2. 明日の順番

    ① 本人の管理画面から補正2件 —— **#1484 が入ったので、もう行は落ちない**
       （締切 09-09 が先。3節に文面）
    ② 重複5組を A で畳む（空でない値を残す・応募URLを持つ側を勝たせる）
       ※ 作られるのを止める側は #1484 で入った。**既にある5組は別に畳む必要がある**
    ③ 明日の相手（訂正後）: 本当に取れていない4社 ＋ KIDDY LAND の別名登録漏れ
       （6節）。**「候補0の8社」は存在しない**
    ④ #1471 → #1468（**本人の承認待ち**・store-name-b が回す。増分 82→34）
    ⑤ #1479（source_gone）— ④のあと。`fetch_status` を変えるので土台が動く
    ⑥ first_source を埋める（配布行のほとんどが空）
    ⑦ 締切が過ぎた回を候補にしない関門（#1470・159回・90店）
    ⑧ #1475（metrics の代理起票）— metrics が戻ってから中身を書く

---

## 3. 本人の操作待ち

**（1）管理画面から商品名の補正2件** —— **前提は解けた**（#1484 マージ済・刻印が main にある）。

    ブックオフプラス新宿駅西口  BRIGHTNESS → BRIGHTNESS OF HOPE（締切 09-09・受付中）
    MINT GAMES池袋店          30th → 30th CELEBRATION（締切 09-13・受付中）

**（2）弾の名簿 `lottery_stores.json` の `'BRIGHTNESS'` は触らない。**
名簿を直すと弾の全行で `lot_key` が動き、利用者の端末に保存された鍵が外れて戻せない。
配布行の訂正（1）は鍵が動かないので安全。

**（3）長く止まっているもの**（今日の作業とは独立）: Play $25 ／ Apple $99 の登録、
AdSense の slot ID、LINE の資格情報。

---

## 4. 今日わたし（ci）が踏んだ誤り —— **そのまま残す**

### 4-1. 危険を報告に書きながら、塞がずにマージした（#1469）

配布側で店名を寄せる変更を入れるとき、**「候補側は生の名前のままなので食い違う」と
自分で報告に書いた**。承認され、塞がないままマージした。

    15:01 JST  #1469 マージ
    15:28 JST  最初の巡回で、配布行が二重になった3組が発生（トレカキャピタル系）

**報告に書いてあることは、塞がれていることを意味しない。** 塞ぐか、塞ぐまで入れないか。

### 4-2. 表の鍵を取り違え、採点を狂わせた（M1g 棚卸し）

候補をまとめた鍵（`GI.チェーン()` ＝ `canonical_name`）と、表を引いた鍵
（`store_keys[0]`）が違い、**55社中22社が「0行」に化けた**
（EDION↔エディオン／YAMADA↔ヤマダデンキ／BIC CAMERA↔ビックカメラ ほか）。

その表でレビューが採点し、**存在しない「失敗8社」を翌日の最優先に決めかけた**。
**誤りが下流まで届いた形。** 直すと本当に0なのは4社で、metrics の命中率
（45/49・取りこぼし4社）と完全に一致した。

→ 規則にした（#1454・20b1bd95）。**表を渡すときは鍵を添える／他人の表で採点する前に
鍵を確かめる。**

### 4-3. 同じ型を1日に3回

    ai_not_run 290 → 19    別々の時点・別々の母数の数を並べた
    本文の置き場            `linked_page_cache` だけ見て、根拠キャッシュを見ていない
                          （「判定できない 1,205」が実は 138 だった）
    表の鍵                  上の 4-2

**3件とも「その値がどこに書かれ、誰が読むか」を先に数えていれば防げた。**

---

## 5. 未マージで残るもの（2026-09-08 19:56 JST 時点・実測）

**今日の作業ぶん**

| PR | 中身 | 状態 |
| --- | --- | --- |
| #1468 / #1471 | 案B（store-name-b）。#1471 が先 | **本人の承認待ち**（課金が増える） |
| #1470 | 締切も発表も過ぎた回を入口で作らない | 明日⑦ |
| #1475 | metrics の代理起票（`ON_DEMAND`） | metrics 不在。中身は未着手 |
| #1476 | 竜のしっぽ・竜星のPAO の membercard を「登録しない理由」に残す | 文書のみ |
| #1479 | `source_gone`（7本） | ④のあと（土台が動く） |

**前の版の表からの訂正**: #1482（口A）は**すでにマージ済**（09:10Z）。「V5 の一段落後」は古い。

**前から開いているもの**（今日は触っていない・次の人が順番を決める）

    #1447 [判断待ち・マージしない] りらい(福島) の別名 —— 移行が終わるまで開けない
    #1441 e-starbox の名乗る店を人へ下書き
    #1428 §0-1 原則1 の書き換え（本人判断 09-07）
    #1426 ヨドバシ／ビックの extra_store_keys
    #1423 API の商品名を商品マスタから返す
    #1418 /api/v1/chains をゴールデン台帳に
    #1413 報告への追記を禁止（文書）
    #1399 workflow の待ち行列は1本（文書）
    #1327 素性から応募URLを外す（案E）
    #1197 契約の段2・段3
    #1190 店本人の単独投稿を全文で拾う
    #1189 読める見込みの無い材料に課金しない

---

## 6. 明日の相手（訂正後）

**「公式経路の実績があるのに候補0の8社」は存在しない。** 実際は2つ。

1. **本当に取れていない4社** — トイザらス／ゲームアーク宝島／イオン北海道 eショップ／Amazon
2. **KIDDY LAND の別名登録漏れ** — 候補は支店名（カタカナ）で17回あるのに、
   `store_keys` が `['KIDDY LAND']` だけで当たらない

**「発見は詰まっていない」という結論は覆らなかった。**

---

## 7. 各セッションから預かっているもの

| 元 | 落としてはいけないもの |
| --- | --- |
| store-name-b | 案Bの測り方（同じ worktree で1ファイルだけ入れ替え・通信0）。#1471→#1468 の順（増分 82→34）。**課金の承認はセッション間で受け渡さない**を守って止めたのが正しい |
| store-name-a | V5 の送信手順（1便ずつ main に乗るのを見る）。口A #1482 は**マージ済**。`sorted(key=len)` が同順を決めていない不具合は修正済み |
| roundup | 口B #1483 済み。**名簿の `BRIGHTNESS` が残るので FB1x（9/12 発売）で口B が再び開く** |
| product | M2 の名寄せ。(3) の9行（表記ゆれ4・切れ2・候補なし3） |
| x-intake | `announce_type` #1474。`unknown` は口の登録漏れも含む |
| api-customer | 同じ id の重複（明日②）。403 の切り分け（17回・2店） |
| metrics | 命中率の測り方（**正しかった**。私の表が誤っていた）。#1475 |

---

## 8. 止まっているもの

    移行   `lottery-admin.json` と `lottery_overrides` の entries の鍵の貼り替えが
           同じ PR に入るまで。entries は追記型で promote は書かない。
           いま効いている補正は121行
    案E   #1471 → #1468 が入れば A✓ B✓ C✓ 4✓ が揃う
    #1447  移行が終わってから
