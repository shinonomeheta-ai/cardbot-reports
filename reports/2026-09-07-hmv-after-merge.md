# HMV の再測定（5件すべて抜粋まで通った）と、残り2本の赤の切り分け

**画面（実時刻）: 7社／上流: 34社**

セッション名: api-customer
日付: 2026-09-07（JST・深夜）
区切り/依頼名: #1437 マージ後の HMV の再測定
対象: `origin/main` = `c5c50024`（#1437・#1433・#1438 マージ後）

## 受けた指示（原文）

> 【明日】#1437 がマージされたので、HMV の再測定をすること。
> 本文7,987字に置換文字293で、題名は明確に抽選記事だった。HMV は M1g の本部。通れば1社増える。
> これが明日の最初の実測になる。

（判断: 1. 実時刻にする。画面7社12行が正本／2.「各店」は本部として数え続ける）

## 結論

- **HMV は5件すべて通りました。** 置換文字 0・抽選ページ判定 ○・抜粋 `ok`。文字コードの修正（#1437）が本番の main で効いています。
- **#1440 と #1432 に同じ赤が1つ出ています**（`test_公開データの応募URLが減らない`）。**手元のマージ後 main では緑**なので、データ依存の揺れと見ています。新しい基準 run（34124471076）で確認中です。**この2本はまだマージしていません。**

## 1. HMV の再測定（#1437 マージ後の main で実測）

| 記事 | 本文 | 置換文字 | 抽選ページか | 抜粋 |
| --- | --- | --- | --- | --- |
| 260714121 ストームエメラルダ | 4,146字 | **0** | ○ | **ok**・2,000字 |
| 260501122 アビスアイ | 4,136字 | **0** | ○ | **ok**・2,000字 |
| 240111103 ワイルドフォース／サイバージャッジ | 3,939字 | **0** | ○ | **ok**・2,000字 |
| 231003112 古代の咆哮／未来の一閃 | 3,769字 | **0** | ○ | **ok**・1,999字 |
| 250225101 熱風のアリーナ | 4,040字 | **0** | ○ | **ok**・2,000字 |

**5/5 で抜粋まで通りました。** 記事リンクは 一覧＋検索 で5件です。

ご指摘の「本文7,987字に置換文字293」は #1437 の**前**の値です。マージ後は**置換文字が 0** になりました。題名も文字化けせず読めています。

### ただし、いま画面に増えるわけではありません

取れた5件は**すべて締切済みの過去の回**です（ストームエメラルダ 7/31 発売、アビスアイ 5月、他は2023〜2025年）。**いま受付中の回は HMV の一覧・検索のどちらにもありません。**

したがって「通れば1社増える」は、**次に HMV が抽選を出したときに増える**という意味になります。今夜の画面7社は変わりません。仕組みが通ったことの確認です。

## 2. 残り2本の赤

`ci_diff.sh`（`--log` から拾う）で判定しました。

| PR | run | 赤 | 基準（21時台）に無い赤 |
| --- | --- | --- | --- |
| #1440 検索＋429 | 34122054135 完走 | 6 | `FAIL: test_公開データの応募URLが減らない` |
| #1432 site_blocked | 34122208252 完走 | 6 | 同じもの1件 |

**2本に同じ赤が出ています。** 片方は `.py` の検索まわり、もう片方は台帳の JSON で、**共通の原因が2つの PR にあるとは考えにくい**形です。

手元で確かめました。

```
マージ後の main（c5c50024）で
  python -m unittest test_url_candidate   → 33件 OK（skipped 1）
```

**手元では緑です。** この試験（`test_url_candidate.test_公開データの応募URLが減らない`）は配布データの応募URLの数を見る実データ試験で、**巡回でデータが変わると赤になる型**です。基準 run（34121180013・21時台）の後にデータが更新され、その後に走った2本だけが赤を踏んだ、という筋が通ります。

**新しい基準 run（34124471076）を起こしました。**そこにも同じ赤が出れば「揺れ」として除外し、出なければ原因を追います。**それまでこの2本はマージしません。**

揺れる試験は今夜2本目です。

```
test_レポートの材料がいまと合う      shadow_url_owner   時間経過で赤くなる
test_公開データの応募URLが減らない    url_candidate      配布データの更新で赤くなる（確認中）
```

## 3. 判断の受け取り

1. **実時刻にする**を了解しました。画面7社12行を正本として、以後の報告の冒頭は「画面 X社（実時刻）／上流 Y社」に統一します。**API の判定そのものを実時刻に変えるかは、別の作業として着手を待ちます**（`isActive` は暦日で判定していて、変えると `/api/v1/lotteries?active=true` の結果と画面の両方に効きます）。指示があれば実装します。
2. **「各店」は本部として数え続ける**を了解しました。除くのは「◯◯店」の個別支店名だけ、という線で揃えます。

## 4. 残り

- #1440・#1432 のマージ（新しい基準待ち）。
- 実時刻判定の実装（指示待ち）。
- 429 の対策は明日の実数を見てから。
- ヤマダの画像レーンは ci の分類待ち。

## 5. 実行したこと・していないこと

- 実行: HMV の再測定（マージ後の main・5件）、残り2本の赤の切り分け、手元での再現確認、新しい基準 run の起動。
- していない: #1440・#1432 のマージ、実時刻判定の実装、429 の対策。費用 0 円。
