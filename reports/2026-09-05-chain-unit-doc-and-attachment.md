# 設計書 §3-1-1・ノジマの付け替え・「M1g にいる分の処理」の中身

セッション名: store-name-a
日付: 2026-09-05（JST）
区切り/依頼名: 段3・B の答えの反映
対象: PR head（base `ec3be2ef` 以降の main）

## 受けた指示（原文）

> ## 1. PR #1292 をマージしてください
> ## 2. B の整理を設計書へ書いてください
> 場所は **§3-1（店マスタ）** に書いてください。（中略）
> あわせて「語尾（〜各店・〜店舗・〜全店）はコードに意味を与えていない。
> チェーン全体を表すのは M1g の store_keys だけ」も書いてください。
> ## 3. ノジマ → @enetjp: 外して付け替えてください
> ## 4. M1g に無い4チェーン: 登録しません（いまは）
> したがって、この4チェーンの台帳の行は**分けたまま**にしてください。
> ## 5. M1g にいる分の処理
> ただし「処理」の中身を確認させてください。台帳の行は分けたままでよい、
> というのが結論なら、何を変えるのですか。
> （公式Xの登録を正しい行へ付け替える、という理解でよいですか）

## 指示と過去の報告の食い違い

なし。

## 1. PR #1292 をマージしました

`ec3be2ef`。マージ前に `shadow_url_owner.json` の競合が出たので、main の版を採って
材料に合わせて作り直しました（`--raw`）。CI の赤は main と `diff` して完全一致（6件）。

## 2. 設計書 §3-1-1（PR #1295）

ご指示のとおり §3-1（店マスタ）の下に書きました。原則・M1g の実測値
（52チェーン中17件が複数キー）・分ける判断の例・語尾の件を含めています。

## 3. ノジマの付け替え（PR #1295）

```
ノジマ → @enetjp          外した（"moved"）
ノジマオンライン → @enetjp   付けた（"confirmed"）

ノジマ         confirmed_x = ''
ノジマオンライン  confirmed_x = 'enetjp'
```

**付け替え先の行が台帳にあることを確認してから**行いました。
なお `set_x_status` は「同じハンドルが別の店で確認済み」を拒否するので、
**先にもとの行を外してから**新しい行を confirmed にしています。

## 4. M1g に無い4チェーン

ご指示どおり**触っていません**。台帳の行は分けたままです。

## 5. 「処理」の中身（ご質問への答え）

**ご理解のとおりです。台帳の行は分けたままで、変えるのは
「公式Xの登録がどの行に付いているか」だけです。**

M1g で複数キーを束ねている17チェーンのうち、公式Xが付いているものを洗い、
**口が名乗る売り方と、付いている行の名前が食い違うもの**を数えました。

### 食い違い 4件

| チェーン | いま付いている行 | handle | Xのプロフィール表示名 | 同じチェーン内の付け替え先 |
| --- | --- | --- | --- | --- |
| フルコンプ | `フルコンプ各店` | @fullcomp_online | `フルコンプオンラインショップ` | **`フルコンプ 通販`** |
| 駿河屋 | `駿河屋` | @surugaya_toreka | `駿河屋通販トレカ情報【公式】` | **`駿河屋 通販` / `駿河屋通販`** |
| EDION | `エディオン` | @edion_com | `エディオン ネットショップ【公式】` | **無し**（通販の行が M1g に無い） |
| コジマ | `コジマ` | @kojima_kojibou | `コジマネット【公式】` | **無し**（同上） |

**付け替えられるのは2件、行き先が無いのが2件**です。

### 判断が要る点

- **`エディオン` は配布データに2行出ています。** 付け替え先が無いまま外すと、
  その2行が公式Xの根拠を失います。行き先を作る（`エディオン ネットショップ` を
  台帳と M1g へ足す）か、いまは触らないかの判断が要ります。
- `駿河屋` は配布0行・候補台帳2行。`フルコンプ各店` は配布0行・候補台帳2行。
  どちらも配布への影響はありません。

### 食い違っていないもの（＝何もしなくてよい）

```
イエローサブマリン   イエローサブマリン @ys_info（『イエローサブマリン宣伝』）  各店にXなし
イトーヨーカドー     イトーヨーカドー各店 @lets_go_yokado（『イトーヨーカドー』）
ドラゴンスター      ドラゴンスター @dorasuta_info ／ ドラゴンスター通販 @DS_ecommerce  ← 正しく分かれている
トレカプラザ55      トレカプラザ55通販店 @torepla_ec（『トレカプラザ55通販店』）    ← 一致
BIC CAMERA      ビックカメラ @biccamerae（『ビックカメラ【公式】』）
オレタン／あみあみ／ヤマシロヤ／キッズリパブリック
```

**`ドラゴンスター` は正しい形の見本**です。実店舗の口と通販の口が、
それぞれの行に付いています。

## 6. 判断していただきたいこと

1. **PR #1295 をマージするか**（設計書 §3-1-1 ＋ ノジマ）。
2. **`フルコンプ各店` @fullcomp_online → `フルコンプ 通販`** の付け替えを実施するか
   （配布0行・影響なし）。
3. **`駿河屋` @surugaya_toreka → `駿河屋 通販`** の付け替えを実施するか
   （配布0行・候補台帳2行が公式X判定を失う）。
4. **`エディオン` @edion_com** は行き先が無く、**配布2行が根拠を失う**ので、
   こちらでは決めません。行き先を作るか、保留か。
5. **`コジマ` @kojima_kojibou** も行き先無し（配布0行）。保留でよいと思います。

## 根拠データ

- [2026-09-05-chain-x-attachment.json](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/data/2026-09-05-chain-x-attachment.json)
  — M1g で複数キーを束ねる17チェーンについて、店キーごとの公式X・プロフィール表示名・
  配布データと候補台帳の行数、および食い違い4件と付け替え先候補

## 状態

- PR #1292 は**マージ済み**（`ec3be2ef`）。
- **PR #1295 を出しました。マージはしていません**（指示待ち）。
- 食い違い4件の付け替えは**していません**（中身の確認をとのご指示）。
- M1g に無い4チェーンは触っていません。
- 外部への通信は行っていません。課金もありません。
