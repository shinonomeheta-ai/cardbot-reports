# 止まっていたデータを流し、溜まっていた枝と一周の結果を main へ入れた（2026-09-17・続報）

前報: https://raw.githubusercontent.com/shinonomeheta-ai/cardbot-reports/main/reports/2026-09-17-hq-intake-sweep.md

## 指示文（本人 2026-09-17）

> 1、2を進めて、支店と本店一周回してください。今の段階で。
> はい、マージしてください。今回はCIも叩いていいです。CI Pythonテストお願いします。

## 結果

| | 前 | 後 |
| --- | --- | --- |
| `latest.json` の `generated_at` | 2026-09-16T13:02:37+09:00（約24時間停止） | **2026-09-17T13:02:28+09:00** |
| `nyuka-watch` | 6回連続 failure | 11:40 JST から緑 |
| `update-data` | 2回連続 failure | 13:00 JST success |
| PR が無い枝 | 24コミット滞留 | **#1606 マージ**（`c2ea6d0c5`） |

* 関門の直し **#1603** は別セッションが先に出していたので、こちらの重複 #1604 は閉じた（マージ commit `c178b7e68`）。
* データが止まっていたのは再起動ではなく関門。`nyuka-watch` / `update-data` の「データ契約（通知の前）」が
  `#1600`（python test を workflow_dispatch だけにする）と食い違って落ち、**落ちた回は push しない**ため。

## 支店と本店の一周（2026-09-17 07:57〜12:00 JST・4時間3分）

```
開いた口     1,466（本店 21 ／ 支店 1,445）
新しい投稿    1,331
告知          29
新規候補      29（うち新しい鍵 18）
経路         描画 1,293 ／ タイムライン 173（約9秒/口・429は描画へ落ちるだけで失敗ではない）
```

サイト側（同日 06:55〜07:25）は S2b 18社・リンク87・抽選26、S1 44社・リンク142・抽選14。
通信のみで、**AIは1回も呼んでいない**。

## CI（本人が今回だけ許可）

```
run 35178183427  feat/hq-intake-sweep  赤 31+4
run 35179200020  main（基準）           赤 15
→ PR 固有 16件。全部が同じ1変更に繋がっていた
```

**16件の正体**: 枝に入っていた `url_quality.classify` の方針変更（まとめ記事のURL: reject → apply・
2026-09-15）が、main 側の試験10モジュールへ届いていなかった。

**本人判断: この PR は収集だけにする。** `classify` を main の挙動へ戻した。方針変更は試験16件ごと別PRで出し直す。
残したのは `--lang=ja` の描画修正（LivePocket が英語の日付を返していた別件）と、まとめ垢の投稿を応募先にしない扱い。

```
run 35182367233  feat/hq-intake-sweep  → PR 固有の赤 0件（main だけの赤 14件はそのまま）
                                          main の赤を1件（本店3社は巡回対象でreadable）解消している
```

マージ後の main で `test_data_contract` 86件 → 緑（関門は通る）。

## main の取り込み方（この枝で使った決め方）

| 種類 | 決め方 |
| --- | --- |
| 上流が upsert で書く台帳 | 鍵で足し合わせ。同じ鍵は時刻の新しいほう（候補 3,660・X投稿 3,548・リンク控え 1,161・店公式X 1,527） |
| 人が決めた台帳 | 枝（`golden_chains` / `lottery_stores` / `product_master` / 本社台帳）。`store_keys` は `link_golden_chains.py` で作り直し |
| 朝夜のまとめのJS | main（#1601・#1602 が上位版。枝の 308行は main の 606行に含まれる） |
| 走った記録 | main（次の巡回で上書き） |

## 残っていること

1. **まとめ記事URLを応募先にする方針変更**は未着地。やるなら試験10モジュール16件を同じPRで揃える。
   現状は「まとめは応募先にしない」（従来どおり）。
2. main 自体の赤14件（実データ由来・xAI smoke・wayback・`test_周辺グッズ` など）は手つかず。
