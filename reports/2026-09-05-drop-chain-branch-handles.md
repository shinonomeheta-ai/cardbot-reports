# (d) チェーン名に支店・通販の口が結ばれた登録4件を外す

セッション名: store-name-a
日付: 2026-09-05（JST）
区切り/依頼名: 引きの別名対応・(d) の除去
対象: PR head（base `3b14daaa`）

## 受けた指示（原文）

> # 判断
>
> ## 1. PR #1287 をマージしてください
>
> ## 2. (d) の4件: 外してください（付け替えない）
> 外し方:
> - 表の鍵からその登録を外す
> - 台帳側は set_x_status で "moved"（別店舗）または相当の状態にする
>
> **付け替えはしないでください。** 正しい店名の行へ付けるには、
> その行が台帳にあるかの確認が要ります。それは段3の作業です。
> いまは「誤った根拠を外す」だけにします。
>
> 外した後、次を報告してください:
> - 4件を外したことで、公式X判定を失う配布行があるか
> - あるなら何行か（失うのが正しいので止めませんが、影響は記録します）
>
> ## ビックカメラ店舗の突き合わせ
> ci には私から共有します。外すのは store-name-a が担当してください
> （二重に外さないように）。

## 指示と過去の報告の食い違い

なし。

## 1. PR #1287 をマージしました

`3b14daaa`。

## 2. 4件を外しました（PR #1288）

| 表の鍵（チェーン全体） | handle | Xのプロフィール表示名 | 台帳側 |
| --- | --- | --- | --- |
| `チャンピオン` | @tcmeieki | `トレカチャンピオン名駅店` | 候補の行が無いので表だけ |
| `ビックカメラ店舗` | @bic_takatsuki | `ビックカメラ高槻阪急スクエア店【公式】` | `moved` |
| `フルコンプ店舗` | @fullcomp_yoko | `フルコンプ横浜店` | `moved` |
| `CARD CREW` | @cardcrew_tuhan | `CARD CREW【カードクルー】トレカ通販店｜年内店舗OPEN予定` | `moved` |

**付け替えはしていません**（ご指示どおり）。誤った根拠を外すだけです。

## 3. 影響の実測（ご依頼の点）

```
配布行で応募URLが出ている   86 → 86   （公式X判定を失う配布行は 0 行）
公式X一致した応募回        997 → 989  （-8）
```

**配布データの行は1行も失われませんでした。**

失った8回の内訳:

```
CARD CREW      5 回
ビックカメラ店舗   3 回
フルコンプ店舗    0 回
チャンピオン     0 回
```

いずれも候補台帳の段階のもので、**落ちるのが正しい**ものです
（別の店・別の売り方の口を根拠にしていた）。

`フルコンプ店舗` と `チャンピオン` は、外しても失うものがありませんでした。
**誤った登録が残っていただけで、実際には使われていなかった**ということです。

### 測り方

- 配布行: `url_fields.enforce()` で `lotteries.json` の全行を引き直し、
  `publishable()` が返すURLの有無を外す前後で比べた
- 応募回: 候補台帳の全応募回について、根拠のXのハンドルが
  その店の登録と一致するかを `date_source.tenant_key` で引いて数えた
  （#1280 の段2 以降の鍵）

## 4. 試験

```
python -m unittest test_store_registry test_store_registry_p0 test_official_announce
                   test_url_fields test_shadow_url_owner test_shared_platform_gate
                   test_discover_official_x_web
  →  Ran 214 tests  OK
```

`shadow_url_owner.json` は材料が変わったので `--raw` で作り直しています。

## 判断していただきたいこと

1. **PR #1288 をマージするか。**
2. `CARD CREW` の 5回・`ビックカメラ店舗` の 3回は、公式Xの根拠を失いました。
   これらの応募回に**ほかの根拠があるか**は測っていません。必要なら測ります
   （根拠が1本も無くなっていれば、§8-1 の関門で配信から落ちます）。

## 根拠データ

- [2026-09-05-chain-branch-misregistration.json](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/data/2026-09-05-chain-branch-misregistration.json)
  — 前の報告と同じもの（4件の表の鍵・handle・Xのプロフィール表示名）

## 状態

- PR #1287 は**マージ済み**（`3b14daaa`）。
- **PR #1288 を出しました。マージはしていません**（指示待ち）。
- (c) と (d) の残り2件（`トレカショップFIRST` `hmvトレカショップ各店`）は
  段3 のままです。
- 外部への通信は行っていません。課金もありません。
