# 9/7 夜の締め（ci 3件・store-name-b 2件・運用2つ・数え方の正本）を設計書 v14 へ（PR #1454）

日付: 2026-09-08（JST・作業は 09-07 深夜〜09-08 未明）
区切り/依頼名: 設計書 v14 への取り込み（今夜の締め・セッション docs）

## 受けた指示（原文）

> ci から3件、store-name-b から2件が渡る。設計書 v14 に入れること。
>
> ci から:
> 1. 数字の出どころを確かめなかった5件
>    （kept 110／配布の新規227行／商品名が空114件／刻印43件／store-name-a の +22）
>    共通する型は「dry-run の戻り値を実績として読む」「表の値を足す」「見る欄を取り違える」。
>    特に links_stamped は dry-run では「刻もうとした数」を返す。
> 2. 廃止したのに名前が残っていて全員が現役だと読んだ件
>    （no_human_decision / pending_review / human_stale）。
>    人の決着は廃止され AI と AI の突合が現行だが、理由名が残っていたため
>    2セッションとレビューが一晩、廃止された関門を「外すかどうか」の判断だと読んだ。
>    今夜いちばん高くついた誤り。
> 3. フラグの既定が統一されていない件
>    （link_golden_chains.py・shadow_url_owner.py は素で書く／ec_lottery_watch.py は --apply で走る／
>     s2b_mode=probe は S2b の段にしか効かない／--max-sources 0 は「上限なし」ではなく
>     「1本も取りに行かない」）。
>
> store-name-b から:
> 4. list-page-not-by-name（一覧を登録する前に、そこから取れた記事を判定へ通して
>    抽選と判定された数を見る。0 なら場所が違う／0件のときは「場所が違う」と「拾えていない」の
>    両方を疑う）。本人の記憶がリポジトリ外なので docs が持つこと。
> 5. test_candidate_ai_haiku_first が実行順依存。
>    いま全セッションが「赤の名前の集合で判定する」運用を使っており、
>    実行順で結果が変わる試験があるとその運用が壊れる。
>
> あわせて運用を2つ:
> - 数を出すときは 時点・母数・定義の3点
> - 設計変更や収集元の停止をした日は判定③が必ず鳴る。変更した側が一言残す。仕組みは足さない
>
> 数え方の正本も記録すること:
>   画面 ＝ 配布行・store_keys 完全一致・支店の個別名だけ除く（「各店」は含む）・締切は実時刻
>   上流 ＝ rounds_of（superseded 除く）・lifecycle active・締切が実時刻で未来・本部のみ
>   公式根拠 ＝ source_confidence.status が trusted の行
>   完全一致を前方一致に緩めるのは誤り（39件が当たるが全部支店）

## 指示と過去の報告の食い違い

1. **指示2 の「人の決着は廃止」は、設計書 §9-4 の前提と食い違う。** §9-4「人の決着は、自動判定の入力になっていなければならない」は、人が1件ずつ決着させる運用があることを前提に書かれている。§9-4 を消すのは設計の変更なので、**注記を足すに留めた**（節の扱いは判断が要る）。
2. それ以外の食い違いは無い。指示の名前は 8 つとも実物を確かめた（下の「裏取り」）。

## 報告

### PR

- **PR #1454** https://github.com/shinonomeheta-ai/cardbot/pull/1454
- 枝 `docs/v14-lessons-0907`・head **7afce934**・base main
- `docs/target-architecture.md` の 1 ファイルだけ。**追加 113 行・削除 0 行**（1,093 → 1,206 行）
- Vercel **SUCCESS**／GitHub Actions は**起動なし**（docs だけでパス条件に当たらない）＝ PR 固有の赤 0／MERGEABLE・CLEAN

### どこへ入れたか

| 指示 | 節 | 中身 |
|---|---|---|
| 4. list-page-not-by-name | **§2-4（新）** | 一覧を M4 へ登録する**前に**、そこから取れた記事を判定へ通して「抽選」と判定された数を見る。名前で「抽選の一覧らしい」と決めない。0 なら場所が違う。**ただし 0 のときは「場所が違う」と「拾えていない」の両方を疑う** |
| 1. 出どころ＋運用「時点・母数・定義」 | **§10-3（新）** | 数を出すときの3点。5件の実例と3つの型（dry-run の戻り値を実績と読む／母数の違う列を足す／欄の取り違え）。`links_stamped` は dry-run では「刻もうとした数」 |
| 数え方の正本 | **§10-4（新）** | 画面・上流・公式根拠の3つを表で定義。**完全一致を前方一致に緩めるのは誤り**（39件は全部支店） |
| 運用「判定③が鳴る日」 | **§10-2 に追記** | 判定③＝前日比の変動。鳴るのは正しい。**変更した側が報告に一言残す。黙らせる仕組みは足さない** |
| 2. 廃止した語 | **§11-2 の決まり4** ＋ **§9-4 に注記** | 「廃止した語は名前ごと消す。残すなら廃止・いつから・後継はどれを書く」。理由名3つが現役と読まれた経緯を残した |
| 3. フラグの既定 | **§13-1（新）** | 素で走らせたときの表と、`s2b_mode=probe`・`--max-sources 0` の落とし穴。決まり3つ（既定は書かない／0 を無制限に使わない／既定を変えるときは呼び手を全部数える） |
| 5. 試験の実行順依存 | **§13-2（新）** | 「赤の名前の集合で判定する」運用が壊れることを理由として明記 |

### 裏取り（v14 と同じ作法・実装の行まで確かめた）

| 指示の名前 | 実物 |
|---|---|
| `links_stamped` | `promote_candidate_decisions.py:1027`（dry-run の戻り値） |
| `no_human_decision` / `pending_review` / `human_stale` | 同 `理由` 表（125行〜）。**廃止の裏取り**: 人の決着が無い回は `PROMOTE_AI_CHECKED`（既定 1・813〜838行）の **AI レーン**が引き取る。3つは関門ではなく AI レーンへ入る道の名前 |
| `link_golden_chains.py` / `shadow_url_owner.py` | `--apply` が**無い**（素で書く） |
| `ec_lottery_watch.py` | `:115` に `--apply` |
| `s2b_mode` | official-site-watch の workflow（`inputs.s2b_mode \|\| 'apply'`） |
| `--max-sources` | `build_evidence_cache.py:5` |
| 画面 | `golden_chain_index.チェーン(店名)`（既定 `支店も=False`）。「各店」は `SUFFIXES`（40行）で外れて本部名へ寄る |
| 上流 | `candidate_ai_ledger.rounds_of`（245行・`superseded_by` は返さない）／`lifecycle_status == "active"` |
| 公式根拠 | `build_lotteries_nyuka.py:660`（`source_confidence.status == "trusted"`） |
| 試験 | `test_candidate_ai_haiku_first.py` は実在 |

### #1428 との関係

同じ `docs/target-architecture.md` を触る **#1428**（§0-1 原則1 の変更・未マージ）と、**手元で試験的に合流させて確認した。競合 0**。触っている行が離れている（#1428 は §0-1・§1-4・§2-2/2-3・§6-5・§8-1・§12、この PR は §2-4・§9-4・§10-2/3/4・§11-2・§13）。**マージの順番はどちらが先でもよい。**

### この PR に入れていないもの（別の判断）

1. **廃止した理由名3つをコードから消す**のは実装の変更。設計書に決まりを書くところまで
2. **§9-4 の扱い**（人が1件ずつ決着させる運用を前提にした節を、残すか書き換えるか）
3. `docs/target-architecture-v14.html`（図）は触っていない。新しい節は箱を増やさないため
4. `test_candidate_ai_haiku_first` の実行順依存そのものの修正（store-name-b の領分）

### マージについて

「マージの条件」1・2 は満たしている（PR 固有の赤 0・Vercel SUCCESS）。3（承認の記録）が要る。承認が出れば自分でマージし、merge commit は**新しいファイルの報告**で記録する。マージ後は `sync-docs.sh` で写しを更新する。

## 根拠データ

なし（設計書の文面の追加のみ。裏取りは上の表のファイル名と行番号がそのまま根拠）。
