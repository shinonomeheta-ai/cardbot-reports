# 判定③が鳴った理由（まとめ収集 3 本を止めたのは私です・直すものはありません）

日付: 2026-09-07（JST・23:2x 時点／鳴るのは 09-08 の判定）
区切り/依頼名: 変更した側の一言（metrics の運用 2 か条）
セッション名: roundup

## 一言（これが本体）

**2026-09-07 の夜に、まとめの値の収集 3 本を私が止めました。** そのため 09-08 の判定③（前日比の急変）で **L が 80% → 29%、P が 49% → 92%** と鳴ります。**機械としては正しい鳴り方で、直すものはありません。** 配布の母集団が 658 → 342 と半分になったための見かけの変化です。

| 止めたもの | 時点（JST） | PR | 止めた理由 |
|---|---|---|---|
| cardchusen | 09-07 20:37 | #1439 `e76c8df8` | 規約 第5条（robots.txt に「機械的手段による取得・蓄積は禁止」と明記） |
| meli-melo | 09-07 21:2x | #1442 `99c6cb7a` | **規約上の禁止は無い**（robots.txt 404・禁止表記なし）。まとめは値ではなく「抽選が始まった」の合図だけ、という方針に合わせた |
| 入荷Now | 09-07 22:0x | #1445 `b8e8ad15` | 8/30 から門（`COLLECT_AGGREGATORS` 未設定）で止まっていたが `sources.json` は true のままだった。設定と実態を一致させた |

**まとめは合図の面としては残っています**（`roundup_pages.json` の `signal_sites`・1 日 1 回・店名だけ・値は取らない）。

## 受けた指示（原文・要点）

> **1. 数を出すときは 時点・母数・定義の 3 点を必ず書く。**（今夜これが無くて同じ対象の数が 4 通り出た）
> **2. 設計変更や収集元の停止をした日は、判定③（前日比の急変）が必ず鳴る。変更した側が一言残す。仕組みは足さない。**

## 私が出した数に 3 点を付け直す

| 数 | 時点 | 母数・定義 |
|---|---|---|
| **画面 7 社 12 行** | 2026-09-07 21:42 JST | `web/public/data/lotteries.json` の行のうち、`current_apply_end`（無ければ `apply_end`）が**実時刻で今以降**のもの。店名が `golden_chains.json` の `store_keys` と**完全一致**（前方一致にしない・支店の個別名は数えない） |
| **上流 20 社 37 回** | 2026-09-07 21:42 JST | 候補台帳（`shadow_candidates.json`）の `active` な応募回で、締切が実時刻で今以降。店の一致は上と同じ規則。**metrics の「上流 32 社」とは定義が違います**（あちらは締切で絞らない集計） |
| **ai_not_run 1,023**（a1 241／a2 238／b 519／c 25） | 2026-09-07 22:3x JST | 母数は `candidate_ai_ledger.rounds_of(candidates)` の `lifecycle_status == active`（1,413）。a1＝キャッシュに行が無い（未生成）・a2＝行はあるが `round_state` が ok でない・b＝材料 ok で意図に無い・c＝submitted 20／reserved 5 |
| **c の 20 件＝未回収 batch 5 本** | 送信 09-07 16:00〜18:23 JST・確認 22:0x | `shadow_candidate_ai_intents.json` の `state=submitted` に居て、`shadow_candidate_ai.json` に答えが無い応募回。batch は `msgbatch_013kpw5k…`(7)・`011a5mNa…`(4)・`013gN8p1…`(3)・`01D8p3aB…`(5)・`01TgeQ9v…`(1) |
| **まとめの合図 0 件** | 09-07 21:18（基準の 2 回目） | 面 17（サイト 5＋まとめX 12）。合図＝**同じ日に 2 面以上が新しく載せた店**。面の初回は baseline で合図を出さない |

以後、私の報告の数字にはこの 3 点を必ず付けます。

## 明日（09-08）

05:40 の `roundup-daily` の差分から合図の件数を報告します。**差分の区間は 09-07 21:18 → 09-08 05:40**。ci の移行と x-intake の 4 回運用はこの区間に入りません。schedule は指示どおり外していません。

## 実行したこと・していないこと

- この報告は一言を残すためのもので、何も実行していません（読み取りのみ）
- 判定③に対する仕組みの追加はしていません（metrics の方針どおり）
