# 判断4（A2）の実装と、#1322・#1323 のマージ

セッション名: store-name-a
日付: 2026-09-05（JST）
区切り/依頼名: M1g の整理・第2段（判断4 の実装）
対象: `origin/main` = `8781628d`

## 受けた指示（原文）

> ## 1. PR #1322（判断3）: マージしてください
> ## 2. PR #1323（判断9）: #1322 の後にマージ。base の切り替えは任せます
> ## 3. 判断4: 選択肢A2
> 駿河屋だけ登録（@surugaya_kosin）。2・4・5 は「同じ口が兼ねている」を登録名を足して明示。
> 3（イトーヨーカドー）は登録できないので鍵から公式X判定を外す。
> 「同じ口が兼ねている会社がある」ことを、設計書 §3-1-1 に実例つきで追記してください。
> 原則の例外ではなく、原則が想定していなかった形として。
> ## 5. 判断6: 選択肢C4（触らない）… 記録だけ残してください。

## 指示と過去の報告の食い違い

なし。

## 1. マージ2件

```
PR #1322（判断3・ec_source_name）   e7f0704f
PR #1323（判断9・綴りを1本に畳む）    8781628d
```

`#1323` は base を main へ切り替えたあと、最新 main を取り込んで CI を通しました。
**base が main でない PR には CI が起動しません**（`pull_request: branches: [main]` の
条件から外れる）。これは今回の副産物として分かったことです。

PR固有の赤は0（main の実データ試験と同じ4件）。`build` の
`ページを辿ると全件を重複なく取れる（286 !== 288）` も `test_url_candidate` も、
`origin/main` のチェックアウトで同じように落ちることを手元で実測しました。

**`store_keys` を2本減らしても配信の関門は動きません。**
`publish_reviewed_only.ゴールデンの店か()` は `store_canon._norm`（空白を落とす）で
引くので、`駿河屋通販` も `駿河屋 通販` も同じ判定です（main と枝で実測して同値）。

## 2. 判断4（A2）→ PR #1324

### やったこと

| | 内容 | 根拠 |
| --- | --- | --- |
| 実店舗の口を登録 | `駿河屋` → `@surugaya_kosin` | suruga-ya.jp のフッター「駿河屋オフィシャルSNS」 |
| 同じ口を明示 | `ヤマシロヤ オンラインショップ` / `ヤマシロヤオンライン` / `ヤマシロヤ通販` → `@toy_yamashiroya` | 通販サイト e-yamashiroya.com 自身が埋め込んでいる |
| 同じ口を明示 | `ポケモンセンター` → `@pokemoncenterpr` | shop.pokemon.co.jp のフッター「ポケモンセンター公式SNS」 |
| 同じ口を明示 | `あみあみ 通販` → `@amiami_figure` | amiami.jp に出てくる X リンクはこれだけ |
| 無いと確かめた | `イトーヨーカドー ネット通販` | iyec.itoyokado.co.jp に X へのリンクが1件も無い |

### 途中で見つけた副作用（測って気づきました）

`駿河屋` に本体の口を足すと、`_norm('駿河屋通販')` が `駿河屋` と同じ鍵になって
**衝突し、空白なしの綴りが引けなくなりました**。

```
足した直後   駿河屋通販   @surugaya_toreka -> 引けない   ← 意図していない
対処        駿河屋通販 → @surugaya_toreka を登録名として足す（完全一致で決まる）
```

**全店名で引き比べていなければ気づけませんでした。**

### `_no_official_x` を新設しました

「**未登録（まだ探していない）**」と「**無いと確かめた**」は別の状態なのに、
後者を書ける場所がありませんでした。`store_x_accounts.json` に
`_no_official_x`（名前 → 確かめた根拠）を足しています。

鍵は `tenant_key`（語を落とさない）です。`_norm` で持つと
`イトーヨーカドー ネット通販` が `イトーヨーカドー` と同じ鍵になり、
**実店舗まで巻き添えで引けなくなります**。試験で留めています。

### 前後の実測（全店名3,506）

```
見た店名 3,506（台帳3,312＋観測名＋配布279＋M1gの鍵＋X表の登録名）
変化 3件（新しく引ける 0 ／ 引けなくなる 2 ／ 引き先が変わる 1）

   駿河屋                  @surugaya_toreka -> @surugaya_kosin   意図した付け替え
   イトーヨーカドー ネット通販    @lets_go_yokado  -> 引けない        意図した停止
   イトーヨーカドーネット通販     @lets_go_yokado  -> 引けない        綴り違い・同上
```

**意図しない取りこぼしは0です。**

影響範囲: 配布行 **0**／保留 **0**／候補 **3件（すべて pending）**。
イトーヨーカドーの候補は根拠が公式サイトのページ（iyec.itoyokado.co.jp）と
まとめ1件なので、**公式根拠は失いません**（X の口は根拠に使われていませんでした）。

### 設計書

**§3-1-1-b（新設）** に、ご指示のとおり「原則の例外ではなく、原則が想定していなかった形」
として書きました。

```
上の「正しい形」は、実店舗と通販に別々の口があることを前提にしている。
実測すると、そもそも口が1つしかない会社があった。
   → 同じ口が兼ねているなら、その口を各行の登録名として並べて書く。語落ちに頼らない。
   → 口が存在しないと確かめた場合は、空欄ではなく「無い」と書く。
   → 「未登録（まだ探していない）」と「無いと確かめた」は別の状態である。
```

## 3. 判断6 → 記録だけ残しました（触っていません）

設計書 §3-1-1 の「語尾はコードに意味を与えていない」の段に足しました。

```
例外は link_golden_chains.SUFFIXES だけで、これは表示の枠を作るときに
ベース名の後ろを許す語である。`（アプリ）` もここに入る（2026-09-03 決定・
コジマ（アプリ）と キッズリパブリック（アプリ）が該当）。
「応募方法だから店名の鍵から外す」という整理も成り立つが、外すとその名前で
来た回がチェーンのカードに出なくなるので、2026-09-05 に据え置いた（判断6）。
```

## 4. CI

main の基準は `gh workflow run python-test.yml --ref main`（run 33968428485）。

```
main       失敗5件
#1324      失敗5件   diff が空（完全一致）
```

**PR固有の赤は0**です。

```
python -m unittest test_official_x_lookup_key test_store_x_match test_store_x_watch \
  test_official_announce test_url_fields test_shadow_url_owner \
  test_register_x_from_evidence test_own_source_not_posted test_store_field \
  test_golden_chains_data                                     Ran 350 tests  OK
```

新しい試験9本（`無いと確かめた行Tests` 7本・`同じ口が両方を兼ねるTests` 2本）。

`shadow_url_owner.json` は台帳が変わると `test_shadow_url_owner` が赤くなるので
作り直しました（`--raw`＝KV無し。件数の変化は0で、動いたのは指紋2つと
`fixes_applied` だけ。直近の巡回コミットも `fixes_applied: false` です）。

## 5. PR #1241 のレビュー材料は別便で出しました

https://raw.githubusercontent.com/shinonomeheta-ai/cardbot-reports/main/reports/2026-09-05-pr1241-review.md

**PR #1241 の URL: https://github.com/shinonomeheta-ai/cardbot/pull/1241**

要点だけ:

```
まとめ由来の根拠は0本（公式サイト26・公式X投稿6・同じ会社の別ドメイン1）
裁定を採っても配布データは1行も動かない（apply_methods を読む Python は0件・画面の文字だけ）
needs_check は 8件 → 1件（TSUTAYA だけ残る）
ただし registered_x / registered_site は2日ぶん古く、
   「登録なし」15件のうち8件は**いま登録済み**（段1〜§4 の16本で変わった分）
```

## 判断していただきたいこと

1. **PR #1324（判断4・A2）をマージするか。**
2. PR #1241 のレビュー結果（判断10・C3 の反映）。

## 状態

- PR #1322・#1323 は**マージ済み**。
- **PR #1324 を出しました。マージはしていません**（指示待ち）。
- 判断10 は**着手していません**（#1241 のマージまで着手しない、というご指示のとおり）。
- 判断6・7・11 は変更なし（記録のみ）。
- 外部への通信は**公式サイトの読み取り（GET）だけ**。課金はありません。
