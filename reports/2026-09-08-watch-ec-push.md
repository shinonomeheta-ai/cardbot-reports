# 見張り: **判定②' が本物を捕まえました**（ec-lottery-watch の 4 run が push で落ち、成果が main に無い）。ただし取り戻せる型です

セッション名: metrics
日付: 2026-09-08（JST・14:35 時点・実測）
区切り/依頼名: health-watch run 34192467449 の中身を追う
対象: `origin/main` = `72f9eef5`

## 冒頭

```
時点 2026-09-08 14:35 JST ／ origin/main 72f9eef5
画面 6 社（実時刻）／上流 19 社 33 回（本部名だけ）／前日同時刻比 ±0 社
```

## 鳴った中身

```
!! ec-lottery-watch: push の段で落ちて成果が main に載っていません
   （run 34126279782・34124351956・34122429481・34107348106 — いずれも Commit artifacts）
 ! promote-now:    直近24時間に通過率の行が1つもありません
 ! resolve-dates:  直近24時間に通過率の行が1つもありません
```

## 1. `ec-lottery-watch` の 4 run —— **本物です。行は main に1本もありません**

4 run すべて、`history/passrate/` に**その run の行が1本もありません**。走って落ちたのではなく、**成果ごと main に載っていない**という判定②' の通りでした。

**いつ落ちたか。** run の番号を、main に載った run と並べると窓が分かります。

| run | 結果 |
|---|---|
| 34107348106 | **落ちた** |
| 34117354107 | 載った（09-07 20:41） |
| 34122429481 | **落ちた** |
| 34124351956 | **落ちた** |
| 34126279782 | **落ちた** |
| 34133142765 | 載った（09-07 23:33） |

**09-07 の夕方から夜にかけての窓**です。**今日の run（09-08 09:47・10:40）は両方とも載っています。** いま落ち続けているわけではありません。

## 2. 失ったものの大きさ —— **小さく、取り戻せます**

main に載った run の実測です。

| | 1 run あたり |
|---|---|
| S1（EC 公式一覧から拾う） | 入 32〜48 → 出 **中央値 2**（0〜6） |
| S2b（同じ workflow の次の段） | 入 21〜22 → 出 **中央値 2**（0〜3） |

**1 run で新しく拾うのは 2 件前後**です。4 run ぶんでも 10 件前後。

**そして取り戻せます。** `ec-lottery-watch` は毎回**同じ一覧ページを読み直す**作りで、「新しいか」の判定は main の側にあります。**押せずに落ちれば、次の run が同じものをもう一度「新しい」として拾います。** 実際、落ちた窓の直後の 23:33 の run は載っています。

**残る実害は取得の手間だけ**です（同じページを取り直す）。**告知そのものを失ってはいません。**

## 3. `promote-now` と `resolve-dates` の 2 本 —— **こちらは誤報です。直しを枝に置いてあります**

**この 2 本は定時を持ちません。** `promote-now` は管理画面の保存が成功したときの `repository_dispatch` だけ、`resolve-dates` は `workflow_dispatch` だけで、日次の仕事はどちらも `update-data`（と `nyuka-watch`）の中に相乗りしています。

**実測**: 09-06〜09-08 の 3 日間で `promote-now-*.jsonl` と `resolve-dates-*.jsonl` は **0 本**。一方 `V7` の行は `nyuka-watch` と `update-data` から**30 分おきに出ています**（`applied=1`）。**段は動いており、切れていません。**

**期待表からは外しません。** 走った回に部品の行が無ければ、そのときは配線の欠落として鳴らす必要があります。**`ON_DEMAND` を 1 つ足して、行が無いときの文言だけを分けました**（試験 2 本つき）。

- 枝: `fix/passrate-on-demand`（`bb06bf2a`）
- 直した後の文言: `promote-now: 直近24時間に行が無い（起こされたときだけ走る段。正常）`

**PR を作れていません。** この環境の `gh` が `shinonomeheta-ai/cardbot` を解決できず（`Could not resolve to a Repository`）、`gh pr create` が通りません。**枝は push 済みなので、PR を立ててもらえれば CI に載ります。**

**しきい値は上げていません。** 鳴る条件は同じで、**「走ったのに行が無い」は今まで通り鳴ります**。変えたのは「そもそも定時が無い段」の文言だけです。

## 状態

報告のみ。**本物 1 件**: `ec-lottery-watch` の 4 run（09-07 夕〜夜・失ったのは 10 件前後・次の run が取り直す型なので告知は失っていない）。押せなかった原因は run のログが要るので **ci へ渡します**。**誤報 1 件**: `promote-now` と `resolve-dates`（直しは枝 `fix/passrate-on-demand`・**PR を立ててほしい**）。
