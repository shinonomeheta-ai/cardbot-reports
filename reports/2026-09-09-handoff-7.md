# 引き継ぎ書（更新7）— 締切順をマージした。掘った先は4回とも空振り（2026-09-09）

宛先: 次のセッション
前任: Claude（セッション ci・2026-09-08 20:40 〜）

**前版**: [更新6](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/2026-09-09-handoff-6.md)

---

## 0. いちばん大事なこと

**今日の実効打は「締切順」1本だけ（#1497・マージ済み）。**
そこから先を4回掘って、**4回とも見立てが外れた。**

```
「商品名が空の9件を直せば出る」  → **トレカでない企画**だった（家電・映画ポスター）
「116件を作り直せば送れる」      → まとめだけの回が混ざる（100件へ訂正）
「100件を作り直せば送れる」      → 70件は中身が取れていない（19件へ訂正）
「Chrome 描画で19件が取れる」    → **実際に描画したら中身は取れなかった**（0件）
```

**掘る前に測ったもの（締切順）だけが残った。**
実装してから気づいていたら、19件のために全経路へ Chrome 描画（1件数秒）を入れて、
何も改善しないところだった。

---

## 1. マージした — #1497 AI へ聞く並びを締切の近い順に

並びは `sorted(現在)` ＝ **応募回IDの辞書順**だった（2026-08-22 の初期実装
`bc1f1f58` のまま。順番に意図は無い）。**締切とは無関係。**

```
                いまの並び      締切順
上位 20件      生きた回 0件  →  20件
上位 40件            2件  →  40件
上位100件           13件  →  100件
```

**今日の20件は生きた回が0件。** 全部、締切切れに $1.93 払っていた。
**費用は1円も変わらない。**

段は3つ（`check_candidates_ai.並びの鍵()` の1か所で決める）:

```
0 締切が未来   171件  近い順
1 締切が空     424件  暫定回（§5-1 の経路を止めないため、切れより先）
2 締切切れ     739件  新しく切れた順
```

### **次のセッションが最初にやること: 効果を測る**

**まだ効果を測っていない。** 次の AI 巡回（`candidate-ai-daily` 朝06:40 /
昼12:40 / 夜21:40 JST）のあとに:

```
① 送った20件が「締切が未来」だったか（前は0件）
② 答えが返ったか（conflict / insufficient_evidence が多くないか）
③ 画面の社数が動いたか（前の集合＝27社・66行 / 受付中5社・6行）
```

**「効果は後の集合 − 前の集合」。** 前の集合は下に記録してある。

---

## 2. 本業の詰まり（本番と同じ引数で数えた確定値）

```
応募回 3,094件                       全部    うち締切が未来
  ai_not_run              1,522件      294件   ★ 最大の詰まり
  ended_or_no_end         1,044件        0件
  OK                        214件      209件
  missing_identity          172件       31件
  not_lottery                98件        0件
  ai_error                   38件       18件
  no_official_evidence        6件        6件
```

材料の状態:

```
  ok                     1,946件   412件
  evidence_cache_missing 1,111件   146件
  evidence_cache_stale      37件     0件
```

「抜粋が無い」146件の内訳（**掘った結果、直せるものは無かった**）:

```
  まとめの本文だけ        45件   出さないのが正しい（§0-1）
  本文が短すぎる(<300字)  51件   中身が取れていない
  JS必須で中身が取れない   19件   Google Forms 18件。**Chrome 描画でも取れない**
  本文はある（要確認）     16件   ガンダムのプラモデル等が混ざる
  その他                  15件
```

### Google Forms を掘った記録（結論: いまの道具では取れない）

```
docs.google.com 16 / forms.gle 2 / books.rakuten 1 / cardbox.online 1
取得の状態: 全部 ok（HTTP 200 で「JavaScript を有効に」の案内が返る）
→ 失敗していないので再取得も render_text も走らない
→ 実際に Chrome で描画したら 115,421字の HTML が取れたが、
   本文にすると **264字**（reCAPTCHA / 利用規約 / ヘルプの定型文だけ）
   質問項目は JS でさらに描かれるので出てこない
```

**やるなら「フォームの中身を取る」別の手段が要る。** 描画を足すだけでは効かない。

---

## 3. 本業の段（**前の集合**・2026-09-09 11:10 JST）

```
① ゴールデン台帳       55社
② 候補台帳に居る       50社   ← 落ちるのは5社だけ（発見は詰まっていない）
③ 配布行に出る         27社・66行
④ 受付中で出る          5社・6行
目標                   20社
```

### ★8社は「持っているのに出せていない」ではなかった

| 社 | 中身 | 判定 |
| --- | --- | --- |
| **イエローサブマリン** | ポケカ 30th CELEBRATION 抽選販売 9/16〜25 | **本物** |
| **竜星のPAO** | ポケカ MEGA 30th CELEBRATION カードセット | **本物** |
| HMV&BOOKS online | 商品名も抜粋も無い | 要確認 |
| お宝創庫 | 店のトップページを取っただけ | 材料が違う |
| BIC CAMERA | Antigravity / Osmo 360 II（カメラ） | **トレカでない** |
| YAMADA | 防災バッグ・スピーカー・ヘアアイロン | **トレカでない** |
| ドン・キホーテ | キャリーケースの感想 → majica | **トレカでない** |
| セブンネット | 映画のポスター | **トレカでない** |

**`missing_identity`（商品名が空）で止まるのは、関門が正しく働いている証拠。**

本物2社が出ない理由:

```
イエローサブマリン  材料は揃っている（本文146字・抜粋ok）。AIの答えが conflict
竜星のPAO          公式 pao.membercard.jp が **http_403**。まとめの本文しか無い
```

**竜星のPAO は「403 を突破しないと出せない」。** 抜粋の作り直しでは解決しない。

---

## 4. 課金（実測）

```
2026-08   1,245回 $48.59   1回 $0.039
2026-09   1,025回 $99.11   1回 $0.097  ← 2.5倍。**9日で月の天井 $100 に到達**
```

1日20件＝月 $58（約¥8,700）。**いまの20件ですら当初予算（月¥5,000）超過。**
上限は `candidate-ai-daily.yml` に `max_requests=20` `max_usd=1.10` が直書き。

**方針 B→D→A の現状**（本人決定）:

```
B 締切順        **完了・マージ済み**（効果は未測定）
D 商品名が空    **取り下げ。** 正しく止まっている
A 単価2.5倍     **未着手。** 同じ予算で2.5倍聞けるので、次に効く見込みが大きい
C 予算を上げる  保留（本人判断）
```

**次にやるなら A**（単価が8月$0.039→9月$0.097になった原因）。

---

## 5. 未マージのブランチ 1本

```
fix/classify-red3
  ・月の天井の隔離（C型の最後）
  ・test_event_id_redirects を緑にして契約へ（移行中 7→6）
  ・test_lottery_overrides のデータ実害を記録
```

**試験は全部緑**（8,141件 OK・今日はじめて）。しかし step
「テストがファイルを汚していないか」で赤。

```
?? history/passrate/2026-08-22/
?? history/passrate/2026-09-09/python-test-<run_id>.jsonl
```

**今まで `unittest` が先に落ちていたので、この step まで到達していなかった。**
前から存在していて赤に隠れていた問題。私の3試験は汚さないことを確認済み。
**犯人は未特定**（手元で全件を走らせて絞り込み中だった）。

**特定するまでマージしない。** 入れると「試験は緑なのに CI は赤」が main に載る。

---

## 6. CI の型分け（前フェーズ・目的達成）

```
赤 8 → 2 ／ 飛ばし 19 → 66 ／ 移行中 6件 ／ データ契約 86件緑
A 本当のバグ 0件 ／ B 実データ 6件 ／ C 外の状態 3件（全部片付いた）
```

**CI ログから試験名を数えないこと。** 今日3回、別々の理由で間違えた。
信用できるのは `failures + errors` の数だけ。

---

## 7. 触ると危ないところ

* **`lottery_io.save()`** — 書き出す直前が `round_id` の唯一の確定点。
  推測で行を消さない。**計測器を先に**
* **データ契約** — 赤いものを入れると巡回が止まる
* **`load_tests` の配線** — 忘れると緑のまま何も見ていない
* **AI の件数・並び** — 1件あたり課金。上限を上げる判断は本人の承認
* **`event_id` 台帳** — `test_lottery_overrides` のデータ実害（5行が管理画面で409。
  同じ `round_id` なのに配布行と控えで `event_id` が違う。TierOne 渋谷店の2件は
  互いに入れ替わっている）

---

## 8. 本人にしかできないこと

管理画面 https://torecanavi.app/admin の**手動補正**タブ。

| 店 | いま | 直す | 締切 |
| --- | --- | --- | --- |
| **ブックオフプラス新宿駅西口** | `BRIGHTNESS` | `BRIGHTNESS OF HOPE` | **2026-09-09（当日）** |
| MINT GAMES池袋店 | `30th` | `30th CELEBRATION` | 2026-09-13 |

---

## 9. 前任が今日踏んだ誤り（16件）

推測で場所を2回外した／古い基準で CI を4回比べた／`round_id` 非空を関門にと言った
／直読み4本と言った／`url` を主キーにと言った／「本当のバグ3件」（全部違った）／
CI の名前の数え方を3回間違えた／印スクリプトの判定が甘く1件飛ばした／空の台帳の
鍵の名前を間違えた／`GI.チェーン()` の呼び方を間違えて「0社」／`rounds_of` に
`sc["candidates"]` を渡して「送れる回0件」／`AI昇格できるか` に `date` を渡して
「1,066件が型で落ちる」と誤報／**★8社を中身を見ずに数えた**（半分以上がトレカで
なかった）／**「116件→100件→19件」と掘るたびに数が減った**／
**「Chrome 描画で取れる」も外れた**（実際に試して0件）／heredoc の罠2回。

**共通する型: 実物を確かめる前に、名前や見た目で判断して数を出した。**

**効いた判断はどれも「先に測る道具を作った」もの。**

```
計測器（#1490）           推測2回で外した場所を、巡回1回で特定
突き合わせ（#1492）       切替前に 311件の事故を止めた
立て忘れの見張り（#1494） 「緑のまま何も見ていない」を防ぐ
移行中の一覧（#1495）     「症状は消えたが巡回が止まる」を防ぐ
締切順の実測（#1497）     直す前に 0件→20件 を測ってから実装した
Chrome で1件試した        **実装前に「効かない」と分かった**
```
