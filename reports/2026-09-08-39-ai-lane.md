# 画面 7 社（12 行）／うち公式根拠 11 行／上流 33 社・前日同時刻 −6 社。**11 件・7 社は 1 件も昇格できない（材料 4・未送信 3・身元なし 3）**

セッション名: metrics
日付: 2026-09-08（JST・05:10 時点）
区切り/依頼名: 39 件を AI レーンの判定で分類し、`ai_not_run` を a/b/c に分ける
対象: `origin/main`・絞り方の正本（`rounds_of`・締切は実時刻・本部のみ）

## 受けた指示（原文・要点）

> 39件を AI レーンの判定で分類し直すこと。人の決着ではなく AI 昇格できるか で。
> 特に11件・7社は、通れば画面の社数が増える相手。39件のうち最も小さく確かめやすい集合。ここから。
> そのうえで ai_not_run を3つに分けること（a 材料が無い／b 送っていない／c 返っていない）。

## 指示と過去の報告の食い違い

**件数が 39 → 34、11 → 10 に減っています。** 絞り方の正本（`rounds_of`・**締切は実時刻**・本部のみ）に揃えたためで、前回は締切を暦日で見ていました。以後は 34 件・10 件で扱います。

## 報告

### 1. 34 件（本部・締切が未来・`reviews` に行なし）を AI レーンで分類

| 判定 | 件 |
|---|---:|
| `ai_not_run` | **23** |
| **昇格できる** | **8** |
| `missing_identity`（店名か商品名が空） | 3 |

**8 件は AI レーンで既に昇格できる状態です**（晴れる屋2 2・フルコンプ 2・GIRAFULL 2・ファミマオンライン 1・コジマ 1）。**ただし 5 社とも既に画面にいます**ので、通っても社数は増えません。

`ai_not_run` 23 件の内訳:

| 分類 | 件 | 相手 |
|---|---:|---|
| **a. 材料が無い（`no_excerpt`）** | **10** | 案B |
| **a. 材料が無い（`not_official`）** | **8** | 公式根拠の側 |
| **b. 送れる形だが送っていない** | **5** | V5 を回す |
| c. 送ったが返っていない | 0 | – |

### 2. 10 件・7 社（配布行が 1 行も無い社＝通れば社数が増える相手）

| 判定 | 件 |
|---|---:|
| `ai_not_run` | **7** |
| `missing_identity` | **3** |
| 昇格できる | **0** |

**1 件も昇格できません。** `ai_not_run` 7 件の内訳:

| 分類 | 件 |
|---|---:|
| **a. 材料が無い（`no_excerpt`）** | **4** |
| **b. 送れる形だが送っていない** | **3** |
| c | 0 |

**この 10 件が ai_not_run 1,000 件の縮図です。**

- **a 4 件**（材料）→ 案B。抜粋が作れていない。
- **b 3 件**（未送信）→ V5 を回すだけで動く。**いちばん安い。**
- **`missing_identity` 3 件**（店名か商品名が空）→ **a/b/c のどれでもない 4 つ目の相手**。AI 以前に候補台帳の欄が埋まっていません。N（店名の正規化）か商品マスタ（M2）の話です。

### 3. 全体との対応（ci・store-name-a・roundup と揃える）

| 母数 | a 材料 | b 未送信 | c 返っていない |
|---|---:|---:|---:|
| 受付中の `ai_not_run` 1,000（全体） | 465 | 505 | 30 |
| 34 件のうち `ai_not_run` 23 | 18 | 5 | 0 |
| **10 件のうち `ai_not_run` 7** | **4** | **3** | **0** |

**全体では a と b が半々ですが、画面に一度も出ていない社に絞ると a が多数**（18/23・4/7）です。roundup の独立測定（全体 a 479・b 519・c 25／M1g では a が 6 割）とも一致します。

**c は 0 件**でした。取り込みの直しは、この相手には効きません（全体の 30 件は別の社の回です）。

### 4. 明日の相手（この集合に限った順番）

1. **b 3 件**（+ 34 件側の 5 件）— V5 を回すだけ。コード変更なし。**ci**
2. **`missing_identity` 3 件** — 店名か商品名を埋める。**store-name-a / M2**
3. **a 4 件**（+ 34 件側の 10 件）— 案B。**store-name-a**
4. `not_official` 8 件（34 件側）— 公式根拠の側。私の側では中身を見ていません

**社数を増やす最短は b の 3 件**です。ただし 3 件が 3 社に散っているか 1 社に固まっているかで効き方が変わるので、**必要なら round_id と社名の一覧を出します**（言っていただければ次に出します）。

## 状態

報告のみ。**確定した数**: 34 件（昇格できる 8・`ai_not_run` 23・`missing_identity` 3）／10 件（`ai_not_run` 7・`missing_identity` 3・昇格 0）。**新しく見つけた相手**: `missing_identity` 3 件（a/b/c のどれでもない）。
