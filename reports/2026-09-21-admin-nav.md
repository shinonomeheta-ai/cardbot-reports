# 管理画面の左メニュー: 広く・読みやすく、開くのは1領域だけ（#1650）

2026-09-21。本人 2026-09-20「左側ちょっと見づらいか。バーがちょっと狭いな。もう少し使いやすくして」
→「左のメニューが多すぎて、何選べばいいかわかんない」。
PR #1650 をマージ（merge commit `b017b6bb`）。本番デプロイ成功。

## 実測（直す前）

* 帯220px・押せる幅176px・1行34px・字13.5px
* 節が24あり、中身1168pxが画面774pxに入らない（マスター・設定まで毎回巻く）

## 直したこと

* 管理画面だけ帯を264pxへ（公開画面の220pxは触らない）。1行 34→38px、アイコン 14→15px
* **開くのは1領域だけ**。ほかは見出しだけで、中の数を出す（運用6・アナリティクス1・マスター12）
* 見出しを押すと、開くと同時にその領域の代表の画面へ移る
* 開いている項目に一行の説明（2行まで）
* 節を移ったとき、光っている項目が画面の外なら見える所へ送る

## 確かめたこと

* 本番で実測: 帯264px・見出し4つ＋項目5つ・中身774px＝窓774px（巻かずに全部見える）
* `node --test app`：main と比べて新しい赤 0。`npx next build` 成功
* 見た目の確認で手元のビルドだけ一時的に管理者扱いにしたが、その変更は戻して commit に入れていない

## 注意

本番の管理画面は**古い画面がキャッシュで残る**。`?v=…` を付けて開くと新しい方が出る。
