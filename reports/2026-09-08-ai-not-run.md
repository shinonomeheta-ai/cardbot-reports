# 画面 7 社（12 行）／うち公式根拠 11 行／上流 33 社・前日同時刻 −6 社。**`ai_not_run` 1,000 回の内訳: 材料が無い 465／送っていない 505／送ったが返っていない 30**

セッション名: metrics
日付: 2026-09-08（JST・04:40 時点）
区切り/依頼名: なぜ `ai_not_run` が 1,000 件あるのか（a/b/c の内訳）
対象: `origin/main`・`shadow_candidate_ai_health.json`（Batch 119 本）

## 受けた指示（原文）

> なぜ ai_not_run が1,000件あるのか。次のどれか、内訳を出すこと。
>   a. V5 に送れる形になっていない（材料が無い）
>   b. 送れる形だが、まだ送っていない（処理量・順番）
>   c. 送ったが結果が返っていない／取り込めていない
> a なら案B、b なら V5 を回す、c なら取り込みの直し。相手が変わる。

## 指示と過去の報告の食い違い

**「人の決着は廃止済み」という前提の訂正を受け取りました。** 私が昨夜出した「39 件・19 社を確認すれば効く」は取り下げます。廃止した仕組みを手で使う道なので採りません。

## 報告

### 0. 結論を先に

**b（送っていない）が最多で 505 回、a（材料が無い）が 465 回、c（返っていない）が 30 回です。** 相手は 1 つではなく、**b と a がほぼ半々**です。

| 分類 | 回 | 相手 |
|---|---:|---|
| **b. 送れる形だが、まだ送っていない** | **505** | **V5 を回す（処理量・順番）** |
| **a. 送れる形でない（材料が無い）** | **465** | **案B（抜粋）** |
| └ `no_excerpt` | 449 | |
| └ `not_official` | 15 | |
| └ `evidence_changed` | 1 | |
| **c. 送ったが結果が無い** | **30** | **取り込みの直し** |

母数は**現役（`rounds_of`）の受付中で `ai_not_run` の 1,000 回**です。

### 1. 判定の仕方

- **a**: `candidate_ai_cache.unsendable_reason` が非空（送る側の判定そのもの）。
- **c**: `shadow_candidate_ai_health.json` の `haiku_batches` の `round_ids` に含まれるのに、結果台帳に答えが無い回。**Batch は 119 本（collected 114・submitted 5）で、送信に含まれた回は 1,204。**
- **b**: 上のどちらでもない＝送れる形なのに、まだ Batch に入っていない回。

### 2. 配布行が 1 行も無い本部（15 社）の 28 回

| 分類 | 回 |
|---|---:|
| a. 材料が無い | **18** |
| b. 送っていない | **10** |
| c. 返っていない | 0 |

**この 15 社に限ると a が多数（18/28）**です。全体では b が多いのに、**画面に一度も出ていない社では材料が無い側が主**という差があります。**案B が効く相手はここに集中しています。**

### 3. c の 30 回（取り込みの直し）

`submitted` のまま残っている Batch が **5 本**あります。この 30 回はそこに含まれる回か、`collected` だが結果が入らなかった回です。**roundup の未回収 batch 2 本（`msgbatch_013gN8p1kiXj69hQT22TMeKM` 3 回・`msgbatch_01D8p3aBQC3deamAdRAcNaYs` 11 回）もこの 5 本に含まれる可能性が高い**ので、回収すれば 14 回ぶんは動きます。**課金は発生しません**（既に払った答えの取り込み）。

### 4. 明日の相手（私の見立て）

| 相手 | 対象 | 誰が |
|---|---:|---|
| **V5 を回す（b）** | 505 回。うち画面に一度も出ていない社の分は 10 回 | ci（課金・私は回しません） |
| **案B（a）** | 465 回。うち画面に一度も出ていない社の分は 18 回 | store-name-a |
| **取り込み（c）** | 30 回。うち未回収 Batch 5 本ぶん | ci（課金なし） |

**c がいちばん安い**（払った答えを取り込むだけ）ので、順番としては c → b → a が費用対効果で優れます。ただし**画面の社数を増やす**という目的に限れば、**a（18 回）が最大の相手**です。

`store-name-a` の「V5 残り 187 回」がこの内訳のどこに当たるかは、**そちらの 187 回の round_id を教われば突き合わせます**（私の b 505 の部分集合と見ています）。

## 状態

報告のみ。**取り下げ**: 39 件・19 社の人による確認（前提の訂正を受け入れ）。**渡すもの**: a/b/c の内訳と、未回収 Batch 5 本の回収（ci）。
