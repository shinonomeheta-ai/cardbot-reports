# ci の13本は「6本は読める・7本は消えている」。ついでにまとめXの表が2枚あってずれていた（影響は今日0回）

日付: 2026-09-08（JST・13:05 時点）
区切り/依頼名: ci の `source_text_missing` 13本をブラウザで確かめる
セッション名: roundup

## 冒頭（指定の形）

| | |
|---|---|
| **画面** | **7 社 11 行**（2026-09-08 12:38 JST・`py upstream_count.py` の出力） |
| **上流** | **19 社 35 回**（同じ道具・正本の数え方） |

## 報告

根拠データ: [2026-09-08-x13-and-curation-gap.json](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/data/2026-09-08-x13-and-curation-gap.json)

### 1. ci の13本: **6本は読める・7本は永久に読めない**

ci から `source_text_missing` の x.com 13本を受け取り、この PC のブラウザで全部開きました（読むだけ・台帳は書いていません）。**形が2つに、きれいに分かれました。**

| 形 | 本数 | 結果 |
|---|---:|---|
| **プロフィール**（`/ハンドル`） | **6** | **全部読めた** |
| **個別の投稿**（`/status/…`） | **7** | **全部読めない**（削除6・アカウント凍結1） |

読めたプロフィール: YS_INFO（イエローサブマリン宣伝）・bicikebukuro（ビックカメラ池袋本店）・bookoff_NGRYM・pokecayoyaku・shop_TSUTAYA・ts_kamie（TSUTAYA上江別店）。

読めない投稿は `BOOKOFF_JP2008` `GEO_sagahyougo` `MEGA_ENTERME` `bic_shibuya` `pokecachan` `tsutayaazkonanT` が「このページは存在しません」、`labo_043` が「凍結されたアカウントの投稿です」。

**結論: 13本は1つの原因ではありません。**

- **プロフィール6本は、私のまとめX 4口と同じ形**です。runner が x.com のプロフィールHTMLを直接読めないだけで、**ブラウザなら読めます**。KV の経路（読む役）に乗せれば取れる見込みです
- **投稿7本は再試行しても永久に取れません。** 削除と凍結なので、`source_text_missing` のまま「もう無い」と印を付けて再試行の対象から外すのが正しい扱いだと思います

ci の分類でいう「本文が取れていない 138」のうち、少なくともこの7本は**取りに行く先が消えている**ぶんです。

### 2. ついでに見つかった穴: まとめXの表が2枚あってずれている（**今日の影響は 0 回**）

読んだ口が公式の根拠として扱われないかを確かめたところ、**同じ一覧が2か所にあり、中身が違っていました**。

| 表 | 数 |
|---|---:|
| `accounts.json` の「まとめの口」（巡回が読む正本） | 12 |
| `candidate_contract.CURATION_X_HANDLES`（公式かどうかの判定が見る表） | 14 |

**判定の表に無いまとめの口が3つ**あります: `one_piecetcg`・`dbfw_cardgamejp`・`dourakulkebuku`。この3口の投稿は `is_official_source` が **True** を返し、**公式の根拠として通ります**（実測で確認）。

逆に判定の表にしか無いものも5つあります（`cardchusen_com`・`pokesoku_jp`・`toreca_navi`・`pokeka_chusen`・`afrotcg`）。こちらは余分に守っているだけなので害はありません。

**影響範囲を測りました。今日の実データでの影響は 0 です。**

| | |
|---|---:|
| 応募回の母数（`rounds_of`） | 2,885 |
| この3口を公式根拠として使っている回 | **0** |
| その3口が唯一の公式根拠の回 | **0** |
| 配布行と重なる | **0** |

**いま配布に出ている行は1つも変わりません。** 潜在的な穴で、まだ発火していません。

なお**店マスタの公式X表との分離は保たれています**。まとめの口12は `store_x_accounts.json` に1つも入っていませんでした（本人指示「まとめX は store_x_accounts.json に入れない」は守られています）。破れていたのは「公式の根拠に数えない」ほうの1枚だけです。

**直し方**は、判定の表を `accounts.json` から作る形にして（今の14はまとめサイト側の口を含むので和集合にする）、2枚がずれたら落ちる試験を足す、です。**影響が 0 回なので急ぎません。** また `candidate_contract.py` は4つの呼び出し元が通る共有の場所で、いま ci も store-name-a も触っています。**私のレーンは優先しないという判断を受けているので、実装していません。** 指示があれば小さい PR 1 本で出します。

判断が要るのは1点だけです。`dbfw_cardgameJP` は**ゲーム発売元の公式アカウント**（まとめではない）で、`accounts.json` には発見用の口として入っています。表を機械的に揃えると、この口の投稿は「店の公式根拠ではない」扱いになります。設計（根拠は**その店の**公式）からは正しいと思いますが、確認したい点です。

## 実行したこと・していないこと

- ブラウザで13本を開いて読んだだけ（**台帳・配布物は触っていません**・課金 0）
- 表のずれの実測と影響範囲の計算（読み取りのみ）
- **直していません**（影響 0 回・共有ファイル・優先しない判断）
- `roundup-daily` の schedule はそのまま。収集元3つは止めたまま
