# 第0段の完了と、この先の全体像（2026-09-09 未明）

## 指示文

> OK、1、2、3。ちょっとトークンの残りが少ないから、これから先の全体像と、
> 直近予定している修正と今の修正を報告書に落としておいて。

直前のやり取り:

> これらのものを直せるか直せないか。抜本的に直したい。

（「これら」＝ CI が実データ試験で赤くなり続け、PR の赤を main の基準と比べても
基準が20分で古くなって判定できない問題）

先行する報告:
[判断](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/2026-09-08-data-layer-decision.md)
／[第0段とDB移行](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/2026-09-08-db-stage0-and-sources.md)

根拠データ:
https://raw.githubusercontent.com/shinonomeheta-ai/cardbot-reports/main/reports/data/2026-09-08-db-stage0-and-sources.json

---

## 1. いまの修正（今日やったこと・全部マージ済み）

### 第0段 — 配布行の重複を止める。**完了**

```
朝（f35b74fe）  重複 5組 ／ round_id 空3行
               応募URLを持たない行き止まりが配られていた
いま（abb172b6） pid_dup=0  rid_dup=0  （400行・応募URLあり221）
```

| PR | 中身 |
| --- | --- |
| **#1485** | 公開ID（`店｜商品｜締切日` の sha1＝利用者が踏む鍵）と `round_id` の二重行を畳む |
| **#1490** | 直せなかったので、**計測器だけ**入れた（行は触らない） |
| **#1491** | 計測器が示した場所（`save` の書き出す直前）で畳む |
| **#1486** | **配布行の主キーの関門**。二度と入らない |

**`lotteries.json` は JSON の配列で、主キーの制約も一意性の試験も1本も無かった。**

#### 場所を推測で2回外し、計測で1回で当てた

畳む場所を推測で2か所（発番の直後／書き出す直前）に置いて、**2回とも手元で発火
しなかった**。`save` は配るデータを書くいちばんきわどい場所なので、**確かめる前に
行を消す処理を入れない**と決めて、計測器だけ入れた。巡回1回で決着した。

```
705  ! 同じ応募回の行を 2組 書きます
773  同じ鍵の二重行を畳んだ: public_id=2      ← verify_lotteries の畳み
777  ! 同じ応募回の行を 1組 書きます           ← 畳みの直後の save が作っている
```

**`round_id` は `lottery_io.守る()` で発番され `_古い応募回を外す()` で外れる。
出入りするので、書き出す直前が唯一の確定点。** だから `verify_lotteries` の側で
いくら畳んでも消えなかった（巡回3回とも残った）。

本番で発火し、**呼び出し元まで出た**。

```
2行/2組（round_id=2）（書き出す直前・publish_reviewed_only.py）
1行/1組（round_id=1）（書き出す直前・verify_lotteries.py）
```

### DB（第2段）— 土台と本文の移行

| PR | 中身 |
| --- | --- |
| **#1488** | `db.py`（接続）／`schema/001_source.sql`／`db_schema.py`（当て方の台帳）／`source_store.py`（読み書きの規則）／`migrate_sources.py`／`db-smoke.yml` |
| **#1489** | `schema/002_x_post.sql`／`db-migrate.yml` |
| **#1492** | `compare_source_store.py`（**切り替える前に JSON と DB の答えを突き合わせる**） |

**移行の結果**（run 34238445937・約4分）:

```
入力の行                          5,133
移したあとの行（身元でまとめる）  2,832   ← 45% が重複だった
本文を持つ行                      2,614
url が null になる行                151（作り話で埋めない）
```

**`apply=false` の見込みと1件も違わなかった。**

### 突き合わせの道具が、311件の事故を止めた

移行の2時間後に突き合わせたら:

```
切り替えると本文が消える・減る件数: 311
```

**この道具を作らずに読み先を切り替えていたら、311件の本文が静かに消えていた。**
本文が無い回は AI へ送れず、送れない回は配信に出ない——**数が静かに減る**。

---

## 2. 直近の修正（次にやること・順番つき）

### 抜本的に直す — CI が実データで赤くなり続ける問題

**赤い試験が2種類混ざっている。**

| | 何を見ている | 性質 |
| --- | --- | --- |
| A 規則の検査 | 「畳む規則は正しいか」 | 決定的 |
| B いまのデータの状態 | 「いま配っているデータは健全か」 | **巡回が20〜30分ごとに書き換える** |

**B を `python-test.yml` に置いているのが根本原因。** コードを1行も触っていない PR が
巡回のせいで赤くなる。基準が20分で腐るので、**今日4回、基準を取り直しては古くなった**。

#### この判断は 2026-08-10 に一度されている

`test_lot_key.py` の中に、そのまま書いてある。

> **生データへの assert をやめた**（外部レビュー 2026-08-10）。巡回がデータを
> 書き換えるたびに、コードを1行も触っていない PR が落ちる。実際に 2026-08-10、
> `[skip ci]` のデータ更新で main が赤くなり、**CIが走らないので誰も気づかなかった**。
> 規則そのものは固定の材料で見る（ここ）。**いまのデータがどうかは `check_health` が見る。**

**正しい形は8月10日に決まっていて、そのあと同じ型の実データ assert が増え続けた。**

#### 直し方

```
python-test.yml    規則だけ。固定の材料。**PRの赤は必ずコードのせい**
data-contract      いまのデータ。巡回のたびに、その回のデータで。落ちたら push を止める
```

データ契約は既に正しい場所にある（押す直前・その回のデータ・落ちたら止まる）。
#1486 で主キーの検査を足したのもそこ。

#### 対象（2026-09-08 16:21 の基準・赤9件＋PR で見えた分）

**移すもの（実データを見ている）**

```
test_dedupe_channel.実データ
test_candidate_ai_runner.支店の境界 / 抜粋の版
test_lottery_overrides.管理用の控え（2件）
test_url_candidate.実データ
test_shared_platform_gate.公開データも直してある
test_shadow_url_owner.配る側とそろっている
test_url_fields.配る側と同じ答え
test_event_id_redirects.決めた形だけを読む
```

**移してはいけないもの（実データを読まないのに落ちている＝本当のバグ）**

```
test_rejudge_before.指定した時刻より前の判定は選ぶ
test_date_priority_merge.畳むときは日付を権威で選ぶ
test_candidate_ai_haiku_first.課金前の決着（ERROR）
```

**今朝の `f35b74fe` でも落ちる**ので、今日より前から壊れている。実データを読まない
決定的な試験なので、環境やデータの版とも無関係。

#### 進め方（本人承認済み・1→2→3）

1. **本当に壊れている3件を直す**（バグなので先）
2. 実データ試験を**1つずつ**データ契約へ移す。**1つ移すごとに巡回1回で確かめる**
   （一気に移すと、どれが原因か分からなくなる）
3. `python-test` が緑になったら、**赤を「必ず直す」ルールに戻す**

**効果**: PR の赤が意味を持つ／基準を取り直す作業が消える／データの問題は押す直前に
その回のデータで止まる（いまより早い）／`main-ci-red-realdata` が解消する。

---

## 3. この先の全体像

### 第2段の残り（DB）

**いちばん大事な事実: 巡回はいまも JSON に書いている。DB は写しのまま。**
`source` は移行した時点の断面で止まり、**2時間で311件ずれた**。
**切り替えまで行かないと、DB は4か所目の写し＝負債にしかならない。**

| | 中身 | 状態 |
| --- | --- | --- |
| **A** | 突き合わせの判定を直す | `DB のほうが長いのに中身が違う`を前方一致だけで見ているのが粗い。**取得時刻でどちらが新しいかを見る**ようにして、「移行後に更新された」と「本当に食い違っている」を分ける |
| **B** | **3モジュールの差し替え** | `linked_page_cache` / `candidate_ai_cache` / `official_x_ledger` の中身を `source_store` へ向ける。**呼ぶ側は1行も変えない**のが目標。巡回の経路に本文の直読みは1つも無いので、3モジュールの中だけで済む |
| **C** | `x_post` のデータ移行 | 表はある。中身はまだ |

**B の手順は固定**（間に巡回を挟まない）:

```
移行を流し直す（冪等・4分）
 → 突き合わせて 311 → ほぼ0 を確認
 → その足で切り替える
```

### DB へ移すもの / 移さないもの（判断済み）

```
移す    機械が毎日書き換えるもの（候補台帳・本文・配布行・刻印・素性）
残す    人が決めるもの（golden_chains / store_aliases / accounts / product_master）
```

**「git 履歴で誰がいつ決めたか追える」が本当に効くのは右側だけ。**
左側は機械が1日108回上書きしていて、履歴に人の判断は入っていない。

### Neon の制約（設計に効く）

```
Plan Free ／ Region us-east-1 ／ PostgreSQL 18.6
ストレージ 0.5GB ／ 計算 100 CU-hours/月 ／ ブランチ 10
5分でゼロまで落ちる（無効化できない）
超えたときは**課金ではなく停止**
```

**20分ごとの巡回を DB へ繋ぐ前に、CU-hours の測り直しが要る**（試算で約80/100）。
起こすたびに最低5分ぶんが計算時間になるので、起こす回数がそのまま効く。

### 第0段の残り

**関門は入った。** ただし `round_id` が同じなのに**店が違う**組（刻印が別の店へ
ずれている合図）は畳まずに関門で止める設計なので、**それが起きたら巡回が止まる**。
起きたときは人が見る。いまは0件。

---

## 4. 今日の誤りと、そこから決めたこと

| 誤り | 決めたこと |
| --- | --- |
| 直す場所を推測で2回外した | **手元で再現できない不具合は、直しでなく1行の計測器を先に入れる** |
| 古い基準で CI の差分を4回取った | **基準は使う直前に取る。それでも20分で腐るなら、上の「抜本的に直す」へ** |
| 「`round_id` 非空を関門に」と言った | **空は正当**（`_古い応募回を外す()` が意図的に外す） |
| 「直読み4本」と言った | **実際は2本、どちらも走らない使い捨て**。この段は丸ごと不要だった |
| 「`url` を主キーに」と言った | **92組が正規化すると同じページ**。そのまま移していたら落ちていた |
| heredoc の罠を2回踏んだ | `\` が潰れる。**Write でファイルに書いてから実行** |
| 巡回の切れ目を2回、根拠なく待った | **触るファイルが巡回の書くものと重ならなければ、またいでよい**。#1469 が壊したのは配る値の意味を変えたからで、git の競合とは別 |

**今日いちばん効いた判断は2つとも「先に測る道具を作った」こと**（計測器＝場所の特定、
突き合わせ＝311件の事故回避）。**外した2回は、どちらも測る前に直そうとしたとき。**

---

## 5. 本人にしかできないこと（残っている）

管理画面 https://torecanavi.app/admin からの商品名の補正2件。
人手補正は KV の `site:lotfix` 1本で、**Python 側に書く道具が無い**。

| 店 | いま | 直す | 締切 |
| --- | --- | --- | --- |
| **ブックオフプラス新宿駅西口** | `BRIGHTNESS` | `BRIGHTNESS OF HOPE` | **2026-09-09（今日）** |
| MINT GAMES池袋店 | `30th` | `30th CELEBRATION` | 2026-09-13 |

## 6. 既知のリスク

* **DB は写しのまま古くなり続ける。** 切り替え（3-B）まで行かないと負債
* Neon Free の計算枠は、20分巡回を繋ぐと**足りなくなる見込み**（試算 80/100）。
  超過は**課金ではなく停止**
* `python-test` が赤いままなので、**いま本当に赤を増やしても気づけない**。
  2章の直しが済むまで、PR の判定は「変更の性質」で見るしかない
* 移行後の DB の大きさを測っていない（移行前 8,176 kB）
