# #1468 を通信 0 回で測りました。**「作れなかった」側には効き（+39 回）、「作れたが中身が無い」285 回はほとんど動きません**

セッション名: metrics
日付: 2026-09-08（JST・13:32 時点・実測）
区切り/依頼名: 案B（#1468）の効果を、245 と 285 の両方で測る
対象: baseline `origin/main` = `70f1ba55` ／ PR #1468 head = `4fde9b42`

## 冒頭

```
時点 2026-09-08 13:32 JST
画面 6 社（実時刻）／上流 19 社 33 回（本部名だけ）／前日同時刻比 ±0 社
```

## どう測ったか

**通信は 0 回です。** PR の枝を使い捨ての作業ツリーへ出し、`build_evidence_cache.py --max-sources 0`（取りに行かない）で抜粋だけ作り直しました。実装を読んで `--max-sources 0` が「1本も取りに行かない」ことを確かめてあります。**AI へは 1 件も送っていません**（課金は ci の領分）。

**比べたのは抜粋の中身**です。保存された AI の答えはコードを変えても変わらないので、**「AI の答えが良くなったか」は測れません**。測れるのは「AI へ渡す材料が良くなったか」までです。

## 1. 関門の通り（main と #1468）

| | main | #1468 | 差 |
|---|---:|---:|---:|
| 関門を通る active な回 | 1,089 | **1,128** | **+39** |
| 送れない・`no_excerpt` | 376 | **340** | **−36** |
| 送れない・`not_official` | 35 | 34 | −1 |
| 送れない・`stale` | 8 | 6 | −2 |

**案B の相手（245 回）のうち 55 回が関門を通るようになりました。** store-name-b の「+53」とほぼ同じです（母数の時点が数十分ずれています）。

## 2. **285 回はほとんど動きません**

```
285 ＝ main で関門を通り、保存された AI の答えが insufficient_evidence だった回
```

| | 回 |
|---|---:|
| 抜粋が変わらなかった | **208** |
| 抜粋が変わった | 68（伸びた 46・縮んだ 7） |
| 関門から外れた | 9 |
| **日付・抽選の語が新しく入った** | **5** |
| **商品名が新しく入った** | **3** |
| 抜粋の長さ 中央値 | 168 → **169 字** |

**中央値が 1 字しか動いていません。** 285 回のうち、AI が「足りない」と言った理由（日付・商品）が新しく入ったのは **8 回だけ**です。

## 3. 読み——**切り分けが付きました**

**#1468 は「作れなかったものを作る」直しです。「作れていたが中身が無かった」ものは直しません。**

| 相手 | 数 | #1468 の効き |
|---|---:|---|
| 作れなかった（`no_excerpt`） | 376 | **−36。効く** |
| 作れたが中身が無い（`insufficient_evidence`） | 285 | **8 回だけ。ほぼ効かない** |

**245 と 285 を両方測る意味はここにありました。** 片方だけ見ていたら「案B が効いた／効かない」のどちらの結論にも転べました。**両方測ると、効く相手と効かない相手が分かれます。**

**#1468 はマージしてよいと思います。** 39 回ぶん材料が増え、`excerpt_status == ok` は減っていません（store-name-b の受入基準）。**ただし 285 回はこの PR では片付きません。** 次の相手として別に立ててください。

**store-name-b が既に次の相手を特定しています**: 245 回の残り 167 回は**店名が本文に 1 度も出てこない**回で、切り方では直りません。うち 116 本は公式 X の投稿で、handle が店と結び付いていない（`trusted_store_context` が立たない）ものです。**285 回とこの 167 回は、同じ「材料はあるが結び付いていない」型**だと見ています。

## 4. 測れていないこと（正直に）

**AI の答えがどう変わるかは測れていません。** 保存された答えは古い抜粋に対するものです。**新しい抜粋で AI が何と答えるかは、送ってみないと分かりません**（課金）。**マージ後に ci が回した結果で、285 回が実際にどう動いたかを測り直します。**

## 状態

報告のみ。**意見**: #1468 はマージ可。**渡すもの**: 285 回はこの PR の射程外という実測（store-name-b・レビュー担当）。**次**: マージ後に ci が AI を回した結果で 285 回を測り直す。
