# PR 3本のマージと、バトロコの別名（条件B は残り2件）

セッション名: store-name-b
日付: 2026-09-08（JST）
区切り/依頼名: 今日の①②
対象: `origin/main` = `49e14d14`

**画面 7社（受付中 93行・配布 314行）／上流 21社。**

数の3点。

    時点  2026-09-08・`origin/main` 49e14d14
    母数  配布 314行 ／ `AL.rounds_of` 2,877回
    定義  画面＝受付中（開始済み・締切が未来）の行のうち M1g 本部名で一致した社
          上流＝候補台帳で締切が未来の回のうち M1g 本部名で一致した社

## ① PR 3本をマージした

判定はすべて**完走した run の名前の集合**で行った。

| PR | head | run | 名前の集合 | 判定 |
|---|---|---|---|---|
| **#1446** 範囲外の回の跡を落とす | `f8a4750f` | 34174346261 | 5件 = main（`acee7b40`・34173418711）と**同一** | マージ `9fe8dde4` |
| **#1450** 化けた保存本文 | `639415c4` | 34174798437 | 6件 = main（`9fe8dde4`・34175254232）と**同一** | マージ `6a61591e` |
| **#1452** 別名2台帳＋バトロコ | `a58bba9e` | 34175945249 | 5件 = main（`b26594d0`・34176408736）の**部分集合**（新規なし） | マージ `49e14d14` |

### #1446 の効果（意図した変化）

`test_candidate_ai_runner` の2件が **ERROR → FAIL** に変わった。守りが `KeyError` で
止まっていたのが、動いて違反を指すようになった。件数は同じ2件だが**中身が別物**。

最初に測ったときは `test_url_candidate` が1件多かったが、これは土台の差
（そのときの main のデータ）で、最新 main に揃えたら消えた。

### #1450 は本番の経路にはまだ効いていない

**保存済みの化けた本文は消していない**ので、次の巡回で `読む()` が取り直して
上書きするまで HMV は 0 のまま。api-customer が本番で測り直せるのは**巡回1回のあと**。

## ② バトロコ小山駅前店 の別名（承認済み）

`TCバトロコ小山駅前 ← バトロコ小山駅前店` を `store_aliases.json` に1組足した。

### 効果を同じ木・同じデータで切り分けた

```
#1452 前の状態（learned もバトロコも無し）  混入 2件（お宝創庫 / TCバトロコ小山駅前）
learned だけ入れる（#1452 のコード部分）    混入 2件（同じ）
バトロコの別名も足す                        混入 1件（お宝創庫）
```

### 前の報告の訂正

昨夜「learned の読み漏れで 4件 → 3件」と報告したが、**今日のデータではその効果は
0件**。対象だった `ミント仙台店` の回がデータから消えていた。

コードの直し自体は正しい（宣言があるのに読んでいなかった）し再発を防ぐが、
**今日の数として1件減ったのはバトロコの別名の方**である。数を効果として出すときは
時点を書く、という今夜の2か条がそのまま自分に返ってきた形。

### 配る名前は動かない

`store_canon._specific_branch` が「支店名の別名を系列名へ潰さない」ので、
`canonical_store()` はどちらもそのまま返す。抜粋の境界
（`candidate_evidence_excerpt._別名の表`）はこの絞りを通さないので、
**「同じ店」の宣言だけが効く。`lot_key` も `event_id` も動かない。**

### 申し送り: バトロコ系は39綴りある

候補台帳に39の綴りがあり、同じ形の組が他にも複数ある。

```
TC バトロコ川越駅前 / バトロコ川越駅前店
TCバトロコ沼津駅前 / バトロコ沼津駅前
TCバトロコ横浜伊勢佐木町 / TC バトロコ横浜伊勢佐木町
TC バトロコ藤沢駅北口 / バトロコ藤沢駅北口店   ほか
```

**承認された1組だけを足した。** まとめて寄せるかは別の判断（§3-3
「勝手に既存のものへ寄せない」）。

## 条件B の現状 — **残り2件**

```
支店の境界  1組  store:エディオン と store:トレカキャピタル（ci が回答中）
抜粋の混入  1件  お宝創庫（抜粋がサイトのグローバルナビ・締切 2023-04-09 の古い回）
```

ポケモンカードストアは今日のデータでは混入しなくなっていた（データの変化）。

## ③ 案B について（着手前の整理）

store-name-a から材料を受け取った。**相手は 472 ではなく 283。**

```
受付中・AI未実行・抜粋なし   472（store-name-a の数え直し。metrics の 464 は時点差）
  本文はある                 283  ← 案Bが相手にできるのはここだけ
  本文が1本も無い            189  ← V4 の取りこぼし。案Bでは直らない
     うち長いページ(5000字超) 140
  本文の長さ 中央値 2,549字 / 最大 55,725字
```

母数と定義（store-name-a）: `rounds_of`（superseded を除く）×
`lifecycle_status == "active"` × `results[rid].checked_at` が空 ×
`excerpt_status != "ok"`。

受入試験の材料（KIDS REPUBLIC・抜粋556字が全部ナビ文言で `excerpt_status` は `ok`
なので状態では捕まらない型）も受け取った。受入の基準は
**「ナビだけの節を採らない」と同時に「KIDS REPUBLIC の11回が
`insufficient_evidence` から動く」**——前者だけだと「何も採らない」でも通る、という
指摘も含めて妥当だと思う。

**metrics の「直し方の違う集合への分解」を待ってから設計に入る**（指示どおり）。

## 429 の再分類は保留

**api-customer の #1440 は未マージ（OPEN）**。入っていない状態で分類すると
「叩けていなかったのか本当に無いのか分からない」に戻るので、着手しない。

## 根拠データ

- [2026-09-08-merge-three-and-alias.json](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/data/2026-09-08-merge-three-and-alias.json)
  — 3本の run と名前の集合、別名の切り分け（同じ木・同じデータ）、条件B の現状、
  案B の相手の分解。

## 未着手

- ③ 案B の実装（metrics の分解待ち）
- 429 の再分類（#1440 待ち）
- 条件B の残り2件（急がない、とされたもの）
- 17社の一覧特定（急がない）
- `www.dmp-ranking.com` の持ち主
