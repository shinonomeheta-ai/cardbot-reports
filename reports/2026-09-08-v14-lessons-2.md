# 9/8 の分（入れ物の型・運用7つ・名前の相談）を設計書 v14 へ（PR #1454 に追加）

日付: 2026-09-08（JST）
区切り/依頼名: 設計書 v14 への取り込み・今日の分（セッション docs）

前の報告: [2026-09-07-v14-lessons.md](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/2026-09-07-v14-lessons.md)（9/7 夜の分）。
**追記の禁止（reports/README 決まり 9）に従い、新しいファイルで出しています。**

## 受けた指示（原文）

> 今日の分を足すこと。急がない。
>
> 【1つの型としてまとめる】「書く側と読む側で見ている入れ物が違う」
>   full 段の抜粋を runner の終わりで捨てていた（#1460）
>   roundup-daily の記録だけの行に控えが無い（#1463）
>   別名は登録済みだが当てる段が無い（#1198 と同型）
>   本文の置き場が2つある（linked_page_cache と evidence_cache の sources。
>     後者は指紋で引くので URL 文字列では当たらない）
>   まとめXの口の表が3か所でずれている（#1467）
>   store-name-a の「見る欄の取り違え」5回
>     excerpt_official→excerpt_status ／ ホストで代用 ／ solve() を真偽で判定 ／
>     game に表示名を渡した ／ master に生の JSON を渡した
> → 次に何か測るときは入れ物を先に数える（この値はどこに書かれ、誰が読むか）
>
> 【運用】
>   git stash の棚はリポジトリで1つ。worktree ごとではない。
>     別セッションの WIP が混ざる（store-name-b が今日踏んだ）
>   MEMORY.md は上限200行。cardbot は全セッションで1つを共有している
>   赤は試験名だけで比べる（FAIL:/ERROR: の接頭辞を含めない）
>   「完走した run」だけでなく「いつの main を見た run か」も見る
>   refs/pull/N/merge は run の時点の main と混ぜ直す
>   数を出すときは 時点・母数・定義の3点
>   見込みは書いてよいが、実測が出るまで相手に渡さない
>
> 【名前の相談】
> CURATION_X_HANDLES が「まとめの口」の意味だが、
> dbfw_cardgameJP のようなゲーム発売元も入る。
> 実際は「店の公式根拠に数えない口」の表。名前を変えるかを検討すること。

## 指示と過去の報告の食い違い

**運用の「数を出すときは 時点・母数・定義の3点」は、9/7 夜の分で既に §10-3 に入っています。**
同じ決まりが2度渡ったので、設計書は変更していません（重複した節は作っていません）。

## 報告

### 出し方（新しい PR にしなかった理由）

**PR #1454 に追加しました**（新しい PR を立てていません）。head **2b3a4eb6**。

- #1454 は同じ `docs/target-architecture.md` の同じ性質の追加で、**まだ承認もマージもされていない**
- 今日の分の一部（赤の比べ方）は、#1454 で新設した §13-2 の続きで、main には無い節を前提にする
- 別 PR を #1454 に積むと、#1454 がマージされても**枝が残っている間は base が main に切り替わらない**
  （2026-09-07 に #1342 で実際に踏んだ型）

**PR の題と本文は、今日の分を含む形に直しました。** 承認は #1454 1本で両日ぶんに掛かります。

### 追加した節（9/8 の分・54 行）

| 指示 | 節 | 中身 |
|---|---|---|
| 入れ物の型 | **§11-1-4（新）** | 6件を表に（書く側／読む側／起きたこと）。決まりは「**測る前に入れ物を数える**——この値はどこに書かれ、誰が読むか」。§11-1 の実装指針を「足すとき」だけでなく「測るとき」にも当てはめる |
| 見込みの扱い | **§10-3 に追記** | 見込みは書いてよい。渡した瞬間に相手の判断の材料になるので、実測に置き換えるか「見込み」と明記して3点を添える |
| 赤の比べ方 | **§13-2 に追記** | 試験名だけで比べる（`FAIL:`／`ERROR:` を含めない・同じ試験が両者を行き来する）／「いつの main を見た run か」／`refs/pull/N/merge` は run 時点の main と混ぜたもの |
| 作業環境 | **§13-3（新）** | `git stash` の棚はリポジトリで1つ（退避は枝を切ってコミット）／`MEMORY.md` は 200 行上限・全セッションで1つ・140 行を目安に畳む |
| 名前の相談 | **§11-2 決まり5** | 表の名前は、その表が実際に効かせている判定の名前にする |

設計書は 1,206 → **1,260 行**（両日で 1,093 → 1,260・追加のみ 167 行）。

### `CURATION_X_HANDLES` の名前 — 調べた結果

**指摘のとおりでした。** 名前は「まとめ（curation）の口」ですが、実際に効かせている判定は
**「店の公式根拠に数えない口」**です。

| 確かめたこと | 実測 |
|---|---|
| 表の在り処 | `candidate_contract.py:516`（14口）。写しが `web/app/lib/candidate-review.mjs:778` |
| 表に無い口はどうなるか | `is_official_source` が **True** を返し、まとめの投稿が「公式告知」として配信の関門（§8-1）を通り得る |
| 発売元が入っているか | **入っている。** #1467 で `dbfw_cardgamejp`・`one_piecetcg`・`dourakulkebuku`（現役）と `torecagetnavi2`（**退役**）を追加 |
| 実害 | #1467 の実測で 0 件（その4口を公式根拠に使っている回 0／唯一の根拠 0／配布行と重なる 0）。**発火していない穴** |

**もう一段あります（この相談の本体だと思います）。**
この表は `candidate_contract.py:720` で同時に **`role = "summary"`（まとめ）** も付けています。
**発売元の投稿は「まとめ」ではありません。** #1428（§0-1 原則1 の変更）で
「**2つ以上のまとめで一致**」を数えるようになると、**発売元の1件がまとめの1つとして数えられます。**
いまは §8-1 が「公式根拠1本以上」だけなので害は出ませんが、#1428 を実装した時点で効きます。

#### 推奨

**2段に分けることを勧めます。**

1. **いま（この PR の範囲外・実装）**: 名前を実態に合わせる。`CURATION_X_HANDLES` →
   **`NOT_STORE_OFFICIAL_X_HANDLES`**（店の公式根拠に数えない口）。表は1つのまま、
   Python と画面の写しを同時に直す（写しがずれた #1467 と同じ穴を作らないため）
2. **#1428 の実装と同時**: この表に**種別**（`roundup` / `publisher` / `retired`）を持たせ、
   `role = "summary"` は `roundup` のときだけ付ける。「2つ以上のまとめ」の母数から
   発売元と退役した口を外す

1 だけでも読み違いは止まります。2 は #1428 が実装に入るまで急ぎません。
**どちらも実装なので、この PR には入れていません。** 進めてよいかの判断をください。

### 確認（マージの条件 1・2）

| 条件 | 実測 |
|---|---|
| PR 固有の赤 0 | GitHub Actions は**起動なし**（`docs/` だけでパス条件に当たらない） |
| Vercel | **SUCCESS** |
| 競合 | #1428 と手元で合流させて**競合0**（再確認済み。触っている節が離れている） |

条件 3（承認の記録）が要ります。承認が出れば自分でマージし、merge commit は**新しいファイル**の
報告で記録して `sync-docs.sh` で写しを更新します。

### 未処理（別の判断）

1. `CURATION_X_HANDLES` の改名と、`role="summary"` を種別で分けるか（上の推奨1・2）
2. §9-4 の扱い（人が1件ずつ決着させる運用を前提にした節を、残すか書き換えるか）
3. 廃止した理由名3つをコードから消す
4. `test_candidate_ai_haiku_first` の実行順依存そのものの修正（store-name-b の領分）

## 根拠データ

なし（設計書の文面の追加と、既存コードの読み取りのみ。裏取りはファイル名と行番号がそのまま根拠）。
