# 本部（M1g）の受付中は 34 社（前回 30 社から +4）／数字の対応を確定・自分の報告の誤りを 1 件訂正

セッション名: metrics
日付: 2026-09-07（JST・21:40 時点）
区切り/依頼名: 表の頭に店の数を置く・数字の対応を先に確定させる・見張りに行を足す
対象: `origin/main` = `a24c1837`（09-07 20:34）

## 本部（M1g）の受付中

**34 社**（54 社中）。前回（`history/passrate/m1g/2026-09-07.json`・06:16 の枠）は **30 社**なので **+4 社**。09-06 の枠は 32 社。

受付中の回数は本部だけで **122 回**。支店を含めると 37 社・464 回。全体の受付中は 1,503 回なので、**本部の回は全体の 8%**。

内訳（本部・受付中の回数）: 楽天ブックス 20・KIDS REPUBLIC 11・ポケモンセンター(実店舗) 10・ポケモンセンターオンライン 8・イエローサブマリン 7・ドラゴンスター 6・GEO 6・BIC CAMERA 5・GANGI 4・古本市場 4・YAMADA 3・ドン・キホーテ 3・フルコンプ 3・セブンネット 3・HOBBYSTATION 3・ほか 19 社が 1〜2。

**受付中が 0 の本部は 20 社。** ここが目標との差。

## 受けた指示（原文）

> 【1. 表の頭に店の数を置く】…目標は本部（M1g 54社）の受付中の店数。毎日の判定の頭に置くこと。
> 【2. 明日の2段目の前提が今夜に限って崩れる】…「227件のうち V5 に送られた件数」を直接見ること。
> 【3. 数字の対応を先に確定させる】a. コホートと引き継ぎ書の対応 b.「全2,748中 送れる869」が #1430 と合わない
> 【4. 見張りに足す行】本部の受付中の店数／227件のうち V5 に送られた件数／受付中のうち本部は何回か
> 【5. 監視の棚卸し】残り2本の監視が何を見ていて、いつ期限が切れるか

## 指示と過去の報告の食い違い

**自分の報告に誤りが 1 件ありました**（下の 3-a-3）。引き継ぎ書の「配布 4（1.5%）」は、対応表（`shadow_round_links`）の読みに失敗して店×商品で代用した値です。対応表で数えると **0** です。

## 報告

### 1. 数字の対応（指示 3-a）

| 数 | コホート（9/7・21:30 時点） | 引き継ぎ書（30 時間の窓・18:10 時点） | 対応 |
|---|---|---|---|
| new | 310 | 270 | **窓が違う**（9/7 の 1 日 ／ 9/6 12:00〜9/7 18:00）。**測った時刻も違う**（台帳は 2.5 時間で 180→310 と伸びた） |
| judged | 12 | 25 | **同じ段**（V5 の結果がある回）。母集団が違うだけ。いま同じ窓で数え直すと 29 |
| 配布 | **0** | 4 | **同じでない。** 下の 3-a-3 |

同じ 30 時間の窓を、いま同じ定義で数え直すと `new 381 / judged 29 / end_confirmed 14 / verdict 10 / kept 5 / 配布 0`。

**3-a-3（訂正）**: 引き継ぎ書の `in_lotteries` は、私の集計スクリプトが `shadow_round_links` の形（`{round_id: candidate_round_id}`）を読めず、**店×商品での代用**に落ちていました（`links_loaded: 0`）。コホートの日次（#1421）は対応表で数えるので **0** です。**一致していたのは偶然ではなく、別の数え方の値を並べたためです。**

**これ自体が今日の発見**: 刻印は `kept 5` あるのに、対応表で配布行に結び付く回が **0**。刻印が付いた回が配布行になっていない（V7 の結び付けか、配布行の作り直しで落ちたか）。ci・store-name-a の領分なので、数だけ出します。

### 2. 抜粋の関門を取り直した（指示 3-b）

**#1430 の後で大きく動きました。** 単位も母数も同じ（全応募回）で、変わったのは中身です。

| | 19:00（#1430 の前・2,748 回） | 21:30（#1430 の後・2,910 回） |
|---|---|---|
| V4/sendable ok | 869 | **1,602** |
| not_official | 1,076 | **327** |
| no_excerpt | 801 | 963 |
| stale | 0 | 15 |

**「not_official 1,076 は設計どおり」という私の読みは、749 回ぶん間違っていました。** その 749 回は公式由来なのに、抜粋と本文の突き合わせが空白のずれで落ちて `not_official` に倒れていた回です（#1430 が直した）。設計どおりに送らない回は **327** が本当の数です。

`#1430` の「送れる回 354 → 674」とは母数が違います。あちらは `plan()` が選ぶ集合（人未確認・AI が古い・締切が未来…で絞った後）、こちらは**全応募回**。両方正しく、比べる相手ではありません。

`no_excerpt` が 801 → 963 に増えたのは台帳が 162 回増えたため。**直す相手は変わらずここ**です。

### 3. 見張りに足した行（指示 4）

明日から毎日この 3 つを頭に置きます。

1. **本部（M1g）の受付中の店数**（54 社中・前回比）。M1g の日次（`history/passrate/m1g/<日付>.json`）の `by_chain` から数えます。
2. **受付中のうち本部の回数**（支店を除く）。今日は 1,503 回中 122 回。
3. **227 件のうち V5 に送られた件数**。今夜の応募回 id **2,910 本を起点として保存しました**（`baseline-rounds-2026-09-07.json`）。明日、台帳に増えた id を「起点に無い回」として取り出し、`first_seen` が 9/7 以前のもの（＝滞留分）と今日の新規に分け、そのうち V5 の結果がある数を出します。

**明日の切り分けの注意（指示 2 への補足）**: コホートの日次は `first_seen` の日で束ねます。227 件が**古い `first_seen` のまま入れば、今日の `new` は跳ねず、過去の日の行が太ります**。どちらでも応募回の総数（いま 2,910）で入ったことは分かるので、判定の 1 段目は「総数が増えたか」で見ます。

### 4. 監視の棚卸し（指示 5）

| 監視 | 何を見ているか | 期限 |
|---|---|---|
| `bi6cj940g` | health-watch の run の Check passrate の出力（判定が鳴った内容）。09-06 11:39 から常駐 | 期限なし（セッションが続く限り） |
| `bggbklc38` | 今夜の update-data で `history/passrate/funnel/<日付>.json` が main に載るか、次の candidate-ai-review で `V4/sendable` の行が入るか | **09-07 22:10 ごろ**（1 時間） |

`bggbklc38` は 22:00 の update-data の直後に切れます。**切れたら張り直します**。切れたまま朝を迎える形にはしません。

### 5. CI の run の有無（レビュー担当【1】）

今日マージした 7 本は、すべて `test` の run が実在することを確認してからマージしています（run id を控えてあります: 34014679817・34024399000・34044862994・34045468434・34082179756・34086357408・34107802621・34112283898）。**今後も「緑」ではなく「run があって緑」を見ます。**

### 6. passrate.EXPECTED の所有（レビュー担当【3】）

引き受けます。いま main の `EXPECTED` は #1429（`V4/excerpt`・`V4/sendable`）と S2b・S6・`D1/roundup`・`D1/signal` が入った形で整合しています（試験 125 本 OK）。ただし**期待表にあって行が無い部品が 2 つ**あり、判定②が鳴っています。

- `ec-lottery-watch: S2b の行がありません` — 書き手（`official_site_watch.py`）は入っていて、11:34 の run は `--probe` で走っていました。`--apply` の回が来れば行が出ます。
- `roundup-daily: D1/signal の行がありません` — 書き手（`roundup_seen.py`）は入っていますが、09:58 の run では呼ばれていません。

どちらも「配線の欠落」として正しく鳴っています。所有者として、次の run で行が出るかを見て、出なければ書き手の側（ci・roundup）へ渡します。**今後、期待表への追加は metrics 経由でお願いします。**

## 状態

報告のみ（数字の確定と訂正・見張りの行を追加）。明日の 1 段目は「応募回の総数が 2,910 から増えたか」。
