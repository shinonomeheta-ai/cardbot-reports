# 別名の防御を3方向へ緩める（PR #1309）

セッション名: store-name-a
日付: 2026-09-05（JST）
区切り/依頼名: 別名の防御の緩和
対象: PR head（base `9d1befbd`）

## 受けた指示（原文）

> ## 2. 別名の防御: 3案とも入れてください。ただし試験で固定
> - `【…】` を `（…）` と同じ扱いにする
> - 末尾の肩書きを落としてから判定する
> - ブランドのラテン↔カタカナ表を持つ
>
> 条件: `test_支店の見分けは緩めていない` に、緩めた分だけ「何を通して何を止めるか」を
> 固定する。特に「TSUTAYA調布店 ← GEO調布店」のような別チェーンが
> 引き続き止まることを試験に残すこと。
>
> ## ヒアドキュメントの罠、2度目
> docs/renaming-procedure.md か docs/assumptions.md に
> 「パッチはファイルに書いてから実行する」を既知の罠として残してください。

## 指示と過去の報告の食い違い

なし。

## 1. PR #1308 をマージしました

`9d1befbd`。CI の赤は main（`10b77d0e`）と `diff` して完全一致（3件）。

**main の push で python test が走らなくなる前**だったので、
main の直近の run をそのまま基準に使えました。次からは
`gh workflow run python-test.yml --ref main` で取ります。

## 2. 緩め方（PR #1309）

ご指示の3案とも入れました。実装は
`test_data_regression.StoreAliasTests._同じ支店の書き分け()` の前処理です。

```
① 括弧を落としてから比べる       「」『』【】（）()
     → tenant_key（#1308）と同じ扱いにそろえた
② 末尾の肩書きを落としてから比べる  トレカコーナー／トレカ館／トレカ部／トレカ／
                              カード部門／TCG／カード／公式
③ ブランドのラテン↔カタカナを寄せてから比べる
     bookoff↔ブックオフ／cardbox↔カードボックス／tsutaya↔蔦屋／
     geo↔ゲオ／mint↔ミント
```

**語（通販・各店）は落としていません。** 落とすと [P1]（2026-08-16）の線を
越えます。

③のブランド表は**実データに両方の綴りが出るものだけ**にしました。
増やすときは両方が実データにあることを確かめてから、とコメントに書いています。

## 3. 固定した試験（ご指示の条件）

### 何を通すか

```
test_緩めた3方向は通す
   ザ・グレートヨロズヤ盛岡高松店 ← …盛岡高松店【トレカ】   ①
   萬屋 盛岡店 ← 萬屋盛岡店 トレカコーナー                ②
   ミント梅田店 ← ミント梅田店 TCG                       ②
   BOOKOFFお宝大陸和泉中央店 ← ブックオフお宝大陸和泉中央店  ③
```

### 何を止めるか

```
test_緩めても止めるものは止める
   TSUTAYA調布店 / GEO調布店              別チェーンの同じ地名（ご指示の条件）
   BOOKOFF和泉中央店 / ブックオフ川崎モアーズ店  ブランドを寄せても支店が違う
   萬屋 盛岡店 / 萬屋紫波店 トレカ           肩書きを落としても支店が違う
   ドラゴンスター通販店 / ドラゴンスター各店      **語は落とさない**

test_飾りを落としても語は残る   前処理そのものを留める
   落("…盛岡高松店【トレカ】") == "ザ・グレートヨロズヤ盛岡高松店"
   落("萬屋盛岡店 トレカコーナー") == "萬屋盛岡店"
   "通販" が残る／"各店" が残る
```

既存の `test_支店の見分けは緩めていない`（`TSUTAYA調布店` ← `GEO調布店` ほか4件）は
**そのまま残しています**。緩めた前後で、この4件は通りません。

## 4. docs（ご依頼）

`docs/renaming-procedure.md` に「直すときの道具（既知の罠）」を足しました。

```
- パッチを bash のヒアドキュメントへ直接書かない
     この環境ではヒアドキュメントが `\\` を1本に潰す。
     `\t` が本物のタブになり JS が構文エラーになった。
     2026-09-03 と 2026-09-05 に踏んだ
- JSON を書き戻すときは改行と並びを変えない（store_aliases.json は CRLF）
- git diff --stat の行数を見てから commit する
     3行足したのに455行の差分が出たら、道具のほうが壊れている
- git checkout HEAD -- <file> は、コミット前だと変更を失う
- 共有 worktree では git add -A を使わない
```

置き場は `docs/renaming-procedure.md` にしました（`assumptions.md` ではなく）。
**素性を直す作業のときに毎回踏む罠**なので、手順書と同じ場所にあるほうが
見つかると考えました。

## 5. 試験

```
python -m unittest test_data_regression   Ran 43 tests  OK
```

**このPRは防御だけ**です。別名3件は次のPRで足します
（ご指示の「防御の変更は防御だけで見たい」に従いました）。

## 6. 判断していただきたいこと

1. **PR #1309 をマージするか。**
2. ③のブランド表は5組だけです。ほかにも実データで両方の綴りが出るブランドが
   あれば足しますが、**いまは実測した5組だけ**にしています。
3. マージ後、別名3件のPRを出します。

## 状態

- PR #1308 は**マージ済み**（`9d1befbd`）。
- **PR #1309 を出しました。マージはしていません**（指示待ち）。
- 別名3件は**まだ足していません**。
- 外部への通信は行っていません。課金もありません。
