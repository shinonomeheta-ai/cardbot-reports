# #1341 のマージと、巡回1回のあとの4点確認

セッション名: store-name-a
日付: 2026-09-06（JST）
区切り/依頼名: 案1 のマージと確認
対象: `origin/main` = `5631e1a1`（巡回 run 34008679121 のあと）

## 受けた指示（原文）

> #1341 をマージしてください（gh pr merge が打てるなら自分で）。
> マージ後、巡回を1回回して renaming-procedure の4項目を確認してください。
> 特に lot_key が動いた行（エディオンの2行・締切済み）が旧鍵でも読めることを。
> りらいは 9/15 の後に。待機で結構です。

## 指示と過去の報告の食い違い

**1点あります。私の前便の書き方が誤解を招きました。訂正します。**

前便で「**旧 lot_key では読めません**」と書きましたが、これは
**「行の店名が書き換わったら」という前提の話**でした。実測すると、
**この時点ではまだ書き換わっていません**（下記 §3）。訂正の中身は §3 に書きます。

## 1. マージ

```
merge commit  c54a14e9
```

## 2. 巡回は自動の枠が回りました（私からは起動していません）

```
run 34008679121   nyuka-watch（watch レーン）
   起動 2026-09-06 12:18 JST ／ 対象コミット c54a14e9（＝#1341 のマージ）
   結果 success ／ main は 5631e1a1 へ
```

巡回はデータを書き、AI経路も持つので、自動の枠が回るのを待ちました。
11:46 の次が 12:18 で、20分の枠が1つ落ちた形です（`nyuka-watch.yml` の
コメントにあるとおり、GitHub の schedule は混雑時に破棄されます）。

## 3. 4点確認 ＋ ご指定の⑤

```
① candidate_event_id が動いた候補     0   （対象候補49件とも当たり先は自分）
② identity_keys が欠けている候補      0
③ 食い違う刻印                     0
④ 人の判定 283 のうち引けない         5   （main と同値。母数は 271 → 283 に増えた）
⑤ 巡回前の7行のうち event_id か lot_key が動いた行   0
   店名が消えた行                                0
```

**①② は意味のある確認です。** この巡回は `shadow_candidates.json` を
3,199行ぶん書き換えているので、候補台帳は作り直されています。

### ⑤ について — いまは動いていません。ただし「まだ」です

**この巡回は `web/public/data/lotteries.json` を触っていません**（書き換えたのは
候補台帳と影の測定・保留・判定表）。配布行が作り直されていないので、
`エディオン/トレカキャピタル` の2行は名前も `event_id` も `lot_key` もそのままです。

```
エディオン/トレカキャピタル  30th                      evt_afff2841753b1b88440753af   前後で同じ
エディオン/トレカキャピタル  拡張パック「30th CELEBRATION」  evt_f4d921030630321695a0a167   前後で同じ
旧 lot_key「エディオン/トレカキャピタル|pokemon|2026-08-30|30th」→ いまも当たる行が1件
```

**配布を作り直す回で動きます。** `build_lotteries_nyuka.merge_into` は

```python
for r in existing:                       # 既にある行にも当てる
    r["store"] = canonical_store(r["store"])
```

を通るので、次に `lotteries.json` を作り直す回で `エディオン` へ寄ります。
そのとき `lot_key` が動きます。

### そして、動いたときは旧鍵では読めません

```
isMarked(set, l) → allKeysOf(l) = [lotKey, legacyLotKey, noGameLotKey, preRosterLotKey]
   4つとも**その行のいまの店名**から作る

materializeLegacy() が移せるのは「同じ行の古い形」3通りだけ
   （商品なし／ゲーム空／その両方）。**店名が変わった場合の移し替えは無い**
applied-key-migration.mjs の OLD_TO_NEW も、店名の引っ越しは持っていない
```

**救いは締切が 2026-08-30 で終わっていること**です。終わった回の印なので実害は
小さいと見ていますが、「旧鍵でも読める」とは書けません。**これはご判断（りらいは
9/15 の後）の裏付けでもあります**——りらいは受付中なので、同じことをすると
**いま応募中の人の印が外れます**。

## 4. まとめ

```
①〜④   すべて問題なし（③④は main と同値）
⑤      いまは動いていない。配布を作り直す回で動く
        動いたときは旧鍵では読めない（読み替えの仕組みが無い）
        ただし締切済みなので実害は小さい
```

## 5. 持ち越し

```
2026-09-16 以降   りらい(福島) の別名を足す
                  （"カードゲームショップ りらい（福島）" ← "りらい(福島)"）
                  **受付中の回で lot_key を動かすと印が外れる**ので、締切後に
別の区切り        案3・案4（ロスターの別名23本・②の部分一致）を合流の設計として
                  設計書 §11-1-3 が入口
見張り            配布を作り直す回のあと、エディオン2行の lot_key が
                  実際に動いたかを1度だけ確かめる（動いても締切済み）
```

## 状態

- #1341 は**マージ済み**（`c54a14e9`）。
- 巡回は**自動の枠**が回りました（私からは起動していません）。
- **調査・確認のみ。追加の変更はしていません。** PR も出していません。
- りらいは**触っていません**（9/15 の後）。
- 外部への通信はありません。課金もありません。

引き続き待機します。
