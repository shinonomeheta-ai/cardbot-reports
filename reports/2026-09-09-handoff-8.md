# 引き継ぎ書（更新8）— 単価2.5倍の正体は「Haiku → Sonnet」。判断が要る（2026-09-09）

宛先: 次のセッション
前任: Claude（セッション ci・2026-09-08 20:40 〜）

**前版**: [更新7](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/2026-09-09-handoff-7.md)

---

## 0. A の答え — 単価が上がった原因は**モデルの切り替え**だった

```
claude-haiku-4-5    791回  $19.99   1回 $0.0253
claude-sonnet-5   1,479回  $127.70  1回 $0.0863   ← **3.4倍**

08-26〜08-27  Haiku
08-31 から    Sonnet（PR #1097 / #1116 / #1119）
```

日ごとの単価:

```
08-26〜08-27  $0.025 /回   Haiku
08-31         $0.063 /回   Sonnet へ切替（同日に混在）
09-04 以降    $0.099 /回   Sonnet のみ・横ばい
```

### 途中で外した見立て（記録）

「プロンプトが伸びたせい」と見立てて `candidate_ai_prompt.py` を測った
（44,765 → 55,561 bytes・+24%）。しかし**実際に送る `user_prompt()` は
6,377 → 6,254字でむしろ縮んでいた**。ファイル全体の伸びは説明文だった。
**モデル別に割るまで原因が見えなかった。**

送るプロンプトの実測（いま・300件）: 中央 3,641字 / 最小 3,435 / 最大 5,159。
**ばらつきが小さく、大半が固定部分。** 材料（抜粋・中央175字）は費用をほとんど動かさない。

---

## 1. ★ 判断が要ること（本人へ）

**A は「技術的に単価を下げる」話ではなく、「モデルをどちらにするか」の判断だった。**

| | 1回 | 1日20件の月額 | 同じ予算での件数 |
| --- | ---: | ---: | ---: |
| **Haiku** | $0.025 | 約 $15（¥2,250） | **1日68件相当** |
| **Sonnet（いま）** | $0.086 | 約 $52（¥7,800） | 1日20件 |

**Haiku に戻せば、同じ予算で 3.4倍聞ける。** 送れる回1,334件・締切が未来171件
なので、件数が3.4倍になれば**順番待ち（`ai_not_run` 294件）はほぼ解消する**。

### ただし質を比べた記録が無い

```
shadow_candidate_ai_ab.json   31件。比べているのは **grok と haiku**
                              **Sonnet は入っていない**
```

**Sonnet へ切り替えた判断が質の比較に基づいていたのか、台帳からは分からない。**
`sonnet-check` / `sonnet-partial` の PR を読めば意図が分かるかもしれないが、
**まだ読んでいない。**

### 提案（判断待ち）

1. **まず PR #1097 / #1116 / #1119 を読む**（なぜ Sonnet にしたのか）
2. 理由が「質が足りなかった」なら、**同じ回を両方に聞いて突き合わせる**
   （それ自体に課金がかかる。20件×2モデルで約 $2.2）
3. 理由が「画像を読ませたかった」なら、**画像がある回だけ Sonnet**という
   使い分けができるかもしれない

---

## 2. 今日マージしたもの（本業）

**#1497 — AI へ聞く並びを締切の近い順に**

```
                いまの並び      締切順
上位 20件      生きた回 0件  →  20件
上位 40件            2件  →  40件
上位100件           13件  →  100件
```

**今日の20件は生きた回が0件。** 全部、締切切れに $1.93 払っていた。
**費用は1円も変わらない。**

段は3つ（`check_candidates_ai.並びの鍵()` の1か所）: 締切が未来 → **暫定回** →
締切切れ。暫定回を切れより先にするのは §5-1 の経路を止めないため。

### **次のセッションが最初にやること: 効果を測る**

次の AI 巡回（06:40 / 12:40 / 21:40 JST）のあとに:

```
① 送った20件が「締切が未来」だったか（前は0件）
② 答えが返ったか（conflict / insufficient_evidence が多くないか）
③ 画面の社数が動いたか
```

**前の集合（2026-09-09 11:10 JST）**:

```
ゴールデン台帳 55社 → 候補台帳 50社 → 配布行 27社・66行 → 受付中 5社・6行
```

---

## 3. 本業の詰まり（確定値・本番と同じ引数）

```
応募回 3,094件                       全部    うち締切が未来
  ai_not_run              1,522件      294件   ★ 最大の詰まり
  ended_or_no_end         1,044件        0件
  OK                        214件      209件
  missing_identity          172件       31件
  not_lottery                98件        0件
  ai_error                   38件       18件
```

「抜粋が無い」146件は**掘った結果、直せるものが無かった**:

```
まとめの本文だけ 45 ／ 本文が短い 51 ／ JS必須 19 ／ 要確認 16 ／ その他 15
```

Google Forms（18件）は **HTTP 200 で「JSを有効に」が返る**ので再取得も描画も
走らない。**実際に Chrome で描画しても本文264字（reCAPTCHA と利用規約だけ）**で、
質問項目は出てこない。**やるなら別の手段が要る。**

---

## 4. ★8社は「持っているのに出せていない」ではなかった

抜粋を読んだら、**半分以上がトレカの抽選ではなかった**（YAMADA=防災バッグ、
ドンキ=キャリーケースの感想、セブンネット=映画ポスター、BIC CAMERA=カメラ）。

本物は **イエローサブマリンと竜星のPAOの2社だけ**。

```
イエローサブマリン  材料は揃っている。AIの答えが conflict
竜星のPAO          公式 pao.membercard.jp が **http_403**。まとめの本文しか無い
```

**`missing_identity` で止まるのは関門が正しく働いている証拠。**

---

## 5. 未マージのブランチ 1本

```
fix/classify-red3   試験は全部緑（8,141件 OK・今日はじめて）
                    しかし step「テストがファイルを汚していないか」で赤
                      ?? history/passrate/2026-08-22/
                      ?? history/passrate/2026-09-09/python-test-<run_id>.jsonl
```

**今まで `unittest` が先に落ちていたので、この step まで到達していなかった。**
私の3試験は汚さないことを確認済み。**犯人は未特定。特定するまでマージしない。**

---

## 6. CI の型分け（目的達成）

```
赤 8 → 2 ／ 飛ばし 19 → 66 ／ 移行中 6件 ／ データ契約 86件緑
A 本当のバグ 0件 ／ B 実データ 6件 ／ C 外の状態 3件（全部片付いた）
```

**CI ログから試験名を数えないこと。** 今日3回、別々の理由で間違えた。
信用できるのは `failures + errors` の数だけ。

---

## 7. 触ると危ないところ

* **`lottery_io.save()`** — 書き出す直前が `round_id` の唯一の確定点
* **データ契約** — 赤いものを入れると巡回が止まる
* **`load_tests` の配線** — 忘れると緑のまま何も見ていない
* **AI の件数・並び・モデル** — 1件あたり課金。**本人の承認が要る**
* **`event_id` 台帳** — `test_lottery_overrides` のデータ実害（5行が管理画面で409）

---

## 8. 本人にしかできないこと

管理画面 https://torecanavi.app/admin の**手動補正**タブ。

| 店 | いま | 直す | 締切 |
| --- | --- | --- | --- |
| **ブックオフプラス新宿駅西口** | `BRIGHTNESS` | `BRIGHTNESS OF HOPE` | **2026-09-09（当日）** |
| MINT GAMES池袋店 | `30th` | `30th CELEBRATION` | 2026-09-13 |

---

## 9. 前任が今日踏んだ誤り（17件）

推測で場所を2回外した／古い基準で CI を4回比べた／`round_id` 非空を関門にと言った
／直読み4本と言った／`url` を主キーにと言った／「本当のバグ3件」（全部違った）／
CI の名前の数え方を3回間違えた／印スクリプトの判定が甘く1件飛ばした／空の台帳の
鍵の名前を間違えた／`GI.チェーン()` の呼び方を間違えて「0社」／`rounds_of` に
`sc["candidates"]` を渡して「送れる回0件」／`AI昇格できるか` に `date` を渡して
「1,066件が型で落ちる」と誤報／★8社を中身を見ずに数えた／「116件→100件→19件」と
掘るたびに数が減った／「Chrome 描画で取れる」も外れた／**「単価はプロンプトの伸び」
と見立てたが、実際は `user_prompt()` は縮んでいた（モデル切替が原因）**／
`user_prompt()` に材料を渡す形を間違えて全部同じ長さと出した／heredoc の罠2回。

**共通する型: 実物を確かめる前に、名前や見た目で判断して数を出した。**

**効いた判断はどれも「先に測る道具を作った」もの。**

```
計測器（#1490）           推測2回で外した場所を、巡回1回で特定
突き合わせ（#1492）       切替前に 311件の事故を止めた
立て忘れの見張り（#1494） 「緑のまま何も見ていない」を防ぐ
移行中の一覧（#1495）     「症状は消えたが巡回が止まる」を防ぐ
締切順の実測（#1497）     直す前に 0件→20件 を測ってから実装した
Chrome で1件試した        実装前に「効かない」と分かった
モデル別に割った          プロンプトを疑っていたが、割ったら一発で見えた
```
