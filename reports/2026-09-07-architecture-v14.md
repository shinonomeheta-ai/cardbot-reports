# 設計図 v14 と、節の無かった4部品（PR #1417・セッション design）

日付: 2026-09-07（JST）
区切り/依頼名: 設計図・設計書の版そろえ
対象: `origin/main` = `569c6729` から分岐（`docs/architecture-v14`）。#1373 のコミットを含む

関連: PR [#1417](https://github.com/shinonomeheta-ai/cardbot/pull/1417)（本体・private・**レビュー待ち**）／PR #1373（このPRに取り込んだので、マージ後は閉じてよい）

## 受けた指示（原文）

> 今できることありますか？

指示が範囲を指していないため、**私の担当（09-04 の収集機構 部品設計書 v14）の続きで、
他セッションと重ならないもの**を選んだ。着手前に見たものと、選ばなかったものは下に書く。

## 着手前に見たもの

| 見たもの | 結果 |
| --- | --- |
| `main` の CI | **赤が続いている**（09-07 06:29 の run 34091076008・failures=2 / errors=2）。中身は「同じ商品が2件並んでいます」×2・`evt_…#rnd_…` のリスト不一致・「控えの数が成果物の行と合っていない（308≠310）」 |
| 開いている PR | 11本。うち今日ぶんが5本。**#1373 が 09-06 10:33 から動いていない**（MERGEABLE） |
| 報告の INDEX | 判断待ちが5件（`evidence_cache_missing 635`／V5 の残り219回／LivePocket を Vercel から読む／#1408 と 3回×1,360／同一URLで2行） |
| 設計書の記号表 | 「（本書に節なし）」が4部品（S4・M3・M4・L2）。表の前書きに「本書の節は追って足す」と書かれていた |
| 設計書を触る PR | **0本**（#1412〜#1416・#1399・#1327 のいずれも `docs/target-architecture.md` を触っていない）。衝突しないことを確かめてから書いた |

**選ばなかったもの**: 判断待ち5件は本人の判断が要るので動かさない。CI の赤は #1414 が
1つ目の型を直そうとしており、ci セッションが見ているので触らない。

## やったこと

### 1. 図を v13 → v14（#1373 を取り込んで追いつかせた）

#1373 は「図の元を HTML でリポジトリへ残す」PR で、22時間止まっていた。その間に
設計書へ入った次の4つが図に無かった。

| 足したもの | 中身 |
| --- | --- |
| **S6 共有基盤の新着検知** | 主催者ページを毎日読む・法人名が一致した店だけ登録・課金0（§1-6）。収集の母線へつないだ |
| **S2b に Wayback** | §1-2c。3〜5日遅れるので「取りこぼしを後から記録する」経路で、締切には間に合わない |
| **L に暫定回/正式回** | 鍵は締切から作るので**書き足せない**。締切が分かったら正式な回を起こす（§5-1） |
| **D1 と P に Amazon の例外** | 使ってよいのは受付中かどうかの状態だけ。通した行は応募先URLを空にしてから配る（§0-1 原則1・§8-1） |

箱34・記号31・線39（1560×2818）。図の元は `docs/target-architecture-v14.html`、
描き直しは `node --experimental-websocket tools/shot_architecture.mjs`。撮り直して目視した。

### 2. 「本書に節なし」だった4部品に節を書いた

記号表だけが指していて本文が無い部品は、**指示も報告も噛み合わない**。09-04 の
部品設計書 v14 で定義した内容を本書へ移し、実装の名前で裏を取った。

- **§1-5 S4 外部フィード** — 新着の検知であって**値の根拠にしない**。入荷なうは §1-4 のまとめに含まれるため、根拠がまとめしか無い回は §8-1 の関門で落ちる
- **§3-4 M3 共有基盤の持ち主登録** — 完全一致のURL × 店舗 × evidence × confirmed だけ通す（`resolve_urls.shared_platform_unconfirmed()`）／引く鍵は配る側と同じ `date_source.tenant_key()`（N2）／URL も揃えてから引く（末尾の `#` 1文字で通らなかった 2026-08-16 の実測）／行ごとの例外は `shared_platform_blocked()` の中だけ／表は `store_platforms.json` の `stores`（`promoters` は S6 の欄で別物）
- **§3-5 M4 まとめ一覧** — 正本は `source_names.json` の hosts 1本。写しを作らない
- **§5-2 L2 同一性の鍵と発番** — ①URLで寄せない ②2つ以上当たったら寄せない ③走査を限る ④行が持つURLを判断に使わない ⑤配布行を手で消さない。根拠は 09-04 実測の別支店URL吸収7件

記号表の参照を実節に直した。**「本書に節なし」は0件**になった。

## 確かめたこと

- 設計書を読む試験4本が緑: `test_passrate` / `test_passrate_landed` / `test_passrate_stage2` / `test_livepocket_watch`（計77件）
- 図は実際に撮って目視した（`docs/target-architecture-v14.png`）
- **データも実装も変えていない。** 触ったのは `docs/` と `tools/` だけ

## 未達・持ち越し

1. **#1417 は CI とレビュー待ち。** `main` が赤なので、PR の赤は `main` との差分で見る必要がある
2. **写しの更新は #1417 のマージ後。** `sync-docs.sh` の写し対象は `docs/target-architecture-v12.png` のままなので、**v14 へ差し替えが要る**（いま走らせても origin/main に v14 の png が無く失敗する）
3. #1373 は #1417 に含めたので、マージ後に閉じる

## 判断をお願いしたいこと

1. **#1417 の内容（図 v14 と4部品の節）でよいか。** 特に §1-5 の「S4 は値の根拠にしない」は、
   図と設計書で初めて明示した書き方になる（実装の関門はすでにそう動いている）
2. 次に何をやるか。手元の候補は「main の赤4件の切り分け」「判断待ち5件を1枚に整理」
