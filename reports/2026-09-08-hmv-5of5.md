# HMV が本番の経路で 0/5 → 5/5 になった／#1440・#1432 の判定（#1432 に本物の赤が1件）

**画面 6社（実時刻・10行）／上流 34社**

セッション名: api-customer
日付: 2026-09-08（JST）
区切り/依頼名: ④ HMV の再測定、⑤ #1440・#1432 の判定
対象: `origin/main` = `6b93af8c`（#1450 マージ後）

## 受けた指示（原文・要点）

> ④ #1450 が入ったら HMV を本番の経路で測り直す（0/5 → 5/5 になるか）。
> ⑤ #1440（429 の診断）と #1432 のマージ状況を報告すること。
> 【運用】「いつの main を見た run か」も見ること。

## 結論

1. **HMV は 5/5 になりました。** 本番と同じ呼び方で、置換文字 0・抽選判定 ○・抜粋 `ok`。#1450 が効いています。
2. **#1440 に PR 固有の赤はありません。** 残っていた1件は土台のデータ違いでした。
3. **#1432 に本物の赤が1件ありました。** この PR が4社を落とした分で、試験が正しく鳴っていました。**直して押しました。**

## 1. HMV — 0/5 → 5/5

`ec_lottery_watch.py:162` と同じ呼び方（`OP.読む(u, pages, 刻, sleep, refetch=False, render=False)`）です。

| 記事 | 前（#1450 前） | 後（`6b93af8c`） |
| --- | --- | --- |
| 260714121 ストームエメラルダ | 7,987字 置換4,381 抽選=False 抜粋 `excerpt_missing` | **4,144字 置換0 抽選=True 抜粋 `ok`** |
| 260501122 アビスアイ | 7,987字 置換4,366 False | **4,103字 置換0 True `ok`** |
| 240111103 ワイルドフォース／サイバージャッジ | 7,986字 置換4,383 False | **3,906字 置換0 True `ok`** |
| 231003112 古代の咆哮／未来の一閃 | 7,984字 置換4,327 False | **3,736字 置換0 True `ok`** |
| 250225101 熱風のアリーナ | 7,986字 置換4,398 False | **4,007字 置換0 True `ok`** |

**本番の経路で 0/5 → 5/5。** 題名も読めます。

```
ポケモンカードゲーム MEGA 拡張パック「ストームエメラルダ」HMV店頭 抽選販売受付
```

**社数は増えません。** 取れた5件はすべて締切済みの過去の回で、いま受付中の回は HMV の一覧にも検索にもありません。次に HMV が抽選を出したときに増えます。**経路が通ったことの確認です。**

## 2. #1440 — PR 固有の赤なし

新しい基準（main `acee7b40`）と突き合わせると `test_公開データの応募URLが減らない` が1件だけ残りますが、**PR のせいではありません**。

土台を揃えて手元で測りました。

```
97822a12（土台・PR のコードなし）          OK
4c218fe4（#1440 の枝・土台込み）           OK
f6a6d267（#1432 の枝・土台込み）           OK
```

**3つとも緑です。** CI が赤くなるのは、GitHub の `refs/pull/N/merge` が run の時点の main と混ぜ直すためで、その瞬間の配布データ次第で結果が変わります。この試験は巡回のたびに赤／緑が入れ替わります。

ご指示の運用（「いつの main を見た run か」）に沿って、土台を揃えて判定しました。**#1440 はマージできます。429 の再分類の前提は整っています。**

## 3. #1432 — 本物の赤が1件・直しました

基準に無い赤が4件に見えましたが、**3件は突き合わせの癖**でした。基準では `ERROR:`、PR では `FAIL:` で、接頭辞が違うだけの同じ試験を別物として数えていました。

**本当に新しいのは1件です。**

```
test_本物の台帳で48社   →   44 != 48
```

**この PR のせいで、正しく鳴っています。** BIC CAMERA・Joshin・あみあみ・しまむらパークの4社に `site_blocked` を付けて `official_site` を外したので、`official_site_watch.対象のチェーン()` から落ちます。**減ったこと自体が狙い**です。

この試験は「数が動いたら気づけるように固定する」ためのものなので、数を 44 へ動かして理由を書きました（`be60ee2a`）。土台では 48 で緑、この枝では 44 で緑です。

## 4. 突き合わせの癖について（自分への訂正）

赤の名前を `FAIL:` `ERROR:` の接頭辞ごと比べていたので、**同じ試験が状態を変えただけで「新しい赤」に見えていました**。3件がそれでした。今後は試験名だけで突き合わせます。

## 残り

- **#1440 はマージ可**（判断をお願いします）。
- **#1432 は新しい run 待ち**（`be60ee2a` を押したところ）。
- #1459（closed）・#1461（イオン九州）の判断。
- #1447 は保留のまま。

## 実行したこと・していないこと

- 実行: HMV の再測定（5/5）、#1440・#1432 の土台を揃えた判定、#1432 の本物の赤の修正と push、突き合わせの癖の訂正。
- していない: #1440・#1432・#1459・#1461・#1447 のマージ。**費用 0 円**。
