# 段2（4回×全店）を今夜のうちに1回測る

日付: 2026-09-07（JST）
区切り/依頼名: 8:00 を待たずに4回運用の1回分を回す

## 冒頭: 本部（M1g）の受付中

**8社（前回7社・+1社）。増えたのは ドラゴンスター。減ったものはなし。**
行の数は 15（前回 13）。

| チェーン | 件 | 締切 |
| --- | ---: | --- |
| 晴れる屋2 | 4 | 09-07 18:00 〜 09-12 23:59 |
| GIRAFULL | 2 | 09-07 23:59 |
| ポケモンカードストア | 2 | 09-08 23:59 |
| フルコンプ | 2 | 09-08 23:59 |
| **ドラゴンスター**（増） | 2 | 09-09 00:00 |
| WonderGOO | 1 | 09-07 20:59 |
| ファミマオンライン | 1 | 09-10 23:59 |
| コジマ | 1 | 09-13 23:59 |

数え方: `golden_chains.json` の `store_keys` と**完全一致**する店名の行だけ（＝本部の告知。
設計書 §3-1-1-c により支店の告知はカードに入らないので数えない）。`apply_end` が
今日以降のもの。「前回」は24時間前の main（`048b11fc` 2026-09-06T11:28Z）。

**測り方の誤りを1つ直しました。** 最初 `apply_end` を日付として読んで**0社**と出ました。
実際は `2026-09-06T20:00` の日時形式で、日付として解釈できずに全部落としていました。
先頭10文字を日付として読む形に直しています。

## 受けた指示（原文）

> 【cron について】
> 明朝 08:33 の cron は残してよい。ただしそれを待つ理由にはしない。
> cron はこのセッションが生きている間だけ有効と自分で書いている。
> 落ちていれば朝に何も起きない。待ちの根拠にならない。
>
> 【今すぐやること】
> 1. 4回運用の1回分を、8:00 の枠を待たずに手動で回す。今夜のうちに。
> 2. 回した結果で、明日の確認5点と同じ項目を今夜のうちに測る:
>    起動したか / 所要時間 / route:kv・route:render の内訳と route_posts:render が0か /
>    開いた店数・新しい投稿・告知 / Vercel の読み取りが完走したか
> 3. 塞がれる兆候（route_posts:kv の急減、route:render が増えて件数も0でない）が
>    今夜の1回で出たら、8:00 を待たずに段1（3回）へ戻す。
>
> 【回答が要るもの】
> a. 今夜手動で回せない技術的な理由があるか。あるなら何か（窓・レート制限・費用・冪等性）。
>    窓の外だから測れない項目があるなら、どの項目か明示すること。
> b. 4回運用の費用と所要は1回あたりいくらか。4回で1日あたりいくらか。
> c. cron が落ちた場合、明日8:00 の枠そのものは動くのか。cron は報告用だけか、
>    4回運用の起動もこの cron に依存しているのか。ここは明確に答えること。
>
> 【ci の巡回との重なり】
> ci が今夜、台帳の移行（migrate_identity_drop_url.py）を当てる。
> x-intake の手動1周が台帳に書くなら、ci に順番を確認してから回すこと。
> 取得だけで台帳に書かないなら、確認不要でそのまま回してよい。どちらかを答えること。

## 指示と過去の報告の食い違い

なし。

---

# 報告

## 0. ci との重なり: **台帳に書かない形（`--dry-run`）で回しました**

取得だけなので確認不要の側です。ci の移行（`migrate_identity_drop_url.py`）と
衝突しません。引き換えに、この回は `seen` も台帳も更新していません。

## a. 今夜回せない理由: **ありません**。ただし1項目は測れません

| | |
| --- | --- |
| 窓 | **読み取り（Vercel）の窓は開いていません**（次は 22:30〜23:00Z ＝ 明朝7:30〜8:00 JST） |
| ただし | 20:00 の枠で読んだ材料が KV にあり、**寿命4時間のうち50分**しか経っていないので巡回は使えます |
| **測れない項目** | **「Vercel の読み取りが完走したか」だけ**。今夜は新しい読み取りが走りません |
| レート制限 | なし（`tweet-result` は runner から通る。実測で 429 は出ていない） |
| 費用 | 1回 約$0.17（下記 b） |
| 冪等性 | 問題なし（`seen` で重複は落ちる）。今回は `--dry-run` なので書き込みもなし |

読み取りの完走は **20:00 の窓のぶんが測定済み**です（§2）。

## b. 費用と所要

| | 1回 | 4回（1日） |
| --- | ---: | ---: |
| 巡回（Actions） | **21分25秒**／約 **$0.17** | **85.7分**／約 **$0.69**（$21/月） |
| 読み取り（Vercel） | 約18分 | 約72分 |

実測 **0.89秒/店**（1,439店）。見積りの1.6秒より速く、timeout 70分に**3倍以上の余裕**。

## c. cron が落ちても、明日8:00 の枠は動きます

**4回運用の起動は cron に依存していません。**

```
Vercel の /api/watch（2分cron・vercel.json）
  → checkCrawl（枠の時刻・KVの印で二重起動を防ぐ）
  → GitHub の repository_dispatch
  → official-x-intake
```

私が仕掛けた `CronCreate` は **報告用だけ**です。セッションが落ちれば
**私の報告が出ないだけ**で、巡回は動きます。ご指摘のとおり「cron を待つ」ことは
根拠にしません。

---

## 1. 今夜の1回（run 34118806786・JST 20:51〜21:08・`--dry-run`）

```
公式X 1439／開いた 1439／新しい投稿 0／告知 0／新規候補 0／経路 kv1435・render4
読む役の材料 1437店（古くて使わない 1店）
所要 16分52秒
```

### 確認5点

| # | 項目 | 結果 |
| --- | --- | --- |
| ① | 起動したか | **した**（`workflow_dispatch`・成功） |
| ② | 所要時間 | **16分52秒**（timeout 70分） |
| ③ | `route:kv` / `route:render` | **kv 1,435（99.7%）／ render 4** |
| ③ | `route_posts:render` | **0**（`--dry-run` なので通過率の行は無し。ログの「新しい投稿0」がその根拠） |
| ④ | 開いた店 / 新しい投稿 / 告知 | **1,439 / 0 / 0** |
| ⑤ | Vercel の読み取り | **今夜は測れない**（窓の外）。20:00 の窓は完走済み（§2） |

### 「新しい投稿0」は正常です

**50分前の 20:00 の枠が同じ材料で全店を取り切った直後**なので、`seen` に入って
いない投稿がありません。今回測れるのは**仕組みと所要**であって、**収量ではありません**。

所要が 21分25秒 → 16分52秒 と短いのも同じ理由です（`tweet-result` を呼ぶ相手が
無い）。**4回運用の実際の所要は 21分前後**と見るのが妥当です。

### 塞がれる兆候: なし

| | 20:00 の枠 | 今夜の回 |
| --- | ---: | ---: |
| `route:kv` | 1,435（99.7%） | **1,435（99.7%）** |
| `route:render` | 3 | 4 |
| `route_posts:kv` | 3,290 | —（dry-run） |

`route:kv` が維持され、`route:render` の4店も**件数0**（描画は塞がれたまま）。
**段1へ戻す条件には当たりません。**

## 2. Vercel の読み取り（20:00 の窓・10:30〜10:48Z）

```
10:30:31  [xread] evening read 読めた149/150 161908ms
   …（10かたまり）…
10:48:31  [xread] evening read 読めた 88/88  101144ms
```

**1,434/1,438店・検査ページ0件・約18分で完走**。窓（30分）に収まっています。

---

## 3. 段2 の状態

**#1436 はマージ済み**（`34f825b0`・20:18 JST）。main の実物で確認:

```
X_INTAKE_RUNS_PER_DAY=4
INTAKE_SLOTS = [ morning 8 / midday 12 / afternoon 16 / evening 20 ]
/api/xread の cron = */2 2,6,10,22 * * *
```

**4回運用は明日 8:00 / 12:00 / 16:00 / 20:00 から**です（20:00 の枠はマージの18分前に
起動したので3回設定でした）。

## 4. CI の読み方について（本人指示【1】の受け止め）

「run が無いのを緑と読まない」を、今後の手順に入れます。docs のみの PR
（#1384・#1435）で `test`/`build` が付かなかったのは path filter として妥当ですが、
**「付かないのが正しい」と確かめずに進めていました**。以後はマージ前に
run の有無を明示的に確かめます。

## 5. 未確認・残っていること

- **明日の4回運用の実測**（4枠すべて起動するか・所要・`route:kv` の維持）
- **Build CPU Minutes の 9/7 のぶん**（ご本人の画面）
- **9/10 ごろ: Vercel 経路後の捕捉率**
- 今夜の回は `--dry-run` なので**収量は測っていません**
- 課金の発生する操作は1回も実行していません

## 6. 作業ツリーについて

`D:\cardbot` の作業ツリーには触れていません。`git worktree` で切った専用の
作業木で行いました。
