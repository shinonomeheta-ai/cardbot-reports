# #1417（設計図 v14）をマージ・写しの対象を v14 へ・#1373 を閉じる

日付: 2026-09-07（JST）
区切り/依頼名: 設計図 v14 の後始末（セッション docs）

## 受けた指示（原文）

> #1417 マージ（自分で。docs のみ）。
>
> S6・Wayback・暫定回/正式回・Amazon の例外が図に入り、S4・M3・M4・L2 に節が付いた。
> 「実装の関数名で裏を取っている」のが良い。
>
> ## マージ後
> - sync-docs.sh の写し対象を v12.png → v14.png に
> - #1373 を閉じる

（「判断待ち5件を1枚に整理」は別の報告 `2026-09-07-pending-decisions.md`）

## 指示と過去の報告の食い違い

なし。

## 報告

### 1. #1417 マージ（マージの条件 1〜4）

| 条件 | 実測 |
|---|---|
| 1. PR 固有の赤 0 | GitHub Actions は起動なし（変更は docs/ と tools/shot_architecture.mjs だけでパス条件に当たらない） |
| 2. Vercel | SUCCESS |
| 3. 承認の記録 | 上の指示文（この報告に転記） |
| 4. merge commit | **324d2a45**（2026-09-07 17:47:06 JST・`gh pr merge --merge`） |

変更 4 ファイル: `docs/target-architecture-v14.html`・`docs/target-architecture-v14.png`・`docs/target-architecture.md`・`tools/shot_architecture.mjs`。

### 2. sync-docs.sh の写し対象（cardbot-reports・push 済）

| 変更 | commit |
|---|---|
| 写し対象を `v12.png` → **`v14.png` ＋ `v14.html`**（図の元。`target-architecture.md` が「図の元は HTML」と書くので一緒に写す） | b89cb0a |
| `.claude/CLAUDE.md` を **origin/main から**取る形に（#1267/#1342 で管理下に入ったため。作業ツリーから写す `UNTRACKED` は空に） | b89cb0a |
| Git Bash の MSYS が `origin/main:.claude/CLAUDE.md` を Windows のパス一覧と誤認して「origin/main に無い」と誤判定 → `MSYS_NO_PATHCONV=1` を script 冒頭で export | b89cb0a |
| その副作用で `/d/…` 形式が効かなくなるので `SRC`・`REPO` を `D:/…` 形式に | aa8496b |
| 写しの更新（v14 png/html 追加・v12.png 削除・target-architecture.md 更新・CLAUDE.md は同一） | 147a870 |

実行結果: 4 本とも本体 origin/main（324d2a45）と md5 一致。

```
docs/target-architecture.md        一致
docs/target-architecture-v14.png   一致
docs/target-architecture-v14.html  一致
docs/CLAUDE.md                     一致
```

`docs/target-architecture-v12.png` の写しは削除した（本体には残っている）。

### 3. #1373 を閉じた

「#1417（v14・324d2a45）に含めてマージ済みのため閉じる」のコメント付きで close。枝 `docs/architecture-v13` は消していない。

### 4. 残っている食い違い（要判断・急がない）

`.claude/CLAUDE.md` の「Source of Truth」節は、まだ **`docs/target-architecture-v12.png`** を正本と書いている（10 行目・16 行目）。`target-architecture.md` 側は v14 に更新済みなので、CLAUDE.md だけが古い。直すなら 2 行の PR（#1413 とは分けてある）。

## 根拠データ

なし（マージ・写し・close の操作のみ。数字は上の表のとおり）。
