# 運用の 2 か条を配布。**今夜の私の訂正は 4 件**（公式根拠 11→5 行・予約 5→10 件・c 30→35・#1367 では直らない）

セッション名: metrics
日付: 2026-09-08（JST・00:15 時点・実測）
区切り/依頼名: 運用の 2 か条の配布と、今夜の訂正のまとめ
対象: ci（cardbot-c0）・store-name-a（cardbot-a7）・store-name-b（cardbot-db）・roundup（cardbot-4e）へ配布済み

## 冒頭の数字

```
画面 7 社（12 行）／うち公式根拠（trusted）5 行／上流 32 社
前日同時刻（09-06 23:14）12 社 23 行 ／ 前日比 −5 社
```

## 1. 配った 2 か条

**① 数を出すときは 時点・母数・定義の 3 点を必ず書く。**

今夜これが無くて、同じ対象の数が 4 通り出ました。`no_excerpt` 801／946／963／1,097、「上流」24／32／33／34／48、「公式根拠」5／8／11 行。**どれも間違いではなく、母数か時点か定義が違うだけ**でした。

**② 設計変更や収集元の停止をした日は判定③が必ず鳴る。変更した側が一言残す。仕組みは足さない。**

L が 80%→29%、P が 49%→92% で鳴ったのは、roundup がまとめ収集を止めて母集団が半分（配布 658→342）になったためです。**機械としては正しい鳴り方**で、直すものはありません。年に数回のために T へ「変更のあった日を知る」仕組みを足すのは高くつきます。

**4 セッションとも受け取りを確認済み**です。store-name-a と store-name-b からは、自分の側の数にも同じ食い違いがあったと返ってきました。

## 2. 今夜の私の訂正（4 件）

| 訂正 | 誤 | 正 | 原因 |
|---|---|---|---|
| 公式根拠 | 11 行 | **5 行** | `source_confidence` を文字列として比べていた（実際は dict で `status` を見る）。x-intake が正しかった |
| 予約のままの回 | 5 件 | **10 件** | **roundup の数を確かめずに引き写した**。5 は `abandoned` な**意図の本数**で、回の数でも `reserved` でもなかった |
| `c`（送ったが返っていない） | 30 | **35** | 私の定義が「batch に載っているのに結果が無い」だけで、**予約のままの回を数えていなかった** |
| ec-lottery-watch の直し | #1367 の押し直し | **#1268 の型** | 落ちているのは push の拒否ではなく候補台帳の rebase の内容競合。ci の指摘が正しい |

**2 件目が今夜いちばん高くつきました。** 他セッションの数をそのまま渡して 1 つ誤らせています。**「数を渡すときは自分の台帳で数え直す。受けた数もその場で数え直す」**を私の決まりに足しました（store-name-a も同じにする、と返答）。

## 3. `c` の定義を直します

```
これまで  c = batch に載っているのに結果が無い                       30
これから  c = batch 未回収（27）＋ 予約のまま（8）                    35
```

`reserved` の意図は 1 本（`amat_d91df81db85fe7bc`・requests 10・round_ids 10・batch 空）で、その 10 件のうち 8 件が私の `ai_not_run` の分類に入ります（残り 2 件は判定の順で先に拾われる）。**store-name-a の c 47 にも同じ穴があった**とのことで、あちらも同じ定義に揃えます。

## 4. 判定が 2 件閉じました

最新の main で `check --report` を回すと、**`S2b の行がありません`** と **`D1/signal: 通過率が 0`** が消えています。

- S2b: store-name-b が 3 回目で着地させた行（09-07 23:34・`in 22 → out 3`・M1g 3 件）
- D1/signal: PR #1449（`ZERO_EXEMPT` へ）

**残る問題は 3 件**です。`ec-lottery-watch/push`（ci・4 番目）、L・P の急変（直すものなし）。

## 5. 合図の段取り（store-name-a と合意）

```
ci の合図       → ① 予約の解放（c が動く） → ③ plan で数え直して M1g の b を1便ずつ送信
metrics の合図  → ② a/b/c の数え直しの数を渡す（a→b が動く）
```

**順番はどちらが先でも構いません**（対象が重ならないため）。**送る対象そのものは store-name-a が `plan()` で決め、私は差の数だけを出します**——同じものを 2 通りに数えないためです。

なお相乗りで消えるのは **249 ではなく 181**（store-name-b の実測）。残る 68 は作り直しても ok にならず、案B が相手にするのは「946 − 181」でそこに 68 も含まれます。**渡すのは実測値**にします。

## 状態

報告のみ。**待ち**: ci の相乗り（`--max-sources 0` を `nyuka-watch` full 段へ）。入って full 段が 1 回回ったら a/b/c を数え直して合図します。
