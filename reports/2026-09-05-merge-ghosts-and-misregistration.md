# 案A(a) 7件の統合・案B の絞り込み・(d) 誤登録の一覧

セッション名: store-name-a
日付: 2026-09-05（JST）
区切り/依頼名: 引きの別名対応・案A(a) → 案B の絞り込み → (d) の一覧化
対象: PR head（base `ff444554`）

## 受けた指示（原文）

> # 判断
>
> ## 1. PR #1285 をマージしてください
>
> ## 2. (a) 7件: 統合してください。ただし正規名は動かさない
> 行は本物（公式Xが付いている側）へ寄せ、正規名は現状のままにします。
> 「幽霊のほうが正しい名前」の4件は、observed_names に良い表記が残る形にして、
> 段3・段4 で店名の正本を扱うときにまとめて直します。
>
> ## 3. (b) 5件: 分けたままにします
> 案A のあとに案B の対象からこの5件を除いてください。
> ただし順番として、(a) の統合が終わってからにしてください。
>
> ## 4. (c) チェーン名と「各店」3件: 保留します
> ## 5. (d) 3件: 登録の見直しとして扱ってください
> 「チェーン名に支店の口が結ばれている」誤りとして、まとめて一覧にしてください。
>
> ## 順番
> 1. PR #1285 マージ / 2. (a) 7件の統合（正規名は動かさない）
> 3. (b) 5件を案B の対象から除く / 4. (d) の誤登録を一覧化

## 指示と過去の報告の食い違い

なし。

## 1. PR #1285 をマージしました

`ff444554`。マージ前に `shadow_url_owner.json` の競合が出たので、main の版を採って
材料に合わせて作り直しました（`--raw`）。

## 2. 案A(a) — 7件を統合（PR #1287）

`status="merged"` ＋ `merged_into` にしました。**行は消していません**（台帳の設計どおり）。
**正規名は動かしていません。** 幽霊側の綴りは行き先の `observed_names` に足したので、
段3・段4 で正本を扱うときにそこから直せます。

| 幽霊の行 | 統合先（正規名は変えず） | 根拠 |
| --- | --- | --- |
| `どみなり屋` | `カードゲームショップどみなり屋` | Xのプロフィール表示名『カードゲームショップどみなり屋…』。落ちた語は `カードゲーム` `ショップ`（どちらも `_NOISE`） |
| `カードゲームショップ オレタン松山店` | `オレタン松山店` | 支店名が完全に一致。落ちた語は `カードゲームショップ` |
| `トレカショップ竜のしっぽ 梅田NU茶屋町店` | `竜のしっぽ梅田nu茶屋町店` | 支店名が一致。表示名は『竜のしっぽ梅田NU茶屋町店』 |
| `ワーテル柏駅前` | `トレカショップ ワーテル柏駅前` | 支店名が一致。落ちた語は `トレカショップ` |
| `文教堂浦安西友店` | `文教堂浦安西友店トレカ` | 店名が完全に含まれる。表示名は『文教堂浦安西友店【トレカ】』 |
| `福福秋葉原店` | `福福トレカ秋葉原店` | 落ちた語は `トレカ`。本物は抽選1件・配布1行・公式サイトあり、幽霊は0件 |
| `トレカラビットバーン` | `ラビットバーン` | 落ちた語は `トレカ`。ほかに `ラビットバーン` を名乗る店は無い |

行き先の `observed_names` の実例:

```
カードゲームショップどみなり屋 : ['どみなり屋', 'カードゲームショップどみなり屋']
オレタン松山店             : ['オレタン松山店', 'カードゲームショップ オレタン松山店']
福福トレカ秋葉原店           : ['福福トレカ 秋葉原店', '福福トレカ秋葉原店', '福福秋葉原店']
```

**4件は幽霊のほうが正しい名前**でしたが、ご指示どおり正規名は動かさず、
正しい綴りを `observed_names` に残す形にしました。

## 3. 案B の絞り込み — 売り方が違う相手は止めない

#1285 の案Bは、**通販・オンラインと実店舗まで一括りにしていました**。5件が
探索の対象から外れ、本来必要な公式Xが登録されなくなっていました。

`別の綴りの索引` を「鍵 → 登録名の集合」に変え、`別の綴りで登録済みか` が
**売り方の印が一致する相手だけ**を登録済みと見なすようにしました。

```python
#: 売り方が違うことを表す語。**ここが食い違う相手は別の主体**として扱う。
_売り方 = re.compile(r"(オンライン|通販|ネット|online|ec)", re.I)
```

索引はハンドルを持たないままです（対象選び専用・権限には使えない）。

### 実測

```
探索の対象   1,284（案B前） → 1,267（案B） → 1,272（この PR）
残っている幽霊候補   18 → 0
   7件  統合した（案A(a)）
   5件  別の主体として対象へ戻した
   6件  段3 へ（(c) 3件・(d) 3件）
```

戻った5件:

```
イトーヨーカドー ネット通販 ← イトーヨーカドー各店
ノジマオンライン          ← ノジマ
ポケモンセンター          ← ポケモンセンターオンライン
ポケモンセンター店舗       ← ポケモンセンターオンライン
駿河屋 通販              ← 駿河屋
```

## 4. (d) の一覧 — チェーン名に支店の口が結ばれている

判定は「表の鍵がチェーン全体を指す名前（末尾が『店』でない／『各店』『店舗』
『全店』で終わる）なのに、そのハンドルのXプロフィール表示名は支店を名乗っている」。
**4件**見つかりました。

| 表の鍵（チェーン全体） | handle | Xのプロフィール表示名 | 種類 |
| --- | --- | --- | --- |
| `チャンピオン` | @tcmeieki | `トレカチャンピオン名駅店` | **支店の口**。ci セッションの `ビックカメラ店舗` と同じ型 |
| `ビックカメラ店舗` | @bic_takatsuki | `ビックカメラ高槻阪急スクエア店【公式】` | **支店の口**（ci セッションが報告済み） |
| `フルコンプ店舗` | @fullcomp_yoko | `フルコンプ横浜店` | **支店の口**（新規） |
| `CARD CREW` | @cardcrew_tuhan | `CARD CREW【カードクルー】トレカ通販店｜年内店舗OPEN予定` | **通販の口**。支店ではないが同じ [P1] の型 |

`チャンピオン` は前の報告の (d) で挙げたものです。`フルコンプ店舗` と `CARD CREW` は
今回の走査で新しく出ました。

**この4件はいずれも「チェーン全体の告知」として配ってよい根拠になりません。**
支店の口はその支店のことしか言えず、通販の口は通販のことしか言えません。

なお `ビックカメラ店舗` は、こちらの 2026-09-04 の報告（公式X表の誤登録15件）にも
入っています。**ci セッションの一覧とここで一致しました。**

## 5. 試験

```
python -m unittest test_discover_official_x_web test_discover_official_x
                   test_official_announce test_store_registry test_store_registry_p0
                   test_url_fields test_shadow_url_owner
  →  Ran 173 tests  OK
```

`売り方が違えば別の主体Tests` を5件足しました。

- 通販の登録が実店舗を止めない（`駿河屋 通販` / `駿河屋`）
- オンラインの登録が実店舗を止めない（`ポケモンセンター` / `ポケモンセンターオンライン`）
- 売り方が同じなら止めたまま（`福福秋葉原店` / `福福トレカ秋葉原店`）
- 両方が通販なら止める
- 印の取り出し（`イトーヨーカドー ネット通販` → `{ネット, 通販}`）

全体流し（7,227件）の赤は main と同じものだけです。

## 6. 手順について

前回の報告で書いた `git add -A` の件が**また起きました**。他セッションの未追跡
ファイル（`test_intake_adaptive_sleep.py`）を1度コミットに含めてしまい、
`git reset --soft` で外して commit し直しています。共有worktreeでは
`git add -A` を使わず、**ファイルを名指しする**ことにします。

## 7. CI（追記 2026-09-05 01:46 JST）

**main と同じ赤で、こちらの変更が増やした赤はありません。**
失敗した試験名を main（`ff444554`）のものと `diff` して、**完全に一致**しました。

```
python test   PR #1287  失敗13件   main（ff444554） 失敗13件   ← diff が空
```

```
test_event_id_registry ×3 / test_event_id_reuse / test_event_id_redirects
test_source_control_chars.test_制御文字が混ざっていない
test_dedupe_channel.実データ
test_candidate_ai_runner.抜粋の版
test_lottery_overrides ×3
test_collapse_same_deadline.実データ
```

`test_制御文字が混ざっていない`（`shadow_candidate_evidence_cache.json` の U+007F）は
main 側の実データの問題で、ci セッションへ渡すことになっています。

なお、この追記を最初に書いたとき、**bash のヒアドキュメントを引用符で囲まずに
使ったため、バッククォートと `$` が展開されて本文が壊れました**。書き直しています。
（`bash-heredoc` の同じ罠。パッチはファイルに書いてから実行する）

## 判断していただきたいこと

1. **PR #1287 をマージするか**（案A(a) 7件＋案B の絞り込み）。
2. **(d) の4件**をどうするか。`チャンピオン` `ビックカメラ店舗` `フルコンプ店舗` は
   支店の口、`CARD CREW` は通販の口です。外すか、正しい店名の行へ付け替えるか。
   `ビックカメラ店舗` は ci セッションの一覧にもあるので、突き合わせが要ります。
3. (c) の3件は段3、(d) の残り2件（`トレカショップFIRST` `hmvトレカショップ各店`）も
   段3のままです。

## 根拠データ

- [2026-09-05-chain-branch-misregistration.json](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/data/2026-09-05-chain-branch-misregistration.json)
  — (d) の4件（表の鍵・handle・Xのプロフィール表示名・掃除後の名前）

## 状態

- PR #1285 は**マージ済み**（`ff444554`）。
- **PR #1287 を出しました。マージはしていません**（指示待ち）。
- (c) と (d) の実施はしていません（一覧の提示のみ）。
- 外部への通信は行っていません。課金もありません。
