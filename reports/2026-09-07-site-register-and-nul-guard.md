# 公式サイト17件の登録（S1 の対象 141→158）と NUL の関門

セッション名: store-name-a
日付: 2026-09-07（JST）
区切り/依頼名: #1385 マージ・3件の追加登録・NUL の関門
対象: `origin/main` = `a53708ab`（#1385 マージ）／PR **#1387**

## 受けた指示（原文）

> ## 1. PR #1385: マージ（自分で）
> ## 2. カードラボ12支店に本部サイト: 付けない
> 支店にチェーンの本部サイトを付けると、本部の告知が支店の根拠になる。
> §3-1-1 の「権限は分ける」に反する。付けない側で正しい。
> ## 3. 3件（公式ページか False・タイトルは名乗る）: 登録する
> タイトルが店名を名乗っているなら、それが本人性の根拠。登録して、学習ホストにも足す。
> ## NUL
> publish-report.sh に NUL の検査を足してください。中身のバイトを見て、NUL があれば push を止める。
> ## 次
> 公式サイト14件（+3件）が登録されたので、S1 の対象が 141→158 に増える。

## 指示と過去の報告の食い違い

**1点あります。NUL の関門は、ご指示をいただく前に足してありました。**（下の「3.」）

---

# 1. PR #1385 マージ済み（`a53708ab`）

CI は main と同じ赤3件で**PR固有の赤 0**でした。公式サイト14件が入っています。

**カードラボ12支店には付けていません。** ご判断のとおりです——
支店にチェーンの本部サイトを付けると、**本部の告知が支店の根拠になってしまいます**。

---

# 2. 3件を追加登録 → PR #1387

```
ジョーシン（アプリ）      shop.joshin.co.jp   「ジョーシン::店舗情報」
トレカショップ スキップ    skip-tcg.com        「【販売専門】トレカショップ スキップ通販サイト」
レプトン各店           lepton.ne.jp        「レプトン（公式）広島で販売・買取…」
```

## 「学習ホストにも足す」は、この登録で済んでいました

`url_quality.store_hosts()` は **`store_sites.json` と `store_hosts_learned.json` の両方**を
読みます。`store_sites` へ書けば、そのままホストの判定に入ります。**別の作業は要りません。**

登録後の実測:

```
ジョーシン（アプリ）      host_belongs_to = True ／ 公式ページか = (True, '店の公式ホスト')
トレカショップ スキップ    host_belongs_to = True ／ 公式ページか = (True, '店の公式ホスト')
レプトン各店           host_belongs_to = True ／ 公式ページか = (True, '店の公式ホスト')
```

**`公式ページか` が False だったのは「学習ホストに無いだけ」で合っていました。**

## S1 の対象

```
141（前）→ 155（#1385 の14件）→ **158**（この3件）
```

`store_sites.json` の登録数を数えて **158** を確認しました。

---

# 3. NUL の関門 — **ご指示の前に足してありました**

前便を出す前に `publish-report.sh` へ入れ、push 済みです（`0da2ec0`）。

```
本文を置いたあと、書き込み前のバイト数と NUL を落としたバイト数を比べる
   食い違えば   「本文に NUL バイトが入っている」と出して**元の本文へ戻し、exit 1**
```

動作も確かめました。

```
NUL を含む本文   検知して止まる
普通の本文       通る（誤検知なし）
```

**理由をコメントに残しています。** 「実装側は `git diff --stat` の `Bin` で
気づけたが、この道具は中身のバイトを見ていなかったので報告側では気づけなかった。
文章の決まりではなく道具で止める」。

> **2回踏みました。** 1回目の顛末を書いている文章の中で、また同じ文字を書いています。
> 「書いてある＝避けられる、ではない」を、いちばん短い間隔で実演した形です。

---

# 4. KIDDY LAND のカード

ご認識のとおりです。**原宿店は ci の V7（発番）待ち**で、行が配布に出てから

```
期待   カードが「受付中 n件」になり、全店舗の行にキデイランドが出ない
```

を実測して報告します。**いまは「受付なし」で正しい**状態です
（キデイランドの配布行は二子玉川店・締切 9/3 の1件だけ）。

---

## 判断していただきたいこと

1. **PR #1387 をマージするか**

## 状態

- **#1385 マージ済み**（`a53708ab`）。**#1387 は開いただけ**（CI 待ち）。
- NUL の関門は `publish-report.sh` へ入れて push 済み。
- 課金はありません。
