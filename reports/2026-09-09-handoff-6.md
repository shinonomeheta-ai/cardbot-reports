# 引き継ぎ書（更新6）— 詰まりの正体は `ai_not_run` 294件。締切順を直した（2026-09-09）

宛先: 次のセッション
前任: Claude（セッション ci・2026-09-08 20:40 〜）

**前版**: [更新5](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/2026-09-09-handoff-5.md)

---

## 0. 本業の詰まり（本番と同じ引数で数え直した・確定値）

```
応募回 3,094件                        全部    うち締切が未来
  ai_not_run              1,522件      294件   ★ 最大の詰まり
  ended_or_no_end         1,044件        0件
  OK                        214件      209件
  missing_identity          172件       31件
  not_lottery                98件        0件
  ai_error                   38件       18件
  no_official_evidence        6件        6件
```

**締切が未来の558件のうち、294件（53%）が「AI にまだ聞いていない」だけ。**
これが「持っているのに画面に出ない」の主因。

`ai_not_run` 294件のうち、材料が揃っていて**実際に送れるのは 171件**。

---

## 1. 直した — AI へ聞く並びを締切の近い順に（`fix/ai-queue-by-deadline`・CI 緑）

### 何が起きていたか

並びは `sorted(現在)` ＝ **応募回IDの辞書順**（2026-08-22 の初期実装 `bc1f1f58` のまま。
順番に意図は無く、決定性のための辞書順）。**締切とは無関係。**

```
                いまの並び      締切順
上位 20件      生きた回 0件  →  20件
上位 40件            2件  →  40件
上位100件           13件  →  100件
```

**今日の20件は生きた回が1件も入っていなかった。** 全部、締切が過ぎた回に $1.93 を
払っていた。**費用は1円も変わらない。** 同じ20件で成果が0か20かの差。

### 段は3つ（`check_candidates_ai.並びの鍵()` の1か所で決める）

```
0 締切が未来   171件  近い順
1 締切が空     424件  暫定回。聞いて締切を読めれば正式な回が起きる（§5-1）
2 締切切れ     739件  新しく切れた順
```

**暫定回を締切切れより先にする。** 最後にすると `promote_provisional_rounds` の
経路が永久に回らない。同じ段の中は応募回IDの辞書順で決定的。

`今` を渡せる形にした（試験が時計に依存しないため。#1493 の轍を踏まない）。

**まだマージしていない。** CI は赤2件で main の基準と差分なし。

---

## 2. ★8社の正体 — **半分以上がトレカの抽選ではなかった**

前版で「候補に受付中の回があるのに配布に出ていない8社」を最優先と書いたが、
**中身を1件も見ていなかった。** 抜粋を読んだら:

| 社 | 中身 | 判定 |
| --- | --- | --- |
| **イエローサブマリン** | ポケカ 30th CELEBRATION 抽選販売・受付 9/16〜25 | **本物** |
| **竜星のPAO** | ポケカ MEGA 30th CELEBRATION カードセット | **本物** |
| HMV&BOOKS online | 商品名も抜粋も無い | 要確認 |
| お宝創庫 | 店のトップページを取っただけ（メニューの羅列） | 材料が違う |
| BIC CAMERA | Antigravity / Osmo 360 II（カメラ） | **トレカでない** |
| YAMADA | 防災バッグ・スピーカー・ヘアアイロン | **トレカでない** |
| ドン・キホーテ | キャリーケースの感想 → majica ポイント | **トレカでない** |
| セブンネット | 映画のポスター | **トレカでない** |

**`missing_identity`（商品名が空）で止まっているのは、関門が正しく働いている証拠。**

前版の「D＝商品名が空の9件を直す」は**やってはいけない**。直していたら、
**トレカでない企画を画面に出していた**（設計書 §0-1 の原則に真っ向から反する）。

### 本物2社が出ない理由

```
イエローサブマリン  材料=evidence_cache_missing  AIの答え=conflict
竜星のPAO          材料=evidence_cache_missing  AIの答え=なし（ai_not_run）
```

**2社とも抜粋が無い。** 抜粋が無いと AI に送れないので、締切順に並べても
送る対象に入らない。**次の相手はここ。**

---

## 3. 方針 B→D→A の現状（本人決定）

```
B 締切の近い順に聞く   **直した**（マージ待ち）
D 商品名が空を直す     **取り下げ。** 正しく止まっている（§2）
A 単価2.5倍の原因      未着手
```

**D の代わりに `evidence_cache_missing`（抜粋が無い）を見るべき。**
本物2社がここで止まっている。

---

## 4. 課金（実測）

```
2026-08   1,245回 $48.59   1回 $0.039
2026-09   1,025回 $99.11   1回 $0.097  ← 2.5倍。**9日で月の天井 $100 に到達**
```

1日20件＝月 $58（約¥8,700）。**いまの20件ですら当初予算（月¥5,000）を超えている。**
上限は `candidate-ai-daily.yml` に `max_requests=20` `max_usd=1.10` が直書き。

---

## 5. CI — 今日はじめて試験が全部緑になった

```
fix/classify-red3   Ran 8,141 tests  OK (skipped=66)   ← **試験は全部緑**
                    しかし step「テストがファイルを汚していないか」で赤
                      ?? history/passrate/2026-08-22/
                      ?? history/passrate/2026-09-09/python-test-<run_id>.jsonl
```

**今まで `unittest` が先に落ちていたので、この step まで到達していなかった。**
つまり**前から存在していて赤に隠れていた問題**が、初めて表に出た。

私の3つの試験（`test_event_id_redirects` / `test_candidate_ai_haiku_first` /
`test_ai_queue_order`）は**どれも汚さない**（手元で1つずつ確認）。
**犯人は未特定**——全件を走らせて絞り込み中。

**特定するまで `fix/classify-red3` をマージしない。** マージすると
「試験は緑なのに CI は赤」という分かりにくい状態が main に載る。

### 型分けの到達点

```
赤 8 → 2 ／ 飛ばし 19 → 66 ／ 移行中 6件 ／ データ契約 86件緑
A 本当のバグ 0件 ／ B 実データ 6件 ／ C 外の状態 3件（全部片付いた）
```

---

## 6. 未マージのブランチ（2本）

```
fix/classify-red3          月の天井の隔離／event_id_redirects を緑にして契約へ／
                           lottery_overrides のデータ実害を記録
                           → **汚れの犯人を特定してから**
fix/ai-queue-by-deadline   締切順（上位20件が 0件 → 20件）
                           → CI 緑・差分なし。**入れてよい**
```

---

## 7. 触ると危ないところ

* **`lottery_io.save()`** — 書き出す直前が `round_id` の唯一の確定点
* **データ契約** — 赤いものを入れると巡回が止まる
* **`load_tests` の配線** — 忘れると緑のまま何も見ていない
* **AI の件数・並び** — 1件あたり課金。上限を上げる判断は本人の承認
* **`event_id` 台帳** — `test_lottery_overrides` のデータ実害（5行が管理画面で409）

---

## 8. 本人にしかできないこと

管理画面 https://torecanavi.app/admin の**手動補正**タブ。

| 店 | いま | 直す | 締切 |
| --- | --- | --- | --- |
| **ブックオフプラス新宿駅西口** | `BRIGHTNESS` | `BRIGHTNESS OF HOPE` | **2026-09-09（当日）** |
| MINT GAMES池袋店 | `30th` | `30th CELEBRATION` | 2026-09-13 |

---

## 9. 前任が今日踏んだ誤り（14件）

推測で場所を2回外した／古い基準で CI を4回比べた／`round_id` 非空を関門にと言った
／直読み4本と言った／`url` を主キーにと言った／「本当のバグ3件」（全部違った）／
CI の名前の数え方を3回間違えた／印スクリプトの判定が甘く1件飛ばした／空の台帳の
鍵の名前を間違えた／`GI.チェーン()` の呼び方を間違えて「0社」／`rounds_of` に
`sc["candidates"]` を渡して「送れる回0件」／**`AI昇格できるか` に `date` を渡して
「3,094件中1,066件が型で落ちる」と誤報**（本番は文字列を渡す。報告直前に呼ぶ側を
見て気づいた）／**★8社を中身を見ずに「持っているのに出せていない」と数えた**
（半分以上がトレカでなかった）／heredoc の罠2回。

**共通する型: 実物を確かめる前に、名前や見た目で判断して数を出した。**
**効いた判断はどれも「先に測る道具を作った」もの。**
