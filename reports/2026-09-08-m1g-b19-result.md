# M1g の b 19件を送った結果 — 画面は +1社の見込み。次の関門は**商品名**

セッション名: store-name-a
日付: 2026-09-08（JST 10:00 実測）
区切り/依頼名: 第1便・第2便の回収と測定
対象: `origin/main`（回収後）

## 画面 7社（実時刻 22:03 の値を継続。今朝の再測は下の「4.」）／うち公式根拠 8行／上流 48社

---

# 0. 答え — **処理量ではありませんでした。次の関門は商品名（identity）です**

```
送った                    19件（$0.32・全件が Batch に入った）
AIの状態                  conflict 6 ／ insufficient_evidence 9 ／ likely_valid 4
**締切が入った**            **10件**（うち未来 7件）
**昇格まで通る**            **2件**（ガンギ・GANGI）
**画面に増える社**          **+1社の見込み**（GANGI。いま画面に出ていない社）
**楽天ブックス8件から出た社**  **0社**
```

**「1社でも増えれば処理量だった」——1社増える見込みです。ただし楽天ブックスは0社**で、
そこには別の関門がありました。

# 1. 落ちた理由

```
missing_identity   **8件**   店か商品が空（**全部「商品が空」**）
ended_or_no_end      6件    締切が読めない／読んだ締切が過去
昇格OK               2件    ガンギ
その他               3件
```

## `missing_identity` 8件は**全部「台帳の商品名が空」**

そのうち **4件は AI が商品名を読めています**。

```
ビックカメラ        台帳「空」 / AI「Antigravity エクスプローラーバンドル」（conflict）
楽天ブックス        台帳「空」 / AI「スクラブルジュニア」（confirmed）
楽天ブックス        台帳「空」 / AI「ガッツ 狂戦士の甲冑 “怒り”」（confirmed）
ドン・キホーテ       台帳「空」 / AI「ドンペン生誕祭2026 オリジナルタンブラー」（conflict）
```

**#1401 で直した締切と同じ型です。** AIは読めているのに、**読んだ商品名が台帳へ入る口が
ありません**。しかも商品名は `candidate_event_id` の材料なので、**書くとIDが動きます**——
締切のときに「暫定回を正式回として起こし直す」で解いたのと同じ構造の問題です。

> **ただし、そのまま埋めてはいけません。** 読めた4件のうち3件は
> **トレカではありません**（タンブラー・ボードゲーム・フィギュア）。商品名を埋めると
> **抽選でない回が画面へ出ます**。埋める前に「これは扱う商品か」の判定が要ります。

# 2. ②の検証 — **この19件では確かめられません**

ci の仮説「まとめ由来の締切が入っている回のほうが `conflict` で返りやすい」。

```
conflict 6件のうち、**締切（apply_end）が conflict だったのは 0件**
conflict の中身   product 5 ／ store 1 ／ apply_start 1
```

**`conflict` は全部「商品名」と「店名」の食い違い**で、締切ではありませんでした。
締切ありの回が2件（ガンギ・ビックカメラ）しかなく、その2件は**AIの読みと台帳が一致**
しています（日付は同じで、AIのほうが時刻まで読んだだけ）。

**仮説を否定も肯定もできません。** 締切ありの回を意図的に含めた便で測り直す必要があります。

# 3. 回収は3便ぶん・私のものは19件

```
08:31  11件 collected  ← 私
08:38   8件 collected  ← 私
08:53  10件 collected  ← **別セッション**（私のIDとの重なり0）
```

**課金**: 私の19件ぶん。1件あたりの実測が $0.0171 なので **$0.33 前後**の見込みです
（`actual_usd` は collect のあとに載るので、確定値は次便で出します）。

# 4. 画面の社数の変化

**まだ変わっていません。** 昇格まで通る2件は、次の巡回（`update-data` / `nyuka-watch`）の
`promote_candidate_decisions --apply` で行になります。**私は `--apply` を回していません**
（dry-run で確認しただけ）。次の巡回のあとに実測して報告します。

---

## 判断していただきたいこと

1. **「AIが読んだ商品名が台帳へ入らない」を次に直すか。** 締切（#1401）と同じ型で、
   **8件中8件がここで止まっています**。ただし**「扱う商品か」の判定が先に要ります**
   （読めた4件のうち3件がトレカ以外）
2. **②の検証のために、締切ありの回を集めた便を1つ送るか**（$0.2 程度）

## 状態

- **課金は19件ぶんのみ。** 予約は解放済み（`reserved` 0）
- ③ e-starbox は凍結のまま。`#1441` はコードだけで main 未反映
- 案Bの材料と**相手の数え直し（472 → 実際に効くのは283）**は store-name-b へ渡しました
