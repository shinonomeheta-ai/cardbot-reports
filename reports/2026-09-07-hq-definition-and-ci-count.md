# 本部（M1g）は画面 8 社・上流 34 社（画面は上流の部分集合）／CI の赤の数え方を訂正

セッション名: metrics
日付: 2026-09-07（JST・22:30 時点）
区切り/依頼名: 定義の確定・ci への引き渡し・判定②の 2 件・監視の棚卸し
対象: `origin/main` = `99c6cb7a`（09-07 22:10）

## 本部（M1g）の受付中

| 定義 | 今日 | 前回 |
|---|---:|---:|
| **画面**（配布行に受付中として出ている社数） | **8 社** | 16 社（09-06）→ **−8** |
| **上流**（応募回で受付中を持つ社数） | **34 社** | 30 社（09-07 06:16 の枠）→ **+4** |

**差の 26 社が関門の詰まりの大きさ**です。明日からこの 2 つを並べて頭に置きます。

## 受けた指示（原文・要点）

> 1. 「本部の受付中の社数」の定義が割れている。metrics 34社、store-name-a 8社。…定義を次で確定すること: 頭に置く数＝配布行（画面）／2番目の数＝上流（応募回）。両方を毎日並べる。store-name-a と突き合わせ、今夜どちらの定義で何社かを確定して報告すること。
> 2. 「kept 5 なのに配布行に結び付く回が 0」を ci に今すぐ渡すこと。
> 3. no_excerpt の母数が3つある（801 / 963 / ci の1,097）。ci と突き合わせて1つにすること。
> 4. 判定②が鳴っている2件は今夜動かせる。定時を待たないこと。
> 5. bggbklc38 は切れたら必ず張り直すこと。落ちたときに誰も気づかない形にしないこと。

## 指示と過去の報告の食い違い

**自分の報告の数を 1 件訂正します**（下の 2）。CI の赤の本数を、私は 6 本・5 本と書いてきましたが、**正しくは 5 本・4 本**でした。数え方が誤っていました（roundup の指摘）。

## 報告

### 1. 定義の確定（指示 1）

同じ判定（`golden_chain_index.チェーン`・支店を含めない）で両方を数えました。**割れていたのは定義だけで、数はどちらも正しい値でした。**

- **画面 8 社**: 晴れる屋2 4行・GIRAFULL 2・ポケモンカードストア 2・フルコンプ 2・ドラゴンスター 2・WonderGOO 1・ファミマオンライン 1・コジマ 1（配布 313 行中、本部の行は 59 行・うち受付中 15 行）
- **上流 34 社**: 応募回 2,910 のうち受付中 1,503 回、そのうち本部は 122 回

**画面の 8 社は上流 34 社の部分集合です**（画面にあって上流に無い社は 0）。レビュー担当の「1社も入っていない」は、私が前の報告で内訳を上流だけ書いたためで、実際は 8 社とも上流にあります。**上流にあって画面に無いのが 26 社**で、これが関門で止まっている社です（楽天ブックス 20 回・KIDS REPUBLIC 11 回・ポケモンセンター(実店舗) 10 回…が 1 行も画面に出ていない）。

### 2. CI の赤の数え方を訂正（roundup の指摘・自分の報告の訂正）

私はログ全体から `(FAIL|ERROR): test` を拾って数えていました。**そこには「別の試験が内側で回した unittest の出力」が混ざります。**

実測（run 34113401324・main の基準）:

```
要約行          FAILED (failures=3, errors=2, skipped=11)   ＝ 5 本
grep で拾った数  7 行（ユニーク 6）
```

余分な 2 行は `test_dedupe_channel.実データ` で、ログの 3720 行目・3889 行目に出ています（最後の要約は 12481 行目）。**本物の赤は要約行の直前の 5 本**です。

| 私が書いた数 | 本当の数 |
|---|---|
| #1429 の CI「赤 6 本」 | **5 本** |
| main の基準「赤 6 本」 | **5 本** |
| それ以前の「赤 5 本」 | **4 本** |

**マージの判断は変わりません。** PR 側と基準側の両方に同じ 2 行が同じ位置で出ており（行番号まで一致）、差を取る比較は成立していました。#1429 も #1421 も、要約行の本数（5 対 5）と名前の集合が一致しています。

**方法を直しました**: 今後は `failures=X, errors=Y` を読み、**要約行の直前の X+Y 件だけ**を赤とします。

### 3. ci へ渡したもの（指示 2・3）

roundup 経由で中継を依頼しました（ci のセッションを特定できないため）。

- **刻印から配布行への切断**: 9/6・9/7 に入った回で `kept 5` に対し `in_lotteries 0`。移行が成功して 227 件が台帳に入っても、ここが切れていれば画面は 1 行も変わりません。確かめ方（`passrate_funnel.cohorts` の `verdict/kept` と `in_lotteries`）を添えました。
- **`no_excerpt` の母数**: 私の 2 つの定義を明示しました。**801** ＝ 全応募回 2,748（#1430 の前）／**963** ＝ 全応募回 2,910（#1430 の後）。どちらも分母は「候補台帳の全応募回」で絞り込みなし。ci の 1,097 がどの母集団か（`plan()` の対象集合か `根拠の数()` の `evidence_cache_missing` か）を聞いています。

**明日の実測は「227 件が台帳に入ったか」で止めません。** 配布行が何行増えたか、画面の本部が何社増えたか、まで見ます。

### 4. 判定②の 2 件（指示 4）

同じ中継で今夜動かすよう伝えました。

- **S2b**: #1431 はマージ済みで書き手も入っているが、11:34 の run は `--probe` のみ。`--apply` を今夜 1 回回せば行が出て閉じます（store-name-b へ）。
- **D1/signal**: `roundup_seen.py` が workflow から呼ばれていない（「片方だけ配線」の型）。09:58 の run のログに 1 行も出ていません（roundup へ）。

### 5. 監視の棚卸しと、その弱さ（指示 5）

| 監視 | 何を見ているか | 期限 |
|---|---|---|
| `bi6cj940g` | health-watch の run の Check passrate の出力 | 期限なし（**このセッションが続く限り**） |
| `b0t56hwys` | `history/passrate/funnel/<日付>.json` が main に載るか・`V4/sendable` の行が入るか（**切れたので張り直した**） | 09-07 23:30 ごろ |

**弱さをそのまま書きます。** どちらも「このセッションが生きている限り」で、落ちたら誰も気づきません。x-intake の cron と同じ形です。

**ただし、鳴らす役は監視ではありません。** 判定が異常を見つけたときに Discord へ出すのは health-watch の `alert_incident`（GitHub Actions・JST 10:10 / 22:10）で、私の監視は**私が結果を読むため**のものです。私のセッションが落ちても、判定と通知は動き続けます。逆に、**health-watch 自身が止まったときの気づき役**は週 1 の生存確認（KV・`cardbot:passrate_watch`）で、これも監視とは独立です。

私の側の対策は 2 つです。切れたら張り直す（今夜実施）。そして、監視が落ちても**朝の報告で `history/passrate/` の中身を直接読んで確かめる**（監視の有無に依らない確認を必ず 1 回入れる）。

### 6. 申し送り（レビュー担当の全セッション宛の指示について）

「設計どおり」と書いた数を疑う、の対象として、私の側の残りを挙げます。

- **`not_official` 327**（#1430 の後）。1,076 のうち 749 は空白のずれでした。残る 327 が本当に「公式由来でない」かは、まだ 1 件も中身を見ていません。**次に疑うのはここ**です。
- **`no_eligible_evidence` 25・`event_store_mismatch` 25**。少数なので後回しにしていますが、同じ言葉の下にあります。

## 根拠データ

- [2026-09-07-hq-flow.json](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/data/2026-09-07-hq-flow.json) — 日ごとの出入りとリードタイム（前の報告と同じもの）

## 状態

報告のみ。**渡したもの**: ci へ 2 件・store-name-b へ 1 件・roundup へ 1 件（roundup 経由）。**判断待ち**: 画面／上流の 2 つの社数をコホートの日次へ足すか（実装は明日）。
