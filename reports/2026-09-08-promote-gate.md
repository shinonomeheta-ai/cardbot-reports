# **私の (c) は誤りでした。同じ形の関門が昇格の側に残っています**（`no_human_decision` ほか）／画面 6 社・上流 33 社

セッション名: metrics
日付: 2026-09-08（JST・03:10 時点）
区切り/依頼名: 25 社の落ち方を条件名で出す・x-intake と突き合わせる
対象: `origin/main`（`promote_candidate_decisions.py` を dry-run で実行）

## 受けた指示（原文・要点）

> 25社の541本が publish のどの条件で落ちているかを、条件名で出すこと。not_reviewed_ec なのか、別の名前の関門か。…食い違っている。明日いちばんにやることがこれで決まる。
> 26社と25社は別の集合。両方に入っている社が何を意味するかも整理すること。
> 【429 の痕跡0本は、まだ結論にできない】記録する仕組みが無かった期間の根拠に痕跡が残っているはずがない。
> 【数字】内訳の合計が 944 で 946 と2件合わない。

## 指示と過去の報告の食い違い

**私の (c)「人の確認を公開の前提にする関門は `not_reviewed_ec` が最後の 1 つ。開ければ通る相手は残っていない」は誤りです。訂正します。**

**x-intake が正しい。** 同じ形の関門が **`publish` の 1 段手前（昇格 `promote_candidate_decisions`）に残っています。** 私は `publish_reviewed_only` の中だけを見て「最後の 1 つ」と言っていました。

## 報告

### 1. 条件名（`promote` を dry-run で実行した実測）

**25 社の回は `publish` の関門で落ちていません。`publish` に届く前に、昇格で除外されています。**

全体（現役 2,830 回のうち除外 2,620 回）:

| 条件名 | 回 | 意味 |
|---|---:|---|
| **`no_human_decision`** | **1,526** | **人がまだ決着させていない** |
| **`pending_review`** | **734** | **修正は保存されているが人の確認がまだ** |
| **`human_stale`** | **105** | **人の確認がいまの材料と違う** |
| `folded_same_round` | 131 | 同じ回に畳んだ |
| `rejected_shadow_only` | 111 | 影だけで却下 |
| `ambiguous_new_rows` / `ambiguous_master_row` | 13 | 対応が決まらない |

**上の 3 つ（人の決着に依るもの）で 2,365 回**。`counts.rounds_without_human_decision` も 2,365 で一致します。

**配布行が 1 行も無い本部（私の測定で 15 社）の受付中の回**:

| 条件名 | 回 |
|---|---:|
| `no_human_decision` | **38** |
| `rejected_shadow_only` | 14 |
| `pending_review` | 1 |

**`not_reviewed_ec` は 1 件もありません**（#1349 で外れたため）。**代わりに `no_human_decision` が同じ働きをしています。**

### 2. なぜ「関門は残っていない」と誤ったか

`promote` には **AI の道**があります（今日 `ai_new_rows` 27 行）。条件は `semantic_status == likely_valid`。つまり昇格の関門は「**人の決着 または AI が likely_valid**」で、純粋な人の関門ではありません。

私は「AI の道があるから人の関門ではない」と読み、さらに 26 社の分析で **`likely_valid` を得た回を持つ社が 0 社**だったので、「詰まりは材料と処理量だけ」と結論しました。

**その 2 つは両立します。** 15 社は「人が決着していない」**かつ**「AI が likely_valid を出していない」。**どちらか一方でも開けば出ます。**

- **AI 側を通す** → 材料（抜粋 586）と処理量（V5 未判定）の話。**時間がかかる。**
- **人の側を開ける** → 設計書 §0-1 原則 4「人の確認を公開の前提にしない」から見れば、**`no_human_decision` は `not_reviewed_ec` と同じ形の関門**です。#1349 は `publish` の側だけを外し、**昇格の側は残った**。

**明日いちばんにやることは、後者を外すかどうかの判断です。** 私の側で数だけ出します。**外した場合に配布へ出うるのは、いま受付中の 53 回（15 社）**——ただし §8-1 の 2 条件（公式根拠 1 本以上・日付の食い違いなし）と AI 審査の関門は残るので、**全部が出るわけではありません**。#1349 のときと同じで、下見をしてから判断してください（#1349 は 27 回の `not_reviewed_ec` を外して実際に増えたのは 3 行でした）。

### 3. 2 つの集合の関係（指示・整理）

| 集合 | 定義 | 社 |
|---|---|---:|
| **A** | 上流に受付中の回があるが**画面に受付中が無い** | 27 |
| **B** | 上流に受付中の回があるが**配布行が 1 行も無い** | 15 |
| **A∩B** | **受付中の材料があるのに一度も配布に出ていない** | **15** |
| A だけ | 過去に配布へ出たが、いまは締切切れ等 | 12 |

**A∩B の 15 社**: BIC CAMERA・HOBBYSTATION・KIDS REPUBLIC・POKÉMON CARD LOUNGE・TSUTAYA・YAMADA・あみあみ・お宝創庫・イエローサブマリン・セブンネット・ドン・キホーテ・ノジマオンライン・ポケモンセンター(実店舗)・ヨドバシカメラ・竜星のPAO

**A だけの 12 社**: EDION・GANGI・GEO・GIRAFULL・イトーヨーカドー・オレタン・シーガル・ポケモンセンターオンライン・ヤマシロヤ・古本市場・楽天ブックス・駿河屋

**意味**: 両方に入る 15 社は**一度も届いたことがない**社で、直せば純増します。A だけの 12 社は**一度は届いた**ので経路は通っており、締切切れで落ちた社です。ご指摘の 9 社（TSUTAYA・BIC CAMERA・ポケモンセンター(実店舗)・イエローサブマリン・KIDS REPUBLIC・竜星のPAO・お宝創庫・あみあみ・YAMADA）は全部 **A∩B**、つまり**一度も届いていない側**です。

**x-intake の 25 社との差**は、私が「上流に**受付中**の回がある社」に絞っているためです（x-intake は候補があれば数えていると見られます）。**締切が未来の回に絞ると 15 社**、絞らなければもっと多くなります。x-intake の「10 社・82 件は締切がまだ未来」と私の 15 社・53 回は、締切の判定（実時刻か暦日か）と `rounds_of` 補正の差だと思われます。**x-intake の絞り方を教えてもらえれば揃えます。**

### 4. 429 の但し書き（指示・受け入れ）

**「429 の痕跡 0 本」は「429 が無かった」ではありません。「記録する仕組みが無かった」です。** api-customer の #1440（429 の診断）は今夜まだマージされていないので、それ以前の根拠に痕跡が残るはずがありません。**#1440 がマージされて数日回ってから見直します。** 前の報告にもこの但し書きを入れます。

### 5. 内訳の 2 件（指示・数字）

`no_excerpt` 946 の内訳の合計は **946** で合っています。前の報告の表で最後の行を「根拠が 0 本・その他 2」とまとめたため 944 に見えました。**差の 2 件は `fetch_status` が `ok` と `source_text_missing` の両方を持つ回**です（根拠が複数あり、片方は本文が取れ、片方は本文が無い）。

## 状態

**訂正**: 同じ形の関門は残っています（`no_human_decision` ほか・昇格の側）。**判断待ち**: 昇格の人の関門を外すか（外した場合の対象は受付中 53 回・15 社。ただし §8-1 と AI 審査の関門は残る）。**確認待ち**: x-intake の 25 社の絞り方・ci の配布行の版。
