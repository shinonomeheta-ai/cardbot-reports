# 上流 18 社の内訳——差は「締切で絞ったか」と「支店を親に寄せたか」の 2 つだけ

日付: 2026-09-08（JST・09:45 時点）
区切り/依頼名: 上流の数の食い違い（roundup 18 / metrics 30 / x-intake 34）
セッション名: roundup

## 冒頭（指定の形）

| | |
|---|---|
| **画面** | **6 社 10 行**（2026-09-08 09:45 JST・実時刻・`store_keys` の完全一致・支店を数えない） |
| **上流** | **18 社 32 回**（下の定義 A） |

## 受けた指示（原文）

> 上流18社32回（08:26）が他と合わない。x-intake 34社（08:40）、metrics 30社（06:26）。
> 昨夜確定した定義（rounds_of・superseded を除く・lifecycle active・
> 締切が実時刻で未来・本部のみ）のどこで数えたかを、時点・母数・定義の3点で出すこと。

## 報告

根拠データ: [2026-09-08-upstream-definition.json](https://github.com/shinonomeheta-ai/cardbot-reports/blob/main/reports/data/2026-09-08-upstream-definition.json)

### 1. 私が数えた 3 点

| | |
|---|---|
| **時点** | 2026-09-08 **09:45 JST**（`origin/main` の `acee7b40`。前の報告の 18 社は 08:26 の断面で、社の顔ぶれは同じ） |
| **母数** | `candidate_ai_ledger.rounds_of(shadow_candidates.json)` が返す応募回 **2,878**（`superseded_by` のある回は除かれる） |
| **定義** | 応募回の `lifecycle_status == "active"` **かつ** `apply_end` が**実時刻で今以降** **かつ** 候補の `store` が `golden_chains.json` の `store_keys` と**完全一致**（支店は数えない） |

**結果 18 社 32 回。** 昨夜確定した定義のとおりに数えており、外れているところはありません。

### 2. 同じ母数・同じ時点で、条件を 1 つずつ外して数え直した

| | 定義 | 社 | 回 |
|---|---|---:|---:|
| **A** | **本部のみ・回が active・締切が実時刻で未来**（＝私の数） | **18** | **32** |
| B | 本部のみ・回が active・**締切で絞らない** | 31 | 108 |
| G | **支店を親チェーンに寄せる**・回が active・締切で絞らない | 36 | 431 |

**A と B の差は「締切で絞ったか」だけ**です。active な回 108 のうち、締切が実時刻で今以降なのは 32 回しかありません。**B と G の差は「支店を親に寄せたか」だけ**です。

### 3. どの数がどの定義に当たるか

| 出どころ | 数 | 一番近い定義 | 差の理由 |
|---|---:|---|---|
| roundup（私） | 18 社 | **A** | — |
| metrics（06:26） | 30 社 | **B**（いま 31 社） | **締切で絞っていない**。私も前の報告で「metrics の上流 32 社は締切で絞らない集計」と書いています |
| x-intake（08:40） | 34 社 | **G**（いま 36 社） | **支店を親チェーンに寄せている**（＋締切で絞らない）。1〜3 社の差は断面の時刻差 |

**B にあって A に無い 13 社**（締切が過ぎた回しか持たない本部）:
EDION・GEO・HOBBYSTATION・KIDS REPUBLIC・TSUTAYA・イトーヨーカドー・オレタン・セブンネット・ノジマオンライン・ポケモンセンター(実店舗)・ヨドバシカメラ・古本市場・駿河屋。

**G にあって B に無い 5 社**（支店の回しか持たない本部）:
C-labo・KIDDY LAND・WonderGOO・あみあみ・プレイズ。

### 4. どれが正か（私の見方）

**「いま応募できる本部」を数えるなら A（18 社）が正**です。§3-1-1-c で支店の告知は本部として数えず、締切は実時刻で見る、という昨夜確定した規則そのままです。

ただし **B と G が誤りとは限りません**。「材料がどれだけ来ているか」を見るなら締切で絞らない B が要りますし、「支店を含めてどれだけ届いているか」を見るなら G が要ります。**同じ「上流」という言葉で 3 つの別の量を呼んでいたことが、食い違いの正体**です。以後、私は上流の数を出すときに **A** と明記します。

## 実行したこと・していないこと

- `origin/main` から使い捨ての作業木を切って数え直しただけ（通信なし・書き込みなし・課金 0）
- 定義を変える提案はしていません。数え方の対応表を出しただけです
- ci の #1455 の時刻を実測（マージ 01:48・`19d12fea`）に直し、前の報告の該当行を訂正しました

## 待機について

指示どおり待機します。収集元は 3 つとも止まっており、合図はまとめX の 4 口（HTTP 403）が読めるまで出ません。**合図 0 は結論にしません。** 読めるようになったら差分を出します。`roundup-daily` の schedule はそのままです。

中継は外れます。詰まったら声を掛けてください。
