# 第1便が送信の手前で落ちました（根拠キャッシュの関門）

セッション名: store-name-a
日付: 2026-09-07（JST）
区切り/依頼名: V5 の画像読み 10便
対象: `origin/main` = `8aa483e4`（#1398 マージ）

## 受けた指示（原文）

> ## 1. #1398: マージ（自分で）
> ## 2. 案2。いま10便まで送ってください
> 10便を順に送って、collect を回して、締切が入った数と配布に出た数を報告。

## 指示と過去の報告の食い違い

**あります。10便を順に送る前に、第1便が落ちました。** 原因が分かるまで残りは送っていません。

---

# 1. PR #1398 マージ済み（`8aa483e4`）

---

# 2. 第1便は**送信されていません**（課金0）

```
run 34062884937   candidate-ai-daily / submit / 20件
   → 子の candidate-ai-review 34063245208 が failure
   → 「X根拠取得、候補のキャッシュ生成、またはAI確認が失敗しました」
```

**AI の台帳に、この20件の記録は1件も入っていません**（`記録なし`）。
**送る前に落ちているので、課金は発生していません。**

## 原因

`shadow_candidate_ai_health.json` にこう出ていました。

```
error_rounds                      20        ← ちょうど私の20件
evidence_cache_missing            1,624
evidence_cache_missing_breakdown  excerpt_missing 633 ／ excerpt_unsendable 966 ／
                                  no_eligible_evidence 25
```

**根拠キャッシュ（送れる抜粋）が無い回は、送信の対象から外れます。**
私が選んだのは `crnd_prov_…`＝**仮の応募回**で、抜粋がまだ作られていませんでした。

> **見落としです。** 「画像あり・締切なし」で選んだだけで、
> **送れる状態か**を確かめていませんでした。

## やり直し

workflow にちょうどこのための入口があります。

```
include_missing_evidence   「根拠抜粋が無い回も（推測禁止のまま）送る」
```

**同じ20件を、この印を立てて送り直しました**（run `34063784224`・実行中）。

**画像は抜粋ではなく `media_urls` から積むので、抜粋が無くても画像は渡ります。**
今回いちばん効かせたいのは画像なので、この形で目的は満たせるはずです。

---

# 3. 残り9便は、まだ送っていません

**同じ落ち方をする便を9つ積んでも意味がない**ためです。
送り直しが**実際に Batch を作れたことを確かめてから**、残りを続けます。

```
いま     第1便（やり直し）実行中
確認する  ① Batch が作られたか（shadow_candidate_ai_health の haiku_batches に増えるか）
         ② 実費（この便でいくら使ったか）
確認できたら   第2便〜第10便を順に送る → collect → 締切が入った数・配布に出た数
```

**ご指示（10便を送る）に対して、いま送れているのは1便ぶんです。**
理由は上のとおりで、**止めているのではなく、直して送り直しているところ**です。

---

## 判断していただきたいこと

1. **やり直しが通ったら、残り9便をそのまま送ってよいか**（ご指示どおり進めます）
2. **`include_missing_evidence` を立てて送ることでよいか。**
   「根拠抜粋が無い回も、推測禁止のまま送る」——**抜粋が無いぶん、
   AI が答えられない回も増える**はずです。歩留まりは下がる可能性があります

## 状態

- **課金は発生していません**（送信の手前で落ちたため）。
- 第1便（やり直し）が実行中。**残り9便は未送信。**
